script: |
  super -f text -I q96.spq | sort > q96.out

inputs:
  - name: q96.spq
    data: |
      SELECT a+b*2+c*3,
      (a+b+c+d+e)/5
      FROM t1 (FORMAT parquet)
      WHERE b>c
      AND a>b
      AND (e>a AND e<b);
  - name: t1

outputs:
  - name: q96.out
    data: |
