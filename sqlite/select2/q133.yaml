script: |
  super -f text -I q133.spq | sort > q133.out

inputs:
  - name: q133.spq
    data: |
      SELECT abs(b-c),
      (a+b+c+d+e)/5,
      d
      FROM t1 (FORMAT parquet)
      WHERE (e>a AND e<b)
      AND a IS NULL;
  - name: t1

outputs:
  - name: q133.out
    data: |
