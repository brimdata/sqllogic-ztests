script: |
  super -f text -I q96.spq | sort > q96.out

inputs:
  - name: q96.spq
    data: |
      SELECT b-c
      FROM t1 (FORMAT parquet)
      WHERE a IS NULL
      AND (e>c OR e<d);
  - name: t1

outputs:
  - name: q96.out
    data: |
      -1
