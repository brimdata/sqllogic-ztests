script: |
  super -f text -I q239.spq | sort > q239.out

inputs:
  - name: q239.spq
    data: |
      SELECT d-e
      FROM t1 (FORMAT parquet)
      WHERE (e>a AND e<b);
  - name: t1

outputs:
  - name: q239.out
    data: |
      -2
      1
      2
