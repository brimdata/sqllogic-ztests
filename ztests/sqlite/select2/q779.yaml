script: |
  super -f text -I q779.spq | sort > q779.out

inputs:
  - name: q779.spq
    data: |
      SELECT a+b*2
      FROM t1 (FORMAT parquet)
      WHERE a>b
      AND coalesce(a,b,c,d,e)<>0
      AND a IS NULL;
  - name: t1

outputs:
  - name: q779.out
    data: |
