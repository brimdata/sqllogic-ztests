script: |
  super -f tsv -I q779.sql | sort > q779.out

vector: true

inputs:
  - name: q779.sql
    data: |
      SELECT a+b*2
      FROM t1
      WHERE a>b
      AND coalesce(a,b,c,d,e)<>0
      AND a IS NULL;
  - name: t1

outputs:
  - name: q779.out
    data: |
