script: |
  super -f tsv -I q900.sql | sort > q900.out

vector: true

inputs:
  - name: q900.sql
    data: |
      SELECT d-e,
      a,
      d
      FROM t1
      WHERE (e>c OR e<d)
      AND a IS NULL;
  - name: t1

outputs:
  - name: q900.out
    data: |
      4	NULL	114
