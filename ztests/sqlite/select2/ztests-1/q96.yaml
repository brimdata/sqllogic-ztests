script: |
  super -f tsv -I q96.sql | sort > q96.out

vector: true

inputs:
  - name: q96.sql
    data: |
      SELECT b-c
      FROM t1
      WHERE a IS NULL
      AND (e>c OR e<d);
  - name: t1

outputs:
  - name: q96.out
    data: |
      -1
