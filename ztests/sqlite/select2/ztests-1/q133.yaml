script: |
  super -f tsv -I q133.sql | sort > q133.out

vector: true

inputs:
  - name: q133.sql
    data: |
      SELECT abs(b-c),
      (a+b+c+d+e)/5,
      d
      FROM t1
      WHERE (e>a AND e<b)
      AND a IS NULL;
  - name: t1

outputs:
  - name: q133.out
    data: |
