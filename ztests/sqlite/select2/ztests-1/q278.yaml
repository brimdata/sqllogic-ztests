script: |
  super -f tsv -I q278.sql | sort > q278.out

vector: true

inputs:
  - name: q278.sql
    data: |
      SELECT a,
      c
      FROM t1
      WHERE c BETWEEN b-2 AND d+2
      AND a IS NULL
      AND (a>b-2 AND a<b+2);
  - name: t1

outputs:
  - name: q278.out
    data: |
