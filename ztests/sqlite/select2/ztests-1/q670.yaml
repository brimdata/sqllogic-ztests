script: |
  super -f tsv -I q670.sql | sort > q670.out

vector: true

inputs:
  - name: q670.sql
    data: |
      SELECT a+b*2,
      abs(b-c),
      a
      FROM t1
      WHERE a IS NULL
      AND (e>c OR e<d)
      AND c>d;
  - name: t1

outputs:
  - name: q670.out
    data: |
