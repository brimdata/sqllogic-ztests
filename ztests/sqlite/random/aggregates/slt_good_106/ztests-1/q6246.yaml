script: |
  super -f text -I q6246.spq | sort > q6246.out

inputs:
  - name: q6246.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT NULL >= ( - ( + col1 ) );
  - name: tab1

outputs:
  - name: q6246.out
    data: |
