script: |
  super -f tsv -I q471.sql | sort > q471.out

vector: true

inputs:
  - name: q471.sql
    data: |
      SELECT ALL * FROM tab1 WHERE NOT NULL NOT IN ( + 85 * - col1 );
  - name: tab1

outputs:
  - name: q471.out
    data: |
