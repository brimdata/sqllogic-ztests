script: |
  super -f tsv -I q154.sql | sort > q154.out

vector: true

inputs:
  - name: q154.sql
    data: |
      SELECT DISTINCT * FROM tab0 WHERE NOT ( NULL ) > + 11 + - col1;
  - name: tab0

outputs:
  - name: q154.out
    data: |
