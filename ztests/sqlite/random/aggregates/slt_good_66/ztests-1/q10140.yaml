script: |
  super -f tsv -I q10140.sql | sort > q10140.out

vector: true

inputs:
  - name: q10140.sql
    data: |
      SELECT * FROM tab1 WHERE NOT col0 NOT IN ( col1 * col1 * - col0 * + col1 );
  - name: tab1

outputs:
  - name: q10140.out
    data: |
