script: |
  super -f text -I q10140.spq | sort > q10140.out

inputs:
  - name: q10140.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT col0 NOT IN ( col1 * col1 * - col0 * + col1 );
  - name: tab1

outputs:
  - name: q10140.out
    data: |
