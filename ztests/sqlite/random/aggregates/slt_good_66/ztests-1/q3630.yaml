script: |
  super -f text -I q3630.spq | sort > q3630.out

inputs:
  - name: q3630.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) AS cor0 WHERE NOT NULL BETWEEN + ( + col2 ) + + col2 * - + col1 AND + 59;
  - name: tab0

outputs:
  - name: q3630.out
    data: |
