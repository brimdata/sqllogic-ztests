script: |
  super -f tsv -I q62.sql | sort > q62.out

vector: true

inputs:
  - name: q62.sql
    data: |
      SELECT * FROM tab1 WHERE col1 + - col1 + col2 * - + 91 IS NULL;
  - name: tab1

outputs:
  - name: q62.out
    data: |
