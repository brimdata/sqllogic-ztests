script: |
  super -f tsv -I q959.sql | sort > q959.out

vector: true

inputs:
  - name: q959.sql
    data: |
      SELECT * FROM tab2 WHERE NOT ( + col1 + - col0 ) IS NOT NULL;
  - name: tab2

outputs:
  - name: q959.out
    data: |
