script: |
  super -f text -I q588.spq | sort > q588.out

inputs:
  - name: q588.spq
    data: |
      SELECT + col2 * col1 FROM tab1 (FORMAT parquet) WHERE NOT ( + col2 NOT IN ( + col0 ) );
  - name: tab1

outputs:
  - name: q588.out
    data: |
