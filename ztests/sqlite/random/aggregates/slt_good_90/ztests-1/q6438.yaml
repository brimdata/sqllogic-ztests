script: |
  super -f text -I q6438.spq | sort > q6438.out

inputs:
  - name: q6438.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT NULL BETWEEN + - col0 / + col2 + col0 * ( + 89 ) AND ( + CAST ( + col0 AS REAL ) / - 26 );
  - name: tab0

outputs:
  - name: q6438.out
    data: |
