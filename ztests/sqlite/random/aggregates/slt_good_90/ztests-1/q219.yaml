script: |
  super -f tsv -I q219.sql | sort > q219.out

vector: true

inputs:
  - name: q219.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL IN ( + 35 * - - col2 );
  - name: tab1

outputs:
  - name: q219.out
    data: |
