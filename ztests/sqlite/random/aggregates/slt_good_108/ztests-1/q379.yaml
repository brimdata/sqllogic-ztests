script: |
  super -f text -I q379.spq | sort > q379.out

inputs:
  - name: q379.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT NULL <= col0;
  - name: tab1

outputs:
  - name: q379.out
    data: |
