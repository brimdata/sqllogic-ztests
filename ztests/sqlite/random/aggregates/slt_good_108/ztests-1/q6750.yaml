script: |
  super -f text -I q6750.spq | sort > q6750.out

inputs:
  - name: q6750.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( col2, col1 * - col1, + ( col2 ) + + col2 + col1 + col1 * - ( + - col2 ) );
  - name: tab1

outputs:
  - name: q6750.out
    data: |
