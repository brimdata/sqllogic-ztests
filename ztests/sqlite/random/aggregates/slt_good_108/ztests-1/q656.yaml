script: |
  super -f tsv -I q656.sql | sort > q656.out

vector: true

inputs:
  - name: q656.sql
    data: |
      SELECT * FROM tab1 WHERE NULL NOT BETWEEN ( + col2 * 84 ) AND col0;
  - name: tab1

outputs:
  - name: q656.out
    data: |
