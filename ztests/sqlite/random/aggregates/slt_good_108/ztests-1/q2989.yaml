script: |
  super -f text -I q2989.spq | sort > q2989.out

inputs:
  - name: q2989.spq
    data: |
      SELECT + col2 - 63 FROM tab2 (FORMAT parquet) WHERE NOT col1 - + col1 IS NOT NULL;
  - name: tab2

outputs:
  - name: q2989.out
    data: |
