script: |
  super -f tsv -I q6856.sql | sort > q6856.out

vector: true

inputs:
  - name: q6856.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT + + col0 - - - ( + col2 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q6856.out
    data: |
