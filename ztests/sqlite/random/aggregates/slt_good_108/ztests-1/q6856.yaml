script: |
  super -f text -I q6856.sql | sort > q6856.out

inputs:
  - name: q6856.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT + + col0 - - - ( + col2 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q6856.out
    data: |
