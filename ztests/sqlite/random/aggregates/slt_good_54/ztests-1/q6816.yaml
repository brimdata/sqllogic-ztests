script: |
  super -f tsv -I q6816.sql | sort > q6816.out

vector: true

inputs:
  - name: q6816.sql
    data: |
      SELECT - MIN ( + col2 ) FROM tab1 WHERE NULL IS NOT NULL;
  - name: tab1

outputs:
  - name: q6816.out
    data: |
      NULL
