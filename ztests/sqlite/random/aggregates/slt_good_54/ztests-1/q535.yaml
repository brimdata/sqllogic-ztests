script: |
  super -f tsv -I q535.sql | sort > q535.out

vector: true

inputs:
  - name: q535.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL BETWEEN ( + - col1 ) AND + col2 * col1 * - - 69;
  - name: tab1

outputs:
  - name: q535.out
    data: |
