script: |
  super -f text -I q535.spq | sort > q535.out

inputs:
  - name: q535.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL BETWEEN ( + - col1 ) AND + col2 * col1 * - - 69;
  - name: tab1

outputs:
  - name: q535.out
    data: |
