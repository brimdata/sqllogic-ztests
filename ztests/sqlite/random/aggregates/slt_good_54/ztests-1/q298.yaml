script: |
  super -f text -I q298.spq | sort > q298.out

inputs:
  - name: q298.spq
    data: |
      SELECT ALL * FROM tab2 (FORMAT parquet) WHERE NOT NULL BETWEEN + col1 * 75 + col2 AND ( - col1 / + col1 );
  - name: tab2

outputs:
  - name: q298.out
    data: |
