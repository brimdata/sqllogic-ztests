script: |
  super -f text -I q509.spq | sort > q509.out

inputs:
  - name: q509.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) AS cor0 WHERE NOT col0 IS NULL AND ( col2 * - - col2 ) <> ( NULL );
  - name: tab2

outputs:
  - name: q509.out
    data: |
