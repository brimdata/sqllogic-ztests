script: |
  super -f tsv -I q6413.sql | sort > q6413.out

vector: true

inputs:
  - name: q6413.sql
    data: |
      SELECT DISTINCT * FROM tab1 AS cor0 WHERE NOT ( + col2 ) + ( + col0 ) BETWEEN NULL AND - col1 * + col1 * - 81;
  - name: tab1

outputs:
  - name: q6413.out
    data: |
