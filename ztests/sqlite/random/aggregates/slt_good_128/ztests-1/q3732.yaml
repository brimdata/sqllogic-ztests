script: |
  super -f text -I q3732.spq | sort > q3732.out

inputs:
  - name: q3732.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + ( CAST ( NULL AS INTEGER ) ) + - col0 * col0 IS NULL;
  - name: tab1

outputs:
  - name: q3732.out
    data: |
