script: |
  super -f tsv -I q2121.sql | sort > q2121.out

vector: true

inputs:
  - name: q2121.sql
    data: |
      SELECT + + col2 * 2 * - col1 * - CAST ( NULL AS INTEGER ) AS col0 FROM tab2 WHERE NULL NOT IN ( - col1 * - col0, - col1 ) OR 3 IS NULL;
  - name: tab2

outputs:
  - name: q2121.out
    data: |
