script: |
  super -f text -I q4346.spq | sort > q4346.out

inputs:
  - name: q4346.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) AS cor0 CROSS JOIN tab2 (FORMAT parquet) AS cor1 WHERE ( ( ( NOT ( + - 4 ) IS NOT NULL ) ) );
  - name: tab2

outputs:
  - name: q4346.out
    data: |
