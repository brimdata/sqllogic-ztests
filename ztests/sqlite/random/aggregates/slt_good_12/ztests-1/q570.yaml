script: |
  super -f tsv -I q570.sql | sort > q570.out

vector: true

inputs:
  - name: q570.sql
    data: |
      SELECT DISTINCT COUNT ( * ) + - COUNT ( * ) FROM tab2 WHERE NOT NULL = ( NULL );
  - name: tab2

outputs:
  - name: q570.out
    data: |
      0
