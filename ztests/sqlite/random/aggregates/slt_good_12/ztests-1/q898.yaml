script: |
  super -f text -I q898.spq | sort > q898.out

inputs:
  - name: q898.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NULL IS NOT NULL AND NOT col1 IS NULL;
  - name: tab0

outputs:
  - name: q898.out
    data: |
