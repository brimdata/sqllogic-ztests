script: |
  super -f tsv -I q172.sql | sort > q172.out

inputs:
  - name: q172.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT - col2 >= NULL;
  - name: tab1

outputs:
  - name: q172.out
    data: |
