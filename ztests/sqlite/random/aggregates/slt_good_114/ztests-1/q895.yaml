script: |
  super -f tsv -I q895.sql | sort > q895.out

vector: true

inputs:
  - name: q895.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT - col1 >= + col1 - + + col0;
  - name: tab1

outputs:
  - name: q895.out
    data: |
      91	47	68
