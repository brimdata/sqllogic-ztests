script: |
  super -f text -I q1094.spq | sort > q1094.out

inputs:
  - name: q1094.spq
    data: |
      SELECT + col0 * - + col1 FROM tab2 (FORMAT parquet) WHERE NOT ( + + col0 ) <> ( col1 );
  - name: tab2

outputs:
  - name: q1094.out
    data: |
