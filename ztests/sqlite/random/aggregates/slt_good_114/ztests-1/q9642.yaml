script: |
  super -f text -I q9642.spq | sort > q9642.out

inputs:
  - name: q9642.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + col0 + CAST ( NULL AS INTEGER ) IN ( col2 + col0 );
  - name: tab1

outputs:
  - name: q9642.out
    data: |
