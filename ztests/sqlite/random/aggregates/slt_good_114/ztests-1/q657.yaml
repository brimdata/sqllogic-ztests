script: |
  super -f text -I q657.spq | sort > q657.out

inputs:
  - name: q657.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT + col1 >= - 24;
  - name: tab2

outputs:
  - name: q657.out
    data: |
