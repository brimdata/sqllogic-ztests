script: |
  super -f tsv -I q657.sql | sort > q657.out

vector: true

inputs:
  - name: q657.sql
    data: |
      SELECT * FROM tab2 WHERE NOT + col1 >= - 24;
  - name: tab2

outputs:
  - name: q657.out
    data: |
