script: |
  super -f text -I q4101.spq | sort > q4101.out

inputs:
  - name: q4101.spq
    data: |
      SELECT + ( - col2 ) + 94 FROM tab1 (FORMAT parquet) WHERE NOT NULL < ( + col0 + + + col2 );
  - name: tab1

outputs:
  - name: q4101.out
    data: |
