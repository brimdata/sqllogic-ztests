script: |
  super -f text -I q6912.spq | sort > q6912.out

inputs:
  - name: q6912.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT col0 * + col0 + + + col2 * - col1 NOT IN ( + col2, - col0 );
  - name: tab2

outputs:
  - name: q6912.out
    data: |
