script: |
  super -f text -I q78.spq | sort > q78.out

inputs:
  - name: q78.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT + - CAST ( NULL AS INTEGER ) * col0 / col1 IS NULL;
  - name: tab2

outputs:
  - name: q78.out
    data: |
