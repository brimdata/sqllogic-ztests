script: |
  super -f tsv -I q5172.sql | sort > q5172.out

vector: true

inputs:
  - name: q5172.sql
    data: |
      SELECT DISTINCT + COUNT ( * ) FROM tab1 WHERE + col2 IS NULL;
  - name: tab1

outputs:
  - name: q5172.out
    data: |
      0
