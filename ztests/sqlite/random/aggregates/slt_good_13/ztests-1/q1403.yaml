script: |
  super -f text -I q1403.spq | sort > q1403.out

inputs:
  - name: q1403.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) AS cor0 CROSS JOIN tab0 (FORMAT parquet) cor1 WHERE NULL >= ( NULL );
  - name: tab0

outputs:
  - name: q1403.out
    data: |
