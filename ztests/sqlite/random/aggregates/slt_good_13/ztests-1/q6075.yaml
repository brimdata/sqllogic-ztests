script: |
  super -f text -I q6075.spq | sort > q6075.out

inputs:
  - name: q6075.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) AS cor0 CROSS JOIN tab0 (FORMAT parquet) AS cor1 WHERE ( NOT NULL IS NULL );
  - name: tab0

outputs:
  - name: q6075.out
    data: |
