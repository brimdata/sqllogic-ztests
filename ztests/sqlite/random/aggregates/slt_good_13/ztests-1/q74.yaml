script: |
  super -f text -I q74.spq | sort > q74.out

inputs:
  - name: q74.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT + - col0 * - - ( + CAST ( NULL AS INTEGER ) ) = 28 * col1 * + - col2 * - + col1;
  - name: tab1

outputs:
  - name: q74.out
    data: |
