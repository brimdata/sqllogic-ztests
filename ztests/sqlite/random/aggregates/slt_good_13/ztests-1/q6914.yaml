script: |
  super -f text -I q6914.spq | sort > q6914.out

inputs:
  - name: q6914.spq
    data: |
      SELECT + col1 AS col1 FROM tab1 (FORMAT parquet) WHERE NOT + + 28 * - col1 NOT IN ( + col1 * col2 );
  - name: tab1

outputs:
  - name: q6914.out
    data: |
