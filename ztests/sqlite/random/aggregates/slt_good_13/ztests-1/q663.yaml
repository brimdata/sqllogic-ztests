script: |
  super -f text -I q663.spq | sort > q663.out

inputs:
  - name: q663.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( NOT NULL IN ( col0 / + 83 ) );
  - name: tab1

outputs:
  - name: q663.out
    data: |
