script: |
  super -f text -I q953.spq | sort > q953.out

inputs:
  - name: q953.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 WHERE ( NULL ) NOT BETWEEN + col0 AND - - col2 * - col0;
  - name: tab1

outputs:
  - name: q953.out
    data: |
