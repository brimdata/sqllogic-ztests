script: |
  super -f text -I q11567.spq | sort > q11567.out

inputs:
  - name: q11567.spq
    data: |
      SELECT + col1 FROM tab1 (FORMAT parquet) WHERE NOT col1 >= + CAST ( - - col1 AS REAL );
  - name: tab1

outputs:
  - name: q11567.out
    data: |
