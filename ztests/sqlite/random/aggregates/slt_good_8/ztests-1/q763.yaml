script: |
  super -f text -I q763.spq | sort > q763.out

inputs:
  - name: q763.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + 76 NOT IN ( + + col1 );
  - name: tab1

outputs:
  - name: q763.out
    data: |
