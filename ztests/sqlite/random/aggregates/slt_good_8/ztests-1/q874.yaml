script: |
  super -f tsv -I q874.sql | sort > q874.out

vector: true

inputs:
  - name: q874.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL NOT BETWEEN + col0 * col2 AND + col0;
  - name: tab1

outputs:
  - name: q874.out
    data: |
