script: |
  super -f text -I q874.spq | sort > q874.out

inputs:
  - name: q874.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT BETWEEN + col0 * col2 AND + col0;
  - name: tab1

outputs:
  - name: q874.out
    data: |
