script: |
  super -f text -I q4652.spq | sort > q4652.out

inputs:
  - name: q4652.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT ( + col1 * - col2 ) >= ( NULL );
  - name: tab0

outputs:
  - name: q4652.out
    data: |
