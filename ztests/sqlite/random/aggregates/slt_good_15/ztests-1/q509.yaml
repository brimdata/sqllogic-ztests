script: |
  super -f text -I q509.spq | sort > q509.out

inputs:
  - name: q509.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) AS cor0 WHERE NOT NULL BETWEEN - 60 * - col1 AND NULL;
  - name: tab0

outputs:
  - name: q509.out
    data: |
