script: |
  super -f text -I q409.spq | sort > q409.out

inputs:
  - name: q409.spq
    data: |
      SELECT DISTINCT - + 93 FROM tab1 (FORMAT parquet) WHERE NOT col0 / + col0 * col0 IS NOT NULL;
  - name: tab1

outputs:
  - name: q409.out
    data: |
