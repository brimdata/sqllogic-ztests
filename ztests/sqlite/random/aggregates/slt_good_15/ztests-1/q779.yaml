script: |
  super -f text -I q779.spq | sort > q779.out

inputs:
  - name: q779.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT + + col0 NOT BETWEEN col1 AND ( + + 32 * - col0 );
  - name: tab1

outputs:
  - name: q779.out
    data: |
