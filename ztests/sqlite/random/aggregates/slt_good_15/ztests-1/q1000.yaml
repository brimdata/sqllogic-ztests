script: |
  super -f text -I q1000.spq | sort > q1000.out

inputs:
  - name: q1000.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE col2 BETWEEN col2 * - col1 AND - + col1;
  - name: tab0

outputs:
  - name: q1000.out
    data: |
