script: |
  super -f text -I q5840.spq | sort > q5840.out

inputs:
  - name: q5840.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT NULL BETWEEN - 59 - - col2 AND ( NULL );
  - name: tab1

outputs:
  - name: q5840.out
    data: |
