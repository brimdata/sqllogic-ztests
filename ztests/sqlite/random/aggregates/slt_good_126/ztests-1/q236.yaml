script: |
  super -f text -I q236.spq | sort > q236.out

inputs:
  - name: q236.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + col1 + - col0 * - col1 IS NOT NULL;
  - name: tab1

outputs:
  - name: q236.out
    data: |
