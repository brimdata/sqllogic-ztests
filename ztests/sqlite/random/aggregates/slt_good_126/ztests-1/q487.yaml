script: |
  super -f text -I q487.spq | sort > q487.out

inputs:
  - name: q487.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL > + + col0;
  - name: tab1

outputs:
  - name: q487.out
    data: |
