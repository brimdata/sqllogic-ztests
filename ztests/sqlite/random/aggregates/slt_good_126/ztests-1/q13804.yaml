script: |
  super -f tsv -I q13804.sql | sort > q13804.out

vector: true

inputs:
  - name: q13804.sql
    data: |
      SELECT DISTINCT - COUNT ( * ) / + 35 FROM tab1 WHERE NOT NULL IS NULL;
  - name: tab1

outputs:
  - name: q13804.out
    data: |
      0
