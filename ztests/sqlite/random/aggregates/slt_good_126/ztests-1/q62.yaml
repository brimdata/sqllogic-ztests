script: |
  super -f tsv -I q62.sql | sort > q62.out

vector: true

inputs:
  - name: q62.sql
    data: |
      SELECT + col0 * 43 FROM tab1 WHERE ( NULL ) <= ( NULL );
  - name: tab1

outputs:
  - name: q62.out
    data: |
