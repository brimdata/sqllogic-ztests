script: |
  super -f text -I q602.spq | sort > q602.out

inputs:
  - name: q602.spq
    data: |
      SELECT + col2 * - 80 * 40 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q602.out
    data: |
      -188800
      -217600
      -307200
