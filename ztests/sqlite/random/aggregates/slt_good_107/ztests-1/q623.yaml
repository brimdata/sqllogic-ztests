script: |
  super -f tsv -I q623.sql | sort > q623.out

vector: true

inputs:
  - name: q623.sql
    data: |
      SELECT * FROM tab1 WHERE ( NOT ( NULL ) BETWEEN + - col1 AND ( + + col2 ) );
  - name: tab1

outputs:
  - name: q623.out
    data: |
