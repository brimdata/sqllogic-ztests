script: |
  super -f text -I q623.spq | sort > q623.out

inputs:
  - name: q623.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE ( NOT ( NULL ) BETWEEN + - col1 AND ( + + col2 ) );
  - name: tab1

outputs:
  - name: q623.out
    data: |
