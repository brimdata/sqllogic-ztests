script: |
  super -f text -I q496.spq | sort > q496.out

inputs:
  - name: q496.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NULL <= ( + col0 * 88 );
  - name: tab1

outputs:
  - name: q496.out
    data: |
