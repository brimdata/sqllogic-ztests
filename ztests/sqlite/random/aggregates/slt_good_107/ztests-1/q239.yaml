script: |
  super -f tsv -I q239.sql | sort > q239.out

vector: true

inputs:
  - name: q239.sql
    data: |
      SELECT * FROM tab1 WHERE NOT + + 64 * - 30 IS NOT NULL;
  - name: tab1

outputs:
  - name: q239.out
    data: |
