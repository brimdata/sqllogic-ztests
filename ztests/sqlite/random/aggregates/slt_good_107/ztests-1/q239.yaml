script: |
  super -f text -I q239.spq | sort > q239.out

inputs:
  - name: q239.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + + 64 * - 30 IS NOT NULL;
  - name: tab1

outputs:
  - name: q239.out
    data: |
