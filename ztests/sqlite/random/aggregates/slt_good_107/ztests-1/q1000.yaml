script: |
  super -f text -I q1000.spq | sort > q1000.out

inputs:
  - name: q1000.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) cor0 WHERE NOT ( + col2 ) - 65 + - col2 * + 74 IS NOT NULL;
  - name: tab0

outputs:
  - name: q1000.out
    data: |
