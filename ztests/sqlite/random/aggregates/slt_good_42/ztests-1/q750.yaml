script: |
  super -f tsv -I q750.sql | sort > q750.out

vector: true

inputs:
  - name: q750.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL <= + col2 * - 57 + col2;
  - name: tab1

outputs:
  - name: q750.out
    data: |
