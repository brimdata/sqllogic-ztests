script: |
  super -f text -I q750.spq | sort > q750.out

inputs:
  - name: q750.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL <= + col2 * - 57 + col2;
  - name: tab1

outputs:
  - name: q750.out
    data: |
