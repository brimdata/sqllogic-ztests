script: |
  super -f text -I q908.spq | sort > q908.out

inputs:
  - name: q908.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE + + col0 / - col1 = NULL;
  - name: tab2

outputs:
  - name: q908.out
    data: |
