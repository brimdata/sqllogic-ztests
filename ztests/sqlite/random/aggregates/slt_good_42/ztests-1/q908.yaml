script: |
  super -f tsv -I q908.sql | sort > q908.out

vector: true

inputs:
  - name: q908.sql
    data: |
      SELECT * FROM tab2 WHERE + + col0 / - col1 = NULL;
  - name: tab2

outputs:
  - name: q908.out
    data: |
