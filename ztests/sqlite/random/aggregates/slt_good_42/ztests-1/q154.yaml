script: |
  super -f text -I q154.spq | sort > q154.out

inputs:
  - name: q154.spq
    data: |
      SELECT ALL - + col1 AS col2 FROM tab1 (FORMAT parquet) WHERE ( + col0 * - 73 * col1 ) BETWEEN ( - col2 * - col2 ) AND col1;
  - name: tab1

outputs:
  - name: q154.out
    data: |
