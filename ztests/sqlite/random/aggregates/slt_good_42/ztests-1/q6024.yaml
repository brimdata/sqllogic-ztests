script: |
  super -f tsv -I q6024.sql | sort > q6024.out

vector: true

inputs:
  - name: q6024.sql
    data: |
      SELECT * FROM tab0 WHERE NOT + col0 + - - ( - ( - 34 ) ) + col0 + col2 * - ( - col1 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q6024.out
    data: |
