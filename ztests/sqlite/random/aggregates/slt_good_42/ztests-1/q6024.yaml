script: |
  super -f text -I q6024.spq | sort > q6024.out

inputs:
  - name: q6024.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT + col0 + - - ( - ( - 34 ) ) + col0 + col2 * - ( - col1 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q6024.out
    data: |
