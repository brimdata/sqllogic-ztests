script: |
  super -f text -I q170.spq | sort > q170.out

inputs:
  - name: q170.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 CROSS JOIN tab0 (FORMAT parquet) AS cor1 WHERE NOT ( + 87 ) IS NOT NULL;
  - name: tab0
  - name: tab1

outputs:
  - name: q170.out
    data: |
