script: |
  super -f text -I q9908.spq | sort > q9908.out

inputs:
  - name: q9908.spq
    data: |
      SELECT + col2 AS col0 FROM tab2 (FORMAT parquet) cor0 WHERE NOT ( + col1 ) IN ( + col2 * - col2 + ( + col2 ) );
  - name: tab2

outputs:
  - name: q9908.out
    data: |
      23
      40
      58
