script: |
  super -f tsv -I q2172.sql | sort > q2172.out

vector: true

inputs:
  - name: q2172.sql
    data: |
      SELECT * FROM tab1 WHERE NOT ( + col0 * 67 + col1 / col0 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q2172.out
    data: |
