script: |
  super -f text -I q301.spq | sort > q301.out

inputs:
  - name: q301.spq
    data: |
      SELECT ALL - col1 / 53 FROM tab2 (FORMAT parquet) WHERE NOT col1 = ( ( ( col1 ) ) + + ( + col1 ) );
  - name: tab2

outputs:
  - name: q301.out
    data: |
      -1
      -1
      0
