script: |
  super -f tsv -I q602.sql | sort > q602.out

vector: true

inputs:
  - name: q602.sql
    data: |
      SELECT + CAST ( NULL AS INTEGER ) FROM tab2 WHERE NOT NULL IS NULL;
  - name: tab2

outputs:
  - name: q602.out
    data: |
