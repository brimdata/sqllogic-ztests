script: |
  super -f text -I q767.spq | sort > q767.out

inputs:
  - name: q767.spq
    data: |
      SELECT DISTINCT - col2 + - + col0 * - 93 AS col1 FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( col1 );
  - name: tab1

outputs:
  - name: q767.out
    data: |
