script: |
  super -f text -I q1767.spq | sort > q1767.out

inputs:
  - name: q1767.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) cor0 CROSS JOIN tab1 (FORMAT parquet) AS cor1 WHERE NOT + ( - ( + 1 ) ) + - 27 * - + 72 IS NOT NULL;
  - name: tab1

outputs:
  - name: q1767.out
    data: |
