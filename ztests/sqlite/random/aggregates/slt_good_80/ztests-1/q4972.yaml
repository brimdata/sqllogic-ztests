script: |
  super -f text -I q4972.spq | sort > q4972.out

inputs:
  - name: q4972.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + col1 * - col1 * - 55 + col2 < NULL;
  - name: tab1

outputs:
  - name: q4972.out
    data: |
