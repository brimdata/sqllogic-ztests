script: |
  super -f text -I q6318.spq | sort > q6318.out

inputs:
  - name: q6318.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT ( NOT + col0 - - col0 IS NULL );
  - name: tab0

outputs:
  - name: q6318.out
    data: |
