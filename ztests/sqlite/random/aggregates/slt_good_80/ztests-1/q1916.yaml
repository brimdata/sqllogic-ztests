script: |
  super -f text -I q1916.spq | sort > q1916.out

inputs:
  - name: q1916.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE col0 * - col1 * + - col1 >= NULL;
  - name: tab1

outputs:
  - name: q1916.out
    data: |
