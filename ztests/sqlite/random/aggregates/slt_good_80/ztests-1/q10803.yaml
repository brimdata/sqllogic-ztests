script: |
  super -f text -I q10803.spq | sort > q10803.out

inputs:
  - name: q10803.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT + col0 / + col1 * ( + col0 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q10803.out
    data: |
