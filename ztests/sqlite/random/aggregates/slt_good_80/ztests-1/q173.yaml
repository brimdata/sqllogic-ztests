script: |
  super -f text -I q173.spq | sort > q173.out

inputs:
  - name: q173.spq
    data: |
      SELECT ALL ( + col0 ) FROM tab2 (FORMAT parquet) WHERE NOT ( NOT ( NULL ) IS NOT NULL );
  - name: tab2

outputs:
  - name: q173.out
    data: |
