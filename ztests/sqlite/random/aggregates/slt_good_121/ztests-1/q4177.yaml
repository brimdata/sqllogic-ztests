script: |
  super -f text -I q4177.spq | sort > q4177.out

inputs:
  - name: q4177.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) AS cor0 CROSS JOIN tab0 (FORMAT parquet) cor1 WHERE NOT ( NOT NULL IS NOT NULL );
  - name: tab0
  - name: tab1

outputs:
  - name: q4177.out
    data: |
