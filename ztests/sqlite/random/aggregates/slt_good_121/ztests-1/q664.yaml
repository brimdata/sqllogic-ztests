script: |
  super -f text -I q664.spq | sort > q664.out

inputs:
  - name: q664.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT + 25 + col2 * 1 NOT IN ( 20 );
  - name: tab1

outputs:
  - name: q664.out
    data: |
