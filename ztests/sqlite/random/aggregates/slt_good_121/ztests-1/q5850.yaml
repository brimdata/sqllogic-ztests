script: |
  super -f text -I q5850.spq | sort > q5850.out

inputs:
  - name: q5850.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT NULL = - - ( + + col1 );
  - name: tab2

outputs:
  - name: q5850.out
    data: |
