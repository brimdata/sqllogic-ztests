script: |
  super -f text -I q5802.spq | sort > q5802.out

inputs:
  - name: q5802.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT + col0 - - 52 IS NOT NULL;
  - name: tab1

outputs:
  - name: q5802.out
    data: |
