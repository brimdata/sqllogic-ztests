script: |
  super -f text -I q3391.spq | sort > q3391.out

inputs:
  - name: q3391.spq
    data: |
      SELECT DISTINCT - + col0 / - col2 FROM tab1 (FORMAT parquet) WHERE NOT ( NOT ( NOT + ( - 80 ) = NULL ) );
  - name: tab1

outputs:
  - name: q3391.out
    data: |
