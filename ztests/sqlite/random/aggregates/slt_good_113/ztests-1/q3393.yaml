script: |
  super -f text -I q3393.spq | sort > q3393.out

inputs:
  - name: q3393.spq
    data: |
      SELECT + col2 AS col0 FROM tab2 (FORMAT parquet) WHERE NOT + col0 / + ( + - col1 ) * - col1 - + - col1 >= ( + col1 );
  - name: tab2

outputs:
  - name: q3393.out
    data: |
