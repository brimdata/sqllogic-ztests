script: |
  super -f text -I q714.spq | sort > q714.out

inputs:
  - name: q714.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT NULL BETWEEN ( + + col0 + - + col2 * - ( 99 ) + + col0 ) AND ( NULL );
  - name: tab2

outputs:
  - name: q714.out
    data: |
