script: |
  super -f text -I q4605.spq | sort > q4605.out

inputs:
  - name: q4605.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( + col1 * + col2 * col2 * - col1 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q4605.out
    data: |
