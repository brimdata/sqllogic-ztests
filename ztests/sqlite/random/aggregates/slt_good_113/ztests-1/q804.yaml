script: |
  super -f tsv -I q804.sql | sort > q804.out

vector: true

inputs:
  - name: q804.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT ( NULL = - col0 );
  - name: tab1

outputs:
  - name: q804.out
    data: |
