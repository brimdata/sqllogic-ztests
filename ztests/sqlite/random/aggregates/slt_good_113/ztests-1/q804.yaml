script: |
  super -f text -I q804.spq | sort > q804.out

inputs:
  - name: q804.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT ( NULL = - col0 );
  - name: tab1

outputs:
  - name: q804.out
    data: |
