script: |
  super -f text -I q1300.spq | sort > q1300.out

inputs:
  - name: q1300.spq
    data: |
      SELECT DISTINCT + col0 * - 45 * - - col1 * - + col0 + + 42 AS col0 FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT ( NULL ) < + 90 + col0;
  - name: tab1

outputs:
  - name: q1300.out
    data: |
