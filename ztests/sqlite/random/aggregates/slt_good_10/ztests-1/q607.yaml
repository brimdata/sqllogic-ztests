script: |
  super -f text -I q607.spq | sort > q607.out

inputs:
  - name: q607.spq
    data: |
      SELECT + CAST ( + ( + col0 ) AS REAL ) FROM tab2 (FORMAT parquet) AS cor0 WHERE NOT NULL NOT IN ( + col1 * col0 );
  - name: tab2

outputs:
  - name: q607.out
    data: |
