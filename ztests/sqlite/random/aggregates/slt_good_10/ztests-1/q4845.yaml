script: |
  super -f text -I q4845.spq | sort > q4845.out

inputs:
  - name: q4845.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( - col1 * - - 20 * + col0 + - col1 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q4845.out
    data: |
