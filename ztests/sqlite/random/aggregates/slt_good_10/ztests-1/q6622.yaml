script: |
  super -f text -I q6622.spq | sort > q6622.out

inputs:
  - name: q6622.spq
    data: |
      SELECT 23 FROM tab1 (FORMAT parquet) WHERE NOT + + CAST ( NULL AS INTEGER ) + - col1 BETWEEN NULL AND NULL;
  - name: tab1

outputs:
  - name: q6622.out
    data: |
