script: |
  super -f text -I q186.spq | sort > q186.out

inputs:
  - name: q186.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + col2 IS NOT NULL AND ( NULL ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q186.out
    data: |
