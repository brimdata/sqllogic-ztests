script: |
  super -f text -I q438.spq | sort > q438.out

inputs:
  - name: q438.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE + + col0 * + 25 = ( NULL );
  - name: tab1

outputs:
  - name: q438.out
    data: |
