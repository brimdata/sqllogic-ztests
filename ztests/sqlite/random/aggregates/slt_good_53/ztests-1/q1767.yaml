script: |
  super -f text -I q1767.spq | sort > q1767.out

inputs:
  - name: q1767.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT col1 NOT IN ( - 12 );
  - name: tab1

outputs:
  - name: q1767.out
    data: |
