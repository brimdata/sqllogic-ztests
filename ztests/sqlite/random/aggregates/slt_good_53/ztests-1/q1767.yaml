script: |
  super -f tsv -I q1767.sql | sort > q1767.out

vector: true

inputs:
  - name: q1767.sql
    data: |
      SELECT * FROM tab1 WHERE NOT col1 NOT IN ( - 12 );
  - name: tab1

outputs:
  - name: q1767.out
    data: |
