script: |
  super -f text -I q570.spq | sort > q570.out

inputs:
  - name: q570.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT NULL > col0 * - CAST ( + col1 AS INTEGER );
  - name: tab1

outputs:
  - name: q570.out
    data: |
