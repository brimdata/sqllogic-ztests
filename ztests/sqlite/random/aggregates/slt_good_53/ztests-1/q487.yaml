script: |
  super -f text -I q487.spq | sort > q487.out

inputs:
  - name: q487.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT + col2 / + + ( + 72 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q487.out
    data: |
