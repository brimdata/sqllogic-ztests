script: |
  super -f tsv -I q653.sql | sort > q653.out

vector: true

inputs:
  - name: q653.sql
    data: |
      SELECT * FROM tab1 AS cor0 WHERE NOT - col1 * - col1 IS NOT NULL;
  - name: tab1

outputs:
  - name: q653.out
    data: |
