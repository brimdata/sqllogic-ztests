script: |
  super -f text -I q548.spq | sort > q548.out

inputs:
  - name: q548.spq
    data: |
      SELECT + col2 / + + col0 FROM tab0 (FORMAT parquet) WHERE NOT NULL = - col2;
  - name: tab0

outputs:
  - name: q548.out
    data: |
