script: |
  super -f text -I q28.spq | sort > q28.out

inputs:
  - name: q28.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) cor0 WHERE NOT NULL NOT IN ( - CAST ( NULL AS REAL ) );
  - name: tab1

outputs:
  - name: q28.out
    data: |
