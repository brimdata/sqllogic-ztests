script: |
  super -f text -I q50.spq | sort > q50.out

inputs:
  - name: q50.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( + 59 ) <> ( + col0 );
  - name: tab1

outputs:
  - name: q50.out
    data: |
