script: |
  super -f tsv -I q5509.sql | sort > q5509.out

vector: true

inputs:
  - name: q5509.sql
    data: |
      SELECT ALL * FROM tab1 WHERE NOT + col2 + + col0 * col1 < NULL;
  - name: tab1

outputs:
  - name: q5509.out
    data: |
