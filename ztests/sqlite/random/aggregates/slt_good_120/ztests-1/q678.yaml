script: |
  super -f text -I q678.spq | sort > q678.out

inputs:
  - name: q678.spq
    data: |
      SELECT ALL * FROM tab2 (FORMAT parquet) WHERE NOT ( + col2 * - 29 + col0 ) > NULL;
  - name: tab2

outputs:
  - name: q678.out
    data: |
