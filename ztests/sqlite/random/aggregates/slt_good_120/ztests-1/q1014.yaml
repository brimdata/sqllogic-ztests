script: |
  super -f text -I q1014.spq | sort > q1014.out

inputs:
  - name: q1014.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT ( + 16 * + + col1 * - col1 IS NOT NULL );
  - name: tab2

outputs:
  - name: q1014.out
    data: |
