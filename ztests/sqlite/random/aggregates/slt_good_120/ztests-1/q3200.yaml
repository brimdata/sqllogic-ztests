script: |
  super -f text -I q3200.spq | sort > q3200.out

inputs:
  - name: q3200.spq
    data: |
      SELECT + col0 FROM tab0 (FORMAT parquet) WHERE NOT + col2 NOT IN ( + col1 * - col0 );
  - name: tab0

outputs:
  - name: q3200.out
    data: |
