script: |
  super -f text -I q664.spq | sort > q664.out

inputs:
  - name: q664.spq
    data: |
      SELECT DISTINCT col1 + - col0 FROM tab0 (FORMAT parquet) WHERE NOT + 33 + + col0 / col2 IS NOT NULL;
  - name: tab0

outputs:
  - name: q664.out
    data: |
