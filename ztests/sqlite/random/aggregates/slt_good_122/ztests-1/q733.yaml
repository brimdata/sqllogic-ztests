script: |
  super -f text -I q733.spq | sort > q733.out

inputs:
  - name: q733.spq
    data: |
      SELECT ALL * FROM tab0 (FORMAT parquet) WHERE ( + ( + + col1 ) * - 9 ) = - col1;
  - name: tab0

outputs:
  - name: q733.out
    data: |
