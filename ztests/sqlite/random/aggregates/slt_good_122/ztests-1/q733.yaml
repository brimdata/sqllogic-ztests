script: |
  super -f tsv -I q733.sql | sort > q733.out

vector: true

inputs:
  - name: q733.sql
    data: |
      SELECT ALL * FROM tab0 WHERE ( + ( + + col1 ) * - 9 ) = - col1;
  - name: tab0

outputs:
  - name: q733.out
    data: |
