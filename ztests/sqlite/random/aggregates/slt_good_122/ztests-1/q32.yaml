script: |
  super -f text -I q32.spq | sort > q32.out

inputs:
  - name: q32.spq
    data: |
      SELECT + col1 FROM tab1 (FORMAT parquet) WHERE NOT ( + ( - 38 ) + - col1 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q32.out
    data: |
