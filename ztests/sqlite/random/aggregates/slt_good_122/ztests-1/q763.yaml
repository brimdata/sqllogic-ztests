script: |
  super -f text -I q763.spq | sort > q763.out

inputs:
  - name: q763.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) AS cor0 WHERE col0 * + col0 / - - col2 * - ( + col1 ) IS NULL;
  - name: tab2

outputs:
  - name: q763.out
    data: |
