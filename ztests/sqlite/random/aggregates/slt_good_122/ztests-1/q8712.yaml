script: |
  super -f text -I q8712.spq | sort > q8712.out

inputs:
  - name: q8712.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT ( + + 95 ) IS NOT NULL;
  - name: tab2

outputs:
  - name: q8712.out
    data: |
