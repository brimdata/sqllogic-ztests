script: |
  super -f text -I q547.spq | sort > q547.out

inputs:
  - name: q547.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE + col1 NOT IN ( - - col1 * - col2 * col1, 30, col1 );
  - name: tab2

outputs:
  - name: q547.out
    data: |
