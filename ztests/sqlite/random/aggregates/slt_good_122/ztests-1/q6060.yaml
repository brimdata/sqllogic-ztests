script: |
  super -f tsv -I q6060.sql | sort > q6060.out

vector: true

inputs:
  - name: q6060.sql
    data: |
      SELECT * FROM tab2 WHERE ( NULL ) IS NOT NULL AND col1 IS NOT NULL;
  - name: tab2

outputs:
  - name: q6060.out
    data: |
