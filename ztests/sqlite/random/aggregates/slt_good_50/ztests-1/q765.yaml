script: |
  super -f text -I q765.spq | sort > q765.out

inputs:
  - name: q765.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT ( + 40 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q765.out
    data: |
