script: |
  super -f text -I q455.spq | sort > q455.out

inputs:
  - name: q455.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT ( NOT + col2 * ( - - col0 ) * + col2 - - col1 / + col0 >= NULL );
  - name: tab1

outputs:
  - name: q455.out
    data: |
