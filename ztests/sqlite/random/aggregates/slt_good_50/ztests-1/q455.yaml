script: |
  super -f tsv -I q455.sql | sort > q455.out

vector: true

inputs:
  - name: q455.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT ( NOT + col2 * ( - - col0 ) * + col2 - - col1 / + col0 >= NULL );
  - name: tab1

outputs:
  - name: q455.out
    data: |
