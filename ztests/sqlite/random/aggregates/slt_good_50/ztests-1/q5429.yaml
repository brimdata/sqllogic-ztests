script: |
  super -f text -I q5429.spq | sort > q5429.out

inputs:
  - name: q5429.spq
    data: |
      SELECT DISTINCT + COUNT ( * ) * COUNT ( * ) FROM tab1 (FORMAT parquet) WHERE NOT col2 * - col1 IS NULL;
  - name: tab1

outputs:
  - name: q5429.out
    data: |
      9
