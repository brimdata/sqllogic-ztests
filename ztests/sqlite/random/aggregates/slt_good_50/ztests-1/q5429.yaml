script: |
  super -f tsv -I q5429.sql | sort > q5429.out

vector: true

inputs:
  - name: q5429.sql
    data: |
      SELECT DISTINCT + COUNT ( * ) * COUNT ( * ) FROM tab1 WHERE NOT col2 * - col1 IS NULL;
  - name: tab1

outputs:
  - name: q5429.out
    data: |
      9
