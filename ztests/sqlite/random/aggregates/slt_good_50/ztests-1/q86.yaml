script: |
  super -f text -I q86.spq | sort > q86.out

inputs:
  - name: q86.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT col0 + + col1 * - col0 <= NULL;
  - name: tab1

outputs:
  - name: q86.out
    data: |
