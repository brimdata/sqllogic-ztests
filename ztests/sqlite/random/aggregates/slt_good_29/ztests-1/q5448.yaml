script: |
  super -f text -I q5448.spq | sort > q5448.out

inputs:
  - name: q5448.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT col2 / ( + + col2 ) + - col2 * - col2 * - 50 + col0 BETWEEN NULL AND ( NULL );
  - name: tab2

outputs:
  - name: q5448.out
    data: |
