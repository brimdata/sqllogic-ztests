script: |
  super -f text -I q60.spq | sort > q60.out

inputs:
  - name: q60.spq
    data: |
      SELECT + + col0 FROM tab2 (FORMAT parquet) WHERE NOT ( NOT NULL >= ( NULL ) );
  - name: tab2

outputs:
  - name: q60.out
    data: |
