script: |
  super -f text -I q375.spq | sort > q375.out

inputs:
  - name: q375.spq
    data: |
      SELECT ALL * FROM tab2 (FORMAT parquet) cor0 WHERE NOT col1 / col0 * - - ( ( + col0 ) ) + col0 + col1 IS NOT NULL;
  - name: tab2

outputs:
  - name: q375.out
    data: |
