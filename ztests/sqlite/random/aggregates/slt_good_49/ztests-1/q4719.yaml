script: |
  super -f text -I q4719.spq | sort > q4719.out

inputs:
  - name: q4719.spq
    data: |
      SELECT col1 * 25 + ( col1 ) AS col1 FROM tab2 (FORMAT parquet) WHERE NOT NULL BETWEEN ( NULL ) AND col2 + col2 / - 52;
  - name: tab2

outputs:
  - name: q4719.out
    data: |
