script: |
  super -f tsv -I q656.sql | sort > q656.out

vector: true

inputs:
  - name: q656.sql
    data: |
      SELECT DISTINCT - col1 - + 3 FROM tab1;
  - name: tab1

outputs:
  - name: q656.out
    data: |
      -17
      -50
      -8
