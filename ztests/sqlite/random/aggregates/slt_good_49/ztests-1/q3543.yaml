script: |
  super -f text -I q3543.spq | sort > q3543.out

inputs:
  - name: q3543.spq
    data: |
      SELECT DISTINCT 32 * - + COUNT ( * ) / COUNT ( * ) * 93 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q3543.out
    data: |
      -2976
