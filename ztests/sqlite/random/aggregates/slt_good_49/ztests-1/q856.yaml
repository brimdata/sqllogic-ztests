script: |
  super -f text -I q856.spq | sort > q856.out

inputs:
  - name: q856.spq
    data: |
      SELECT DISTINCT + + col2 FROM tab1 (FORMAT parquet) WHERE NOT col1 + col0 / + 35 * col2 IS NULL;
  - name: tab1

outputs:
  - name: q856.out
    data: |
      59
      68
      96
