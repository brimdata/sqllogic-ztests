script: |
  super -f text -I q749.spq | sort > q749.out

inputs:
  - name: q749.spq
    data: |
      SELECT + + col2 - - col2 FROM tab2 (FORMAT parquet) WHERE NOT + 96 IS NOT NULL;
  - name: tab2

outputs:
  - name: q749.out
    data: |
