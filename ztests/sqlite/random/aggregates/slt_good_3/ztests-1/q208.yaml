script: |
  super -f text -I q208.spq | sort > q208.out

inputs:
  - name: q208.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + ( 89 ) = NULL;
  - name: tab1

outputs:
  - name: q208.out
    data: |
