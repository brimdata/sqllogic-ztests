script: |
  super -f text -I q6300.spq | sort > q6300.out

inputs:
  - name: q6300.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT ( col1 + - col1 * - - col0 * - - col0 + - ( - + 4 ) ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q6300.out
    data: |
