script: |
  super -f text -I q6856.spq | sort > q6856.out

inputs:
  - name: q6856.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT - col1 * col0 * - CAST ( NULL AS INTEGER ) > + col1;
  - name: tab1

outputs:
  - name: q6856.out
    data: |
