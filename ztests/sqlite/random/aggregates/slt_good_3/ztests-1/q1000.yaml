script: |
  super -f text -I q1000.spq | sort > q1000.out

inputs:
  - name: q1000.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) AS cor0 CROSS JOIN tab1 (FORMAT parquet) AS cor1 WHERE NOT NULL NOT BETWEEN ( ( + + 24 ) ) AND ( NULL );
  - name: tab1
  - name: tab2

outputs:
  - name: q1000.out
    data: |
