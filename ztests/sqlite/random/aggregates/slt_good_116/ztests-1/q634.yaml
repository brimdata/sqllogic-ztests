script: |
  super -f text -I q634.spq | sort > q634.out

inputs:
  - name: q634.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT ( col2 ) <= ( NULL );
  - name: tab1

outputs:
  - name: q634.out
    data: |
