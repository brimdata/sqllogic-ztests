script: |
  super -f tsv -I q739.sql | sort > q739.out

vector: true

inputs:
  - name: q739.sql
    data: |
      SELECT * FROM tab1 WHERE NOT ( - 48 + - col1 IS NOT NULL );
  - name: tab1

outputs:
  - name: q739.out
    data: |
