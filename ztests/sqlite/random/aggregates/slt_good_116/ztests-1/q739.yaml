script: |
  super -f text -I q739.spq | sort > q739.out

inputs:
  - name: q739.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( - 48 + - col1 IS NOT NULL );
  - name: tab1

outputs:
  - name: q739.out
    data: |
