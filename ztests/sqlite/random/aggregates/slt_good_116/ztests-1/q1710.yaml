script: |
  super -f text -I q1710.spq | sort > q1710.out

inputs:
  - name: q1710.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) cor0 WHERE NOT NULL NOT BETWEEN + CAST ( NULL AS INTEGER ) + + + col1 + col1 AND + + col0;
  - name: tab2

outputs:
  - name: q1710.out
    data: |
