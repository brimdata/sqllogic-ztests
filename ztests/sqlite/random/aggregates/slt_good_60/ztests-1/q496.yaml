script: |
  super -f text -I q496.spq | sort > q496.out

inputs:
  - name: q496.spq
    data: |
      SELECT ALL col2 / + col1 FROM tab2 (FORMAT parquet) WHERE NOT col1 - - col2 IS NOT NULL;
  - name: tab2

outputs:
  - name: q496.out
    data: |
