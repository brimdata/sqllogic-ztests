script: |
  super -f text -I q409.spq | sort > q409.out

inputs:
  - name: q409.spq
    data: |
      SELECT 15 / col0 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q409.out
    data: |
      0
      0
      0
