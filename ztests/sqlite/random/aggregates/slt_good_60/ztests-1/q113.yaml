script: |
  super -f tsv -I q113.sql | sort > q113.out

vector: true

inputs:
  - name: q113.sql
    data: |
      SELECT ALL ( + COUNT ( * ) ) / + SUM ( + - ( + col0 ) ) * MAX ( 75 ) FROM tab0;
  - name: tab0

outputs:
  - name: q113.out
    data: |
      0
