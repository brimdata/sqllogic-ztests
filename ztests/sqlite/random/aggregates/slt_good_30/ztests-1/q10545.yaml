script: |
  super -f tsv -I q10545.sql | sort > q10545.out

vector: true

inputs:
  - name: q10545.sql
    data: |
      SELECT DISTINCT COUNT ( * ) + SUM ( DISTINCT - col0 + 3 ) FROM tab1 WHERE NOT NULL IN ( - - col1 * + col0 );
  - name: tab1

outputs:
  - name: q10545.out
    data: |
      NULL
