script: |
  super -f text -I q150.spq | sort > q150.out

inputs:
  - name: q150.spq
    data: |
      SELECT - COUNT ( * ) col2, COUNT ( * ) + - MAX ( + col1 ) + 38 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q150.out
    data: |
      -3	-6
