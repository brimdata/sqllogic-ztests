script: |
  super -f text -I q2864.spq | sort > q2864.out

inputs:
  - name: q2864.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT col0 * - col1 + col0 * - col0 IS NOT NULL;
  - name: tab2

outputs:
  - name: q2864.out
    data: |
