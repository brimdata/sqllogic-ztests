script: |
  super -f tsv -I q219.sql | sort > q219.out

vector: true

inputs:
  - name: q219.sql
    data: |
      SELECT DISTINCT - col1 / + ( ( + 40 ) ) FROM tab0;
  - name: tab0

outputs:
  - name: q219.out
    data: |
      -2
      0
