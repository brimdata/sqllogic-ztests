script: |
  super -f tsv -I q733.sql | sort > q733.out

vector: true

inputs:
  - name: q733.sql
    data: |
      SELECT DISTINCT + 48 * - SUM ( ALL + - col2 ) FROM tab1 WHERE ( NOT ( NULL ) > NULL );
  - name: tab1

outputs:
  - name: q733.out
    data: |
      NULL
