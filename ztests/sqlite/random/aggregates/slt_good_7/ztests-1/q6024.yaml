script: |
  super -f text -I q6024.spq | sort > q6024.out

inputs:
  - name: q6024.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE + ( + col1 ) IS NULL;
  - name: tab1

outputs:
  - name: q6024.out
    data: |
