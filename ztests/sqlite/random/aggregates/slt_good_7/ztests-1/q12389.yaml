script: |
  super -f text -I q12389.spq | sort > q12389.out

inputs:
  - name: q12389.spq
    data: |
      SELECT + col1 - + col1 FROM tab1 (FORMAT parquet) cor0 WHERE NOT ( NOT ( ( NULL ) IS NOT NULL ) );
  - name: tab1

outputs:
  - name: q12389.out
    data: |
