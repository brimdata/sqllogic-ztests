script: |
  super -f tsv -I q519.sql | sort > q519.out

vector: true

inputs:
  - name: q519.sql
    data: |
      SELECT * FROM tab1 WHERE NOT ( NULL NOT IN ( 21 * col2, + col2 ) );
  - name: tab1

outputs:
  - name: q519.out
    data: |
