script: |
  super -f text -I q519.spq | sort > q519.out

inputs:
  - name: q519.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( NULL NOT IN ( 21 * col2, + col2 ) );
  - name: tab1

outputs:
  - name: q519.out
    data: |
