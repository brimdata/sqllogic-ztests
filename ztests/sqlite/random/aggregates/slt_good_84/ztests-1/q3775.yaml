script: |
  super -f tsv -I q3775.sql | sort > q3775.out

vector: true

inputs:
  - name: q3775.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT - col1 * - col2 >= ( NULL );
  - name: tab1

outputs:
  - name: q3775.out
    data: |
