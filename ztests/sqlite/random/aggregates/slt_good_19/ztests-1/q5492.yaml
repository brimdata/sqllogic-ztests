script: |
  super -f text -I q5492.spq | sort > q5492.out

inputs:
  - name: q5492.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL BETWEEN ( + col1 * - col0 ) AND - col1 + col2 + - - col0 - col2;
  - name: tab1

outputs:
  - name: q5492.out
    data: |
