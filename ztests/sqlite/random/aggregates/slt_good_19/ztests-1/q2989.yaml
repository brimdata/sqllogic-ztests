script: |
  super -f text -I q2989.spq | sort > q2989.out

inputs:
  - name: q2989.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT ( - col2 * - + col1 IS NOT NULL );
  - name: tab0

outputs:
  - name: q2989.out
    data: |
