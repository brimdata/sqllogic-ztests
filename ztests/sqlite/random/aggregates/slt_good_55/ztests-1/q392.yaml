script: |
  super -f text -I q392.spq | sort > q392.out

inputs:
  - name: q392.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + ( + col1 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q392.out
    data: |
