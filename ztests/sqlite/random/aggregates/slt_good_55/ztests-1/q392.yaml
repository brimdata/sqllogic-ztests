script: |
  super -f text -I q392.sql | sort > q392.out

inputs:
  - name: q392.sql
    data: |
      SELECT * FROM tab1 WHERE NOT + ( + col1 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q392.out
    data: |
