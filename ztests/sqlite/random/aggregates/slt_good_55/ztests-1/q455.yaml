script: |
  super -f text -I q455.sql | sort > q455.out

inputs:
  - name: q455.sql
    data: |
      SELECT - CAST ( + col0 AS INTEGER ) + + ( + col2 ) FROM tab1 WHERE NOT NULL <= + col1;
  - name: tab1

outputs:
  - name: q455.out
    data: |
