script: |
  super -f text -I q455.spq | sort > q455.out

inputs:
  - name: q455.spq
    data: |
      SELECT - CAST ( + col0 AS INTEGER ) + + ( + col2 ) FROM tab1 (FORMAT parquet) WHERE NOT NULL <= + col1;
  - name: tab1

outputs:
  - name: q455.out
    data: |
