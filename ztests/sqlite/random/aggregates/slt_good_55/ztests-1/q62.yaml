script: |
  super -f tsv -I q62.sql | sort > q62.out

vector: true

inputs:
  - name: q62.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL > + 24;
  - name: tab1

outputs:
  - name: q62.out
    data: |
