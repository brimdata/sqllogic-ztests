script: |
  super -f text -I q62.spq | sort > q62.out

inputs:
  - name: q62.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL > + 24;
  - name: tab1

outputs:
  - name: q62.out
    data: |
