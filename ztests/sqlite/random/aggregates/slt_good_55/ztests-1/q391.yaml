script: |
  super -f text -I q391.spq | sort > q391.out

inputs:
  - name: q391.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + col0 / col0 / + - col1 * + col1 * + 13 + - - col1 IS NOT NULL;
  - name: tab1

outputs:
  - name: q391.out
    data: |
