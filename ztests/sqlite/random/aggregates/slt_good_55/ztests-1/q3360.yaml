script: |
  super -f text -I q3360.spq | sort > q3360.out

inputs:
  - name: q3360.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT NULL NOT IN ( - col2 * col0, - 97, col0, + + col1 );
  - name: tab0

outputs:
  - name: q3360.out
    data: |
