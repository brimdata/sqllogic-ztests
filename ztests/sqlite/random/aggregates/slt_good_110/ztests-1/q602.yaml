script: |
  super -f tsv -I q602.sql | sort > q602.out

vector: true

inputs:
  - name: q602.sql
    data: |
      SELECT DISTINCT col0 FROM tab2 WHERE ( NOT 80 IS NOT NULL );
  - name: tab2

outputs:
  - name: q602.out
    data: |
