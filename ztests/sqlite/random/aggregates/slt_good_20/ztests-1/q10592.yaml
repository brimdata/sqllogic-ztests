script: |
  super -f text -I q10592.spq | sort > q10592.out

inputs:
  - name: q10592.spq
    data: |
      SELECT + col2 + + CAST ( + col2 AS INTEGER ) / - col0 * - - col2 * col0 col2 FROM tab2 (FORMAT parquet) WHERE NOT - + col1 - - col0 + col0 IS NOT NULL;
  - name: tab2

outputs:
  - name: q10592.out
    data: |
