script: |
  super -f text -I q657.spq | sort > q657.out

inputs:
  - name: q657.spq
    data: |
      SELECT ALL col0 * - - col0 * - ( - col1 ) + + + 8 FROM tab1 (FORMAT parquet) WHERE NOT ( ( 64 < NULL ) );
  - name: tab1

outputs:
  - name: q657.out
    data: |
