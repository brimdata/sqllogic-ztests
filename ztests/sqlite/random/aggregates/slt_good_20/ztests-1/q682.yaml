script: |
  super -f tsv -I q682.sql | sort > q682.out

vector: true

inputs:
  - name: q682.sql
    data: |
      SELECT * FROM tab1 WHERE NOT - col2 - - ( + col0 ) <= ( NULL );
  - name: tab1

outputs:
  - name: q682.out
    data: |
