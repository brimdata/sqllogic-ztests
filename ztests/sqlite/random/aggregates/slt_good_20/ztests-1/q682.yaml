script: |
  super -f text -I q682.spq | sort > q682.out

inputs:
  - name: q682.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT - col2 - - ( + col0 ) <= ( NULL );
  - name: tab1

outputs:
  - name: q682.out
    data: |
