script: |
  super -f text -I q1602.spq | sort > q1602.out

inputs:
  - name: q1602.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT ( col0 * - col2 ) IS NOT NULL AND NOT ( NULL ) = NULL;
  - name: tab0

outputs:
  - name: q1602.out
    data: |
