script: |
  super -f text -I q195.spq | sort > q195.out

inputs:
  - name: q195.spq
    data: |
      SELECT + col0 FROM tab1 (FORMAT parquet) WHERE NOT + - col1 + + col2 * col0 IS NOT NULL;
  - name: tab1

outputs:
  - name: q195.out
    data: |
