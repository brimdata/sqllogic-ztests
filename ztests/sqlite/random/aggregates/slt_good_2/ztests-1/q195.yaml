script: |
  super -f tsv -I q195.sql | sort > q195.out

vector: true

inputs:
  - name: q195.sql
    data: |
      SELECT + col0 FROM tab1 WHERE NOT + - col1 + + col2 * col0 IS NOT NULL;
  - name: tab1

outputs:
  - name: q195.out
    data: |
