script: |
  super -f text -I q93.spq | sort > q93.out

inputs:
  - name: q93.spq
    data: |
      SELECT - 21 FROM tab0 (FORMAT parquet) WHERE NOT ( + col0 * + 2 + + col2 / - col0 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q93.out
    data: |
