script: |
  super -f text -I q4140.spq | sort > q4140.out

inputs:
  - name: q4140.spq
    data: |
      SELECT - - COUNT ( * ) FROM tab1 (FORMAT parquet) AS cor0 CROSS JOIN tab2 (FORMAT parquet) cor1 WHERE NOT ( 90 IS NOT NULL ) OR NOT ( NOT + 10 IS NOT NULL );
  - name: tab1
  - name: tab2

outputs:
  - name: q4140.out
    data: |
      9
