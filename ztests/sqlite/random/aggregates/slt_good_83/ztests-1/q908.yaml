script: |
  super -f text -I q908.spq | sort > q908.out

inputs:
  - name: q908.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) AS cor0 WHERE NOT ( NULL ) NOT IN ( + + CAST ( NULL AS INTEGER ), + 50, ( + col2 ) + + ( col1 ) );
  - name: tab0

outputs:
  - name: q908.out
    data: |
