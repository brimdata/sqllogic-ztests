script: |
  super -f tsv -I q602.sql | sort > q602.out

vector: true

inputs:
  - name: q602.sql
    data: |
      SELECT CAST ( NULL AS REAL ) + + col2, col2 FROM tab2;
  - name: tab2

outputs:
  - name: q602.out
    data: |
      NULL	23
      NULL	40
      NULL	58
