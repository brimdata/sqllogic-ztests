script: |
  super -f text -I q6971.spq | sort > q6971.out

inputs:
  - name: q6971.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE ( + col0 ) BETWEEN - col2 AND + 12 / - 85 * - col2;
  - name: tab1

outputs:
  - name: q6971.out
    data: |
