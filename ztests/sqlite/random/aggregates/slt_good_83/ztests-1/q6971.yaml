script: |
  super -f tsv -I q6971.sql | sort > q6971.out

vector: true

inputs:
  - name: q6971.sql
    data: |
      SELECT * FROM tab1 WHERE ( + col0 ) BETWEEN - col2 AND + 12 / - 85 * - col2;
  - name: tab1

outputs:
  - name: q6971.out
    data: |
