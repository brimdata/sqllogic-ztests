script: |
  super -f text -I q1477.spq | sort > q1477.out

inputs:
  - name: q1477.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 CROSS JOIN tab0 (FORMAT parquet) AS cor1 WHERE NOT ( - 3 ) IS NOT NULL;
  - name: tab0
  - name: tab1

outputs:
  - name: q1477.out
    data: |
