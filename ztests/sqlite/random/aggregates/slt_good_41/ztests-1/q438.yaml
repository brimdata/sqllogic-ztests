script: |
  super -f text -I q438.spq | sort > q438.out

inputs:
  - name: q438.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT col2 - col0 * + col0 > NULL;
  - name: tab1

outputs:
  - name: q438.out
    data: |
