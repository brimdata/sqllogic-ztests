script: |
  super -f text -I q750.spq | sort > q750.out

inputs:
  - name: q750.spq
    data: |
      SELECT DISTINCT col1 AS col2 FROM tab2 (FORMAT parquet) WHERE NOT ( ( + col1 ) + - 32 / col2 ) = NULL;
  - name: tab2

outputs:
  - name: q750.out
    data: |
