script: |
  super -f text -I q640.spq | sort > q640.out

inputs:
  - name: q640.spq
    data: |
      SELECT DISTINCT + 20 + - col1 FROM tab1 (FORMAT parquet) WHERE - col1 = ( NULL );
  - name: tab1

outputs:
  - name: q640.out
    data: |
