script: |
  super -f tsv -I q640.sql | sort > q640.out

vector: true

inputs:
  - name: q640.sql
    data: |
      SELECT DISTINCT + 20 + - col1 FROM tab1 WHERE - col1 = ( NULL );
  - name: tab1

outputs:
  - name: q640.out
    data: |
