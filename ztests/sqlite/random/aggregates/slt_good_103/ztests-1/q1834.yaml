script: |
  super -f text -I q1834.spq | sort > q1834.out

inputs:
  - name: q1834.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) AS cor0 WHERE NOT ( NOT + + ( + col2 ) IS NULL );
  - name: tab2

outputs:
  - name: q1834.out
    data: |
