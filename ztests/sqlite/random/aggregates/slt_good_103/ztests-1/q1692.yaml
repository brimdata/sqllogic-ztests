script: |
  super -f tsv -I q1692.sql | sort > q1692.out

vector: true

inputs:
  - name: q1692.sql
    data: |
      SELECT + ( + + COUNT ( * ) ) FROM tab1 WHERE NOT ( ( NOT NULL > NULL ) );
  - name: tab1

outputs:
  - name: q1692.out
    data: |
      0
