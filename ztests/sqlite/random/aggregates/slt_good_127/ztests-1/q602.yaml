script: |
  super -f text -I q602.spq | sort > q602.out

inputs:
  - name: q602.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT ( + 72 ) <> 95;
  - name: tab0

outputs:
  - name: q602.out
    data: |
