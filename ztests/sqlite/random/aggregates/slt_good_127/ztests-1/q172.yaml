script: |
  super -f tsv -I q172.sql | sort > q172.out

vector: true

inputs:
  - name: q172.sql
    data: |
      SELECT ALL * FROM tab1 WHERE NOT - 85 >= NULL;
  - name: tab1

outputs:
  - name: q172.out
    data: |
