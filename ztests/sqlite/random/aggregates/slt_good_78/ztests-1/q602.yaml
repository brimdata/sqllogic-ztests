script: |
  super -f tsv -I q602.sql | sort > q602.out

vector: true

inputs:
  - name: q602.sql
    data: |
      SELECT DISTINCT * FROM tab0 WHERE NOT - col2 - - col0 + col0 IS NOT NULL;
  - name: tab0

outputs:
  - name: q602.out
    data: |
