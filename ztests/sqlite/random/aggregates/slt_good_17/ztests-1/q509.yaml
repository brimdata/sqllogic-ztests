script: |
  super -f text -I q509.spq | sort > q509.out

inputs:
  - name: q509.spq
    data: |
      SELECT ALL + CAST ( NULL AS INTEGER ) + + - col0 * + col0 * - + 78 FROM tab0 (FORMAT parquet) WHERE NOT NULL >= col1 * CAST ( NULL AS INTEGER );
  - name: tab0

outputs:
  - name: q509.out
    data: |
