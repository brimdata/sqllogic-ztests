script: |
  super -f text -I q5172.spq | sort > q5172.out

inputs:
  - name: q5172.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( - col0 * col1 + col2 * 78 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q5172.out
    data: |
