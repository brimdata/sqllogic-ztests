script: |
  super -f tsv -I q5172.sql | sort > q5172.out

vector: true

inputs:
  - name: q5172.sql
    data: |
      SELECT * FROM tab1 WHERE NOT ( - col0 * col1 + col2 * 78 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q5172.out
    data: |
