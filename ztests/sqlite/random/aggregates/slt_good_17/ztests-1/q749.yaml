script: |
  super -f text -I q749.spq | sort > q749.out

inputs:
  - name: q749.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NULL NOT BETWEEN - col2 * - 65 AND NULL;
  - name: tab1

outputs:
  - name: q749.out
    data: |
