script: |
  super -f text -I q2555.spq | sort > q2555.out

inputs:
  - name: q2555.spq
    data: |
      SELECT + col1 FROM tab1 (FORMAT parquet) WHERE NOT ( + col0 * - col0 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q2555.out
    data: |
