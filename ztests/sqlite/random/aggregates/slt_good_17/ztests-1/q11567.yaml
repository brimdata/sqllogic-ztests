script: |
  super -f tsv -I q11567.sql | sort > q11567.out

vector: true

inputs:
  - name: q11567.sql
    data: |
      SELECT * FROM tab0 WHERE NOT ( + + col2 * - - col0 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q11567.out
    data: |
