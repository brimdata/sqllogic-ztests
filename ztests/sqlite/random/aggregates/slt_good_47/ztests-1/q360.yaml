script: |
  super -f text -I q360.spq | sort > q360.out

inputs:
  - name: q360.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE col0 + col2 <= ( NULL );
  - name: tab0

outputs:
  - name: q360.out
    data: |
