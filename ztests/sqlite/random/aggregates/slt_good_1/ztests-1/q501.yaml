script: |
  super -f text -I q501.spq | sort > q501.out

inputs:
  - name: q501.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT NULL = - col0 + - - col1 AND + col1 <= + + 62;
  - name: tab0

outputs:
  - name: q501.out
    data: |
