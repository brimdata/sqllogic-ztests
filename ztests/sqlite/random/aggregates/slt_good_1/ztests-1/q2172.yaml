script: |
  super -f tsv -I q2172.sql | sort > q2172.out

vector: true

inputs:
  - name: q2172.sql
    data: |
      SELECT * FROM tab1 WHERE NOT + + col2 IS NOT NULL;
  - name: tab1

outputs:
  - name: q2172.out
    data: |
