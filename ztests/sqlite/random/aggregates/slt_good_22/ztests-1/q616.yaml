script: |
  super -f text -I q616.spq | sort > q616.out

inputs:
  - name: q616.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT NULL NOT IN ( - col1 * - + col0 * - col0 + + + col0, col1 );
  - name: tab1

outputs:
  - name: q616.out
    data: |
