script: |
  super -f text -I q191.spq | sort > q191.out

inputs:
  - name: q191.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT + + CAST ( 60 AS INTEGER ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q191.out
    data: |
