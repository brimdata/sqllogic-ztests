script: |
  super -f text -I q1834.spq | sort > q1834.out

inputs:
  - name: q1834.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT + - col2 BETWEEN NULL AND NULL OR NULL IS NOT NULL;
  - name: tab1

outputs:
  - name: q1834.out
    data: |
