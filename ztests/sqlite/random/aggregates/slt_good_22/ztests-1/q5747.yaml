script: |
  super -f text -I q5747.spq | sort > q5747.out

inputs:
  - name: q5747.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT + ( - - col1 ) + - CAST ( NULL AS INTEGER ) IS NULL;
  - name: tab1

outputs:
  - name: q5747.out
    data: |
