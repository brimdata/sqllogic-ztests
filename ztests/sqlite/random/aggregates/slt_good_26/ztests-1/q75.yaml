script: |
  super -f tsv -I q75.sql | sort > q75.out

vector: true

inputs:
  - name: q75.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL NOT IN ( + col0 + + col1 );
  - name: tab1

outputs:
  - name: q75.out
    data: |
