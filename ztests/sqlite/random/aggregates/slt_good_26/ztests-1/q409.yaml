script: |
  super -f text -I q409.spq | sort > q409.out

inputs:
  - name: q409.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE - CAST ( + col0 AS INTEGER ) / - CAST ( + - col2 AS INTEGER ) > NULL;
  - name: tab0

outputs:
  - name: q409.out
    data: |
