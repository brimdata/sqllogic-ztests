script: |
  super -f text -I q6628.spq | sort > q6628.out

inputs:
  - name: q6628.spq
    data: |
      SELECT DISTINCT - 53 FROM tab1 (FORMAT parquet) WHERE NOT NULL = ( + ( + - col0 ) );
  - name: tab1

outputs:
  - name: q6628.out
    data: |
