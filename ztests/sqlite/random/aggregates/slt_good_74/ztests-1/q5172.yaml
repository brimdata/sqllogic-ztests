script: |
  super -f tsv -I q5172.sql | sort > q5172.out

vector: true

inputs:
  - name: q5172.sql
    data: |
      SELECT * FROM tab0 WHERE NOT ( + + col1 + col0 ) <> col1 + col1;
  - name: tab0

outputs:
  - name: q5172.out
    data: |
