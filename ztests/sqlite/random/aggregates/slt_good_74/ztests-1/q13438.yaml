script: |
  super -f text -I q13438.spq | sort > q13438.out

inputs:
  - name: q13438.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT ( NOT + col0 * + col0 IS NULL );
  - name: tab2

outputs:
  - name: q13438.out
    data: |
