script: |
  super -f text -I q6959.spq | sort > q6959.out

inputs:
  - name: q6959.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) cor0 CROSS JOIN tab0 (FORMAT parquet) WHERE NOT ( NULL ) BETWEEN NULL AND + 97;
  - name: tab0
  - name: tab1

outputs:
  - name: q6959.out
    data: |
