script: |
  super -f text -I q10728.spq | sort > q10728.out

inputs:
  - name: q10728.spq
    data: |
      SELECT DISTINCT col0 * - col0 - 28 * + col1 FROM tab1 (FORMAT parquet) WHERE NOT ( + col1 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q10728.out
    data: |
