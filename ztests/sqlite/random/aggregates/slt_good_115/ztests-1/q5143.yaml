script: |
  super -f text -I q5143.spq | sort > q5143.out

inputs:
  - name: q5143.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE ( ( + col0 ) + - col2 * - col1 ) IS NULL;
  - name: tab1

outputs:
  - name: q5143.out
    data: |
