script: |
  super -f tsv -I q5143.sql | sort > q5143.out

vector: true

inputs:
  - name: q5143.sql
    data: |
      SELECT * FROM tab1 WHERE ( ( + col0 ) + - col2 * - col1 ) IS NULL;
  - name: tab1

outputs:
  - name: q5143.out
    data: |
