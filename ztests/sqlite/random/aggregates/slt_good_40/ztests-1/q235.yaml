script: |
  super -f text -I q235.spq | sort > q235.out

inputs:
  - name: q235.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE - - ( + col0 ) IS NULL;
  - name: tab1

outputs:
  - name: q235.out
    data: |
