script: |
  super -f text -I q171.spq | sort > q171.out

inputs:
  - name: q171.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) cor0 WHERE NOT NULL = CAST ( + - col1 AS INTEGER );
  - name: tab0

outputs:
  - name: q171.out
    data: |
