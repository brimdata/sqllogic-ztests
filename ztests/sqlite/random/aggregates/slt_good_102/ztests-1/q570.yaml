script: |
  super -f tsv -I q570.sql | sort > q570.out

vector: true

inputs:
  - name: q570.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL BETWEEN - ( + 24 ) * + col1 AND NULL;
  - name: tab1

outputs:
  - name: q570.out
    data: |
