script: |
  super -f text -I q573.spq | sort > q573.out

inputs:
  - name: q573.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT + col2 * - 30 IS NOT NULL;
  - name: tab0

outputs:
  - name: q573.out
    data: |
