script: |
  super -f tsv -I q172.sql | sort > q172.out

vector: true

inputs:
  - name: q172.sql
    data: |
      SELECT DISTINCT * FROM tab2 WHERE ( - 37 * - 20 * col0 ) <> ( NULL );
  - name: tab2

outputs:
  - name: q172.out
    data: |
