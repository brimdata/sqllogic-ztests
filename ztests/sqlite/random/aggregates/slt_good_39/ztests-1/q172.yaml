script: |
  super -f text -I q172.spq | sort > q172.out

inputs:
  - name: q172.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE ( - 37 * - 20 * col0 ) <> ( NULL );
  - name: tab2

outputs:
  - name: q172.out
    data: |
