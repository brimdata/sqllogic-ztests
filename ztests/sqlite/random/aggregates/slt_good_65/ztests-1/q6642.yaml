script: |
  super -f text -I q6642.spq | sort > q6642.out

inputs:
  - name: q6642.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT + col1 IS NOT NULL OR NOT ( + col0 / - col2 < NULL );
  - name: tab0

outputs:
  - name: q6642.out
    data: |
