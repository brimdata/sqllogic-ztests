script: |
  super -f text -I q834.spq | sort > q834.out

inputs:
  - name: q834.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( NOT - col2 * + col2 * 58 = NULL );
  - name: tab1

outputs:
  - name: q834.out
    data: |
