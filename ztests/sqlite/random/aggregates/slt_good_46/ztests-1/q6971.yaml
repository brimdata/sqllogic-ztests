script: |
  super -f text -I q6971.spq | sort > q6971.out

inputs:
  - name: q6971.spq
    data: |
      SELECT ALL col2 / col0 AS col1 FROM tab2 (FORMAT parquet) WHERE NOT ( + col1 + col2 ) IS NULL;
  - name: tab2

outputs:
  - name: q6971.out
    data: |
      0
      0
      0
