script: |
  super -f tsv -I q895.sql | sort > q895.out

vector: true

inputs:
  - name: q895.sql
    data: |
      SELECT * FROM tab2 cor0 WHERE NOT NULL <= ( NULL );
  - name: tab2

outputs:
  - name: q895.out
    data: |
