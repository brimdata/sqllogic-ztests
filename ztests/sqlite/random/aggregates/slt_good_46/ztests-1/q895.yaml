script: |
  super -f text -I q895.spq | sort > q895.out

inputs:
  - name: q895.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) cor0 WHERE NOT NULL <= ( NULL );
  - name: tab2

outputs:
  - name: q895.out
    data: |
