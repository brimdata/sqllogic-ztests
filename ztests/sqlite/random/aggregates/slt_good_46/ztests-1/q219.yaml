script: |
  super -f text -I q219.spq | sort > q219.out

inputs:
  - name: q219.spq
    data: |
      SELECT DISTINCT - COUNT ( * ) FROM tab0 (FORMAT parquet) WHERE NOT ( + col2 ) < + col1 - col1;
  - name: tab0

outputs:
  - name: q219.out
    data: |
      -3
