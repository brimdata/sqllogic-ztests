script: |
  super -f text -I q1994.spq | sort > q1994.out

inputs:
  - name: q1994.spq
    data: |
      SELECT DISTINCT + col2 * - col0 - + col0 FROM tab0 (FORMAT parquet);
  - name: tab0

outputs:
  - name: q1994.out
    data: |
      -720
      -957
      -9700
