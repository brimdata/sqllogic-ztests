script: |
  super -f tsv -I q351.sql | sort > q351.out

vector: true

inputs:
  - name: q351.sql
    data: |
      SELECT DISTINCT * FROM tab2 WHERE NOT + 40 NOT IN ( + col2 * col1 );
  - name: tab2

outputs:
  - name: q351.out
    data: |
