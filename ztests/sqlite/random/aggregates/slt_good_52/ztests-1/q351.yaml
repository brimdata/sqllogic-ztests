script: |
  super -f text -I q351.spq | sort > q351.out

inputs:
  - name: q351.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT + 40 NOT IN ( + col2 * col1 );
  - name: tab2

outputs:
  - name: q351.out
    data: |
