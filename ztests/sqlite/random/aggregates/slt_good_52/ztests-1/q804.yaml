script: |
  super -f text -I q804.spq | sort > q804.out

inputs:
  - name: q804.spq
    data: |
      SELECT DISTINCT - COUNT ( * ) FROM tab0 (FORMAT parquet) WHERE NOT + col1 * - 16 IS NULL;
  - name: tab0

outputs:
  - name: q804.out
    data: |
      -3
