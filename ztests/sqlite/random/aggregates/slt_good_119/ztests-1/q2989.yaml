script: |
  super -f text -I q2989.spq | sort > q2989.out

inputs:
  - name: q2989.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT NULL NOT BETWEEN col1 AND + col2 * - ( col0 ) + - ( + + 6 );
  - name: tab1

outputs:
  - name: q2989.out
    data: |
