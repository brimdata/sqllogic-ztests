script: |
  super -f text -I q628.spq | sort > q628.out

inputs:
  - name: q628.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) cor0 WHERE col1 + - col1 = + col0 OR NOT col1 + col1 IS NOT NULL;
  - name: tab2

outputs:
  - name: q628.out
    data: |
