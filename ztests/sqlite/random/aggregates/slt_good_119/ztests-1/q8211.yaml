script: |
  super -f text -I q8211.spq | sort > q8211.out

inputs:
  - name: q8211.spq
    data: |
      SELECT + col1 - + col0 AS col2 FROM tab1 (FORMAT parquet) WHERE NOT + col0 + col2 * - CAST ( + + col1 AS INTEGER ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q8211.out
    data: |
