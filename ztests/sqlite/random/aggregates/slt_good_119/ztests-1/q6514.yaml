script: |
  super -f text -I q6514.spq | sort > q6514.out

inputs:
  - name: q6514.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT + 5 * col1 NOT IN ( + col1 );
  - name: tab0

outputs:
  - name: q6514.out
    data: |
