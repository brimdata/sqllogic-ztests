script: |
  super -f text -I q150.spq | sort > q150.out

inputs:
  - name: q150.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT NULL = col2 * - - col1 + + col1 OR - col1 / + 38 IS NULL;
  - name: tab1

outputs:
  - name: q150.out
    data: |
