script: |
  super -f text -I q895.spq | sort > q895.out

inputs:
  - name: q895.spq
    data: |
      SELECT DISTINCT + col1 AS col0 FROM tab2 (FORMAT parquet) WHERE NOT ( col0 / + col2 ) IS NOT NULL;
  - name: tab2

outputs:
  - name: q895.out
    data: |
