script: |
  super -f text -I q11912.spq | sort > q11912.out

inputs:
  - name: q11912.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT NULL BETWEEN 14 / - - col1 * 8 * - 0 / col2 + 80 AND ( NULL );
  - name: tab0

outputs:
  - name: q11912.out
    data: |
