script: |
  super -f text -I q5087.spq | sort > q5087.out

inputs:
  - name: q5087.spq
    data: |
      SELECT ALL * FROM tab2 (FORMAT parquet) WHERE NOT + col0 + - col0 + - col0 NOT IN ( + col0, - col1 / col1 );
  - name: tab2

outputs:
  - name: q5087.out
    data: |
