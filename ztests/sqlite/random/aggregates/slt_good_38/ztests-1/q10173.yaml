script: |
  super -f text -I q10173.spq | sort > q10173.out

inputs:
  - name: q10173.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT ( col2 * - col0 + + col0 IS NOT NULL );
  - name: tab1

outputs:
  - name: q10173.out
    data: |
