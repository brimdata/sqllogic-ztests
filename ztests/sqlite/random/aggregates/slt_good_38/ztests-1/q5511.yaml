script: |
  super -f text -I q5511.spq | sort > q5511.out

inputs:
  - name: q5511.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT NULL <> + col2 * - col1 + - col1;
  - name: tab2

outputs:
  - name: q5511.out
    data: |
