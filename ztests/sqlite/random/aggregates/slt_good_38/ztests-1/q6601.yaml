script: |
  super -f text -I q6601.spq | sort > q6601.out

inputs:
  - name: q6601.spq
    data: |
      SELECT ALL * FROM tab0 (FORMAT parquet) WHERE NOT ( + col2 * - col1 + + col1 * - col2 - col1 * col1 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q6601.out
    data: |
