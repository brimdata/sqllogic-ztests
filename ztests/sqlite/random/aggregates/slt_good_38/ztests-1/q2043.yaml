script: |
  super -f text -I q2043.spq | sort > q2043.out

inputs:
  - name: q2043.spq
    data: |
      SELECT + - col2 FROM tab2 (FORMAT parquet) AS cor0 WHERE NOT ( NOT + - col1 <= NULL );
  - name: tab2

outputs:
  - name: q2043.out
    data: |
