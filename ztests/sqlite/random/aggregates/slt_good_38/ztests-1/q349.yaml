script: |
  super -f text -I q349.spq | sort > q349.out

inputs:
  - name: q349.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) AS cor0 WHERE NOT NULL NOT IN ( - 80, col1 );
  - name: tab0

outputs:
  - name: q349.out
    data: |
