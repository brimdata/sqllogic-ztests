script: |
  super -f tsv -I q567.sql | sort > q567.out

vector: true

inputs:
  - name: q567.sql
    data: |
      SELECT * FROM tab0 WHERE NOT ( ( - col1 * - col0 + + col2 >= + + col1 ) );
  - name: tab0

outputs:
  - name: q567.out
    data: |
