script: |
  super -f text -I q50.spq | sort > q50.out

inputs:
  - name: q50.spq
    data: |
      SELECT - COUNT ( * ) AS col0 FROM tab1 (FORMAT parquet) WHERE NOT + + col1 * - col1 * - col1 IS NULL;
  - name: tab1

outputs:
  - name: q50.out
    data: |
      -3
