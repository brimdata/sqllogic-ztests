script: |
  super -f tsv -I q90.sql | sort > q90.out

vector: true

inputs:
  - name: q90.sql
    data: |
      SELECT * FROM tab0 WHERE NOT col0 * - col1 * + col1 + - col0 + + ( + - col1 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q90.out
    data: |
