script: |
  super -f text -I q640.spq | sort > q640.out

inputs:
  - name: q640.spq
    data: |
      SELECT DISTINCT + - MAX ( + - col1 ) * - COUNT ( * ) * - - 2 FROM tab0 (FORMAT parquet) WHERE NOT NULL IS NOT NULL;
  - name: tab0

outputs:
  - name: q640.out
    data: |
      -6
