script: |
  super -f text -I q1305.spq | sort > q1305.out

inputs:
  - name: q1305.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT col2 < ( + col2 + col1 );
  - name: tab0

outputs:
  - name: q1305.out
    data: |
