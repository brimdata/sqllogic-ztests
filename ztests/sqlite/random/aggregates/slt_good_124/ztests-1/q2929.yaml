script: |
  super -f tsv -I q2929.sql | sort > q2929.out

vector: true

inputs:
  - name: q2929.sql
    data: |
      SELECT * FROM tab1 WHERE ( NULL ) IN ( + col0 + + - col1, col0 + + + col1 );
  - name: tab1

outputs:
  - name: q2929.out
    data: |
