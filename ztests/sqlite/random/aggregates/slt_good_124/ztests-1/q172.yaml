script: |
  super -f text -I q172.sql | sort > q172.out

inputs:
  - name: q172.sql
    data: |
      SELECT DISTINCT - col1 FROM tab2 WHERE NOT col1 + + col1 IS NOT NULL;
  - name: tab2

outputs:
  - name: q172.out
    data: |
