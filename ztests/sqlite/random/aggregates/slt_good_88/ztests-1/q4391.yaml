script: |
  super -f tsv -I q4391.sql | sort > q4391.out

vector: true

inputs:
  - name: q4391.sql
    data: |
      SELECT DISTINCT + MIN ( ALL col1 ) FROM tab1 WHERE + col1 > ( NULL );
  - name: tab1

outputs:
  - name: q4391.out
    data: |
      NULL
