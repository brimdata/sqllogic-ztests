script: |
  super -f text -I q172.spq | sort > q172.out

inputs:
  - name: q172.spq
    data: |
      SELECT DISTINCT + 22 AS col0 FROM tab2 (FORMAT parquet) AS cor0 WHERE ( ( + col1 ) + - + col0 ) IS NOT NULL;
  - name: tab2

outputs:
  - name: q172.out
    data: |
      22
