script: |
  super -f text -I q994.spq | sort > q994.out

inputs:
  - name: q994.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) AS cor0 WHERE NOT ( NULL ) >= + - col1 / - + col2 * - col2;
  - name: tab0

outputs:
  - name: q994.out
    data: |
