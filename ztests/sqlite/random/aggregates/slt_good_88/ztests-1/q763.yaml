script: |
  super -f text -I q763.spq | sort > q763.out

inputs:
  - name: q763.spq
    data: |
      SELECT ALL * FROM tab2 (FORMAT parquet) WHERE NOT NULL BETWEEN ( + + col0 ) AND + ( + col1 ) * + - col0 * col1 * col0;
  - name: tab2

outputs:
  - name: q763.out
    data: |
