script: |
  super -f tsv -I q11172.sql | sort > q11172.out

vector: true

inputs:
  - name: q11172.sql
    data: |
      SELECT * FROM tab1 cor0 WHERE NOT ( NULL ) <= + + col1;
  - name: tab1

outputs:
  - name: q11172.out
    data: |
