script: |
  super -f text -I q514.spq | sort > q514.out

inputs:
  - name: q514.spq
    data: |
      SELECT DISTINCT + + col0 * - 3 - - col2 AS col2 FROM tab1 (FORMAT parquet) WHERE NOT - + col2 IS NOT NULL;
  - name: tab1

outputs:
  - name: q514.out
    data: |
