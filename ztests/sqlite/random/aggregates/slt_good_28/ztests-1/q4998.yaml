script: |
  super -f text -I q4998.spq | sort > q4998.out

inputs:
  - name: q4998.spq
    data: |
      SELECT + col0 FROM tab1 (FORMAT parquet) WHERE NOT ( NOT ( NULL ) <= NULL );
  - name: tab1

outputs:
  - name: q4998.out
    data: |
