script: |
  super -f text -I q668.spq | sort > q668.out

inputs:
  - name: q668.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT col1 NOT BETWEEN NULL AND - col2 / - col1 * - col0 + col2;
  - name: tab2

outputs:
  - name: q668.out
    data: |
