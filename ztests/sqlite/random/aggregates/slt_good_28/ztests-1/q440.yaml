script: |
  super -f text -I q440.spq | sort > q440.out

inputs:
  - name: q440.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT + col2 * - col2 <> + col2 + - col0;
  - name: tab1

outputs:
  - name: q440.out
    data: |
