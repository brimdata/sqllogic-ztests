script: |
  super -f text -I q10719.spq | sort > q10719.out

inputs:
  - name: q10719.spq
    data: |
      SELECT DISTINCT + col0 AS col1 FROM tab1 (FORMAT parquet) WHERE NOT + col0 / + col1 IS NOT NULL;
  - name: tab1

outputs:
  - name: q10719.out
    data: |
