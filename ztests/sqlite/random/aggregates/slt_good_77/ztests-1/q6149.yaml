script: |
  super -f text -I q6149.spq | sort > q6149.out

inputs:
  - name: q6149.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT NULL NOT IN ( + col1, + col2, col0, + ( 4 ) + - col1 * - + 93 );
  - name: tab0

outputs:
  - name: q6149.out
    data: |
