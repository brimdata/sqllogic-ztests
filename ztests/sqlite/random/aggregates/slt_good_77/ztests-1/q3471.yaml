script: |
  super -f text -I q3471.spq | sort > q3471.out

inputs:
  - name: q3471.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + col2 NOT IN ( + - col2, col0, + col0, + + col0 * - col2 );
  - name: tab1

outputs:
  - name: q3471.out
    data: |
