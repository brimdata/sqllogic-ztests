script: |
  super -f text -I q5840.spq | sort > q5840.out

inputs:
  - name: q5840.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) cor0 INNER JOIN tab2 (FORMAT parquet) cor1 ON NULL <= NULL WHERE NOT NULL >= NULL;
  - name: tab2

outputs:
  - name: q5840.out
    data: |
