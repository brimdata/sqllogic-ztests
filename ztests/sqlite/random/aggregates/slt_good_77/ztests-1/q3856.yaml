script: |
  super -f text -I q3856.spq | sort > q3856.out

inputs:
  - name: q3856.spq
    data: |
      SELECT - ( 89 ) / col2 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q3856.out
    data: |
      -1
      -1
      0
