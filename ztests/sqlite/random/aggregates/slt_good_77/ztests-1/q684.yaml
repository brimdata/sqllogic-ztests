script: |
  super -f text -I q684.spq | sort > q684.out

inputs:
  - name: q684.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NULL NOT BETWEEN + col0 * - col1 AND + - 39 / col1 * - 3;
  - name: tab1

outputs:
  - name: q684.out
    data: |
