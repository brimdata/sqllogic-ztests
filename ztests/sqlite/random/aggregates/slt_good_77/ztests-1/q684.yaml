script: |
  super -f tsv -I q684.sql | sort > q684.out

vector: true

inputs:
  - name: q684.sql
    data: |
      SELECT * FROM tab1 WHERE NULL NOT BETWEEN + col0 * - col1 AND + - 39 / col1 * - 3;
  - name: tab1

outputs:
  - name: q684.out
    data: |
