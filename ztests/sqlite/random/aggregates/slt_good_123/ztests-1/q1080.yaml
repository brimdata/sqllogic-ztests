script: |
  super -f text -I q1080.spq | sort > q1080.out

inputs:
  - name: q1080.spq
    data: |
      SELECT DISTINCT + col1 + ( + col0 + col0 ) FROM tab2 (FORMAT parquet) WHERE NOT ( NOT col1 < - col1 );
  - name: tab2

outputs:
  - name: q1080.out
    data: |
