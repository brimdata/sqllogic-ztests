script: |
  super -f tsv -I q1080.sql | sort > q1080.out

vector: true

inputs:
  - name: q1080.sql
    data: |
      SELECT DISTINCT + col1 + ( + col0 + col0 ) FROM tab2 WHERE NOT ( NOT col1 < - col1 );
  - name: tab2

outputs:
  - name: q1080.out
    data: |
