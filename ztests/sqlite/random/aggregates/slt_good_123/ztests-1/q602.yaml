script: |
  super -f text -I q602.spq | sort > q602.out

inputs:
  - name: q602.spq
    data: |
      SELECT 30 FROM tab2 (FORMAT parquet) AS cor0 WHERE col0 >= col2 / + col0 * - + 98 * col1;
  - name: tab2

outputs:
  - name: q602.out
    data: |
      30
      30
      30
