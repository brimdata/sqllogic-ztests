script: |
  super -f text -I q186.spq | sort > q186.out

inputs:
  - name: q186.spq
    data: |
      SELECT ALL * FROM tab2 (FORMAT parquet) WHERE NOT ( ( NOT ( + 95 * + col0 ) < ( NULL ) ) );
  - name: tab2

outputs:
  - name: q186.out
    data: |
