script: |
  super -f text -I q6701.spq | sort > q6701.out

inputs:
  - name: q6701.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT ( NOT ( ( NULL ) IS NOT NULL ) );
  - name: tab2

outputs:
  - name: q6701.out
    data: |
