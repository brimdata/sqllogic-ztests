script: |
  super -f text -I q1305.spq | sort > q1305.out

inputs:
  - name: q1305.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) AS cor0 CROSS JOIN tab0 (FORMAT parquet) AS cor1 WHERE NOT ( 95 ) IS NOT NULL;
  - name: tab0
  - name: tab2

outputs:
  - name: q1305.out
    data: |
