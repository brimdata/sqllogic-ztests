script: |
  super -f text -I q1886.spq | sort > q1886.out

inputs:
  - name: q1886.spq
    data: |
      SELECT DISTINCT + col1 FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT NULL NOT IN ( + col2 + - col1, + col2, col0, + + col1, col2 );
  - name: tab1

outputs:
  - name: q1886.out
    data: |
