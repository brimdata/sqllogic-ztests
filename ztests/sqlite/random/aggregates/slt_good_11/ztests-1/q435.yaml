script: |
  super -f text -I q435.spq | sort > q435.out

inputs:
  - name: q435.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT ( + col1 ) <= NULL;
  - name: tab0

outputs:
  - name: q435.out
    data: |
