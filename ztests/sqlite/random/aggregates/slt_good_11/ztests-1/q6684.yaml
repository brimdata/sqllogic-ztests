script: |
  super -f tsv -I q6684.sql | sort > q6684.out

vector: true

inputs:
  - name: q6684.sql
    data: |
      SELECT DISTINCT * FROM tab2 WHERE NOT + + col0 * - - ( + col1 ) IS NOT NULL;
  - name: tab2

outputs:
  - name: q6684.out
    data: |
