script: |
  super -f tsv -I q2929.sql | sort > q2929.out

vector: true

inputs:
  - name: q2929.sql
    data: |
      SELECT * FROM tab0 WHERE NOT + col0 <> col2 * - col2;
  - name: tab0

outputs:
  - name: q2929.out
    data: |
