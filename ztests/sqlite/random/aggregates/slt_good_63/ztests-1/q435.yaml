script: |
  super -f text -I q435.spq | sort > q435.out

inputs:
  - name: q435.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT NULL <= + col2 * col2 * col1 + + col0;
  - name: tab0

outputs:
  - name: q435.out
    data: |
