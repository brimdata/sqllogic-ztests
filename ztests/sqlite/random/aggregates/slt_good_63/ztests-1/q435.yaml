script: |
  super -f tsv -I q435.sql | sort > q435.out

vector: true

inputs:
  - name: q435.sql
    data: |
      SELECT * FROM tab0 WHERE NOT NULL <= + col2 * col2 * col1 + + col0;
  - name: tab0

outputs:
  - name: q435.out
    data: |
