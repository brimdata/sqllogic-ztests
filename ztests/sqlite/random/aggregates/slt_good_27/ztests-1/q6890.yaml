script: |
  super -f text -I q6890.spq | sort > q6890.out

inputs:
  - name: q6890.spq
    data: |
      SELECT ALL + - col0 * - col1 FROM tab2 (FORMAT parquet) WHERE NOT ( ( NOT + - 56 IS NULL ) );
  - name: tab2

outputs:
  - name: q6890.out
    data: |
