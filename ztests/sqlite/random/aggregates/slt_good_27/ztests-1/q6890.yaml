script: |
  super -f tsv -I q6890.sql | sort > q6890.out

vector: true

inputs:
  - name: q6890.sql
    data: |
      SELECT ALL + - col0 * - col1 FROM tab2 WHERE NOT ( ( NOT + - 56 IS NULL ) );
  - name: tab2

outputs:
  - name: q6890.out
    data: |
