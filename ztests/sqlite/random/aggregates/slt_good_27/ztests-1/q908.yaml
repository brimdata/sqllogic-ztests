script: |
  super -f text -I q908.spq | sort > q908.out

inputs:
  - name: q908.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT - - 28 IS NOT NULL;
  - name: tab1

outputs:
  - name: q908.out
    data: |
