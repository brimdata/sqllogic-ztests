script: |
  super -f text -I q874.spq | sort > q874.out

inputs:
  - name: q874.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NULL = + col0 + - col0 + 69 * + col0;
  - name: tab1

outputs:
  - name: q874.out
    data: |
