script: |
  super -f text -I q6079.spq | sort > q6079.out

inputs:
  - name: q6079.spq
    data: |
      SELECT 60 + - SUM ( + + col0 ) FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q6079.out
    data: |
      -167
