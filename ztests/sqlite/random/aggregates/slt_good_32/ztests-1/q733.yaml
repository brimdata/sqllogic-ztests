script: |
  super -f text -I q733.spq | sort > q733.out

inputs:
  - name: q733.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT NULL > - CAST ( - col2 AS REAL ) + - - CAST ( NULL AS INTEGER );
  - name: tab1

outputs:
  - name: q733.out
    data: |
