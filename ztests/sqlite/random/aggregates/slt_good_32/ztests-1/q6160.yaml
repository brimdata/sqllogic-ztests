script: |
  super -f text -I q6160.spq | sort > q6160.out

inputs:
  - name: q6160.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE col0 * - col0 - ( + col2 ) * col0 > ( NULL );
  - name: tab1

outputs:
  - name: q6160.out
    data: |
