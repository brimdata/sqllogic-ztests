script: |
  super -f tsv -I q634.sql | sort > q634.out

vector: true

inputs:
  - name: q634.sql
    data: |
      SELECT + COUNT ( * ) FROM tab2 WHERE NOT NULL NOT IN ( + col0 );
  - name: tab2

outputs:
  - name: q634.out
    data: |
      0
