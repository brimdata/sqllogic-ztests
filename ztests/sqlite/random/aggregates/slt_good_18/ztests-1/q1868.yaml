script: |
  super -f text -I q1868.spq | sort > q1868.out

inputs:
  - name: q1868.spq
    data: |
      SELECT DISTINCT + col0 AS col0 FROM tab1 (FORMAT parquet) WHERE NOT + col1 * col1 * - col1 + col1 < ( col2 );
  - name: tab1

outputs:
  - name: q1868.out
    data: |
