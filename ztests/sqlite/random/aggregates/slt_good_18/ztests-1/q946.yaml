script: |
  super -f text -I q946.spq | sort > q946.out

inputs:
  - name: q946.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT + col1 * - col1 <> NULL;
  - name: tab1

outputs:
  - name: q946.out
    data: |
