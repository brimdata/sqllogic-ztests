script: |
  super -f tsv -I q874.sql | sort > q874.out

vector: true

inputs:
  - name: q874.sql
    data: |
      SELECT * FROM tab1 WHERE NOT col0 * - 31 > ( NULL );
  - name: tab1

outputs:
  - name: q874.out
    data: |
