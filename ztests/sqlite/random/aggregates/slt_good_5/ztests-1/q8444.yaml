script: |
  super -f text -I q8444.spq | sort > q8444.out

inputs:
  - name: q8444.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT ( + + col2 * - col0 - col0 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q8444.out
    data: |
