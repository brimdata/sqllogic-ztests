script: |
  super -f text -I q2043.spq | sort > q2043.out

inputs:
  - name: q2043.spq
    data: |
      SELECT DISTINCT ( + 1 ) AS col2 FROM tab2 (FORMAT parquet) AS cor0 INNER JOIN tab2 (FORMAT parquet) ON NOT NULL NOT IN ( 86 );
  - name: tab2

outputs:
  - name: q2043.out
    data: |
