script: |
  super -f tsv -I q804.sql | sort > q804.out

vector: true

inputs:
  - name: q804.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE col1 / col1 <> NULL;
  - name: tab1

outputs:
  - name: q804.out
    data: |
