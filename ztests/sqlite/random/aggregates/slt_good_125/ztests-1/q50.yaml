script: |
  super -f text -I q50.spq | sort > q50.out

inputs:
  - name: q50.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT ( + col2 ) NOT BETWEEN NULL AND + col0 * CAST ( NULL AS REAL ) + + col2 * - col2;
  - name: tab1

outputs:
  - name: q50.out
    data: |
