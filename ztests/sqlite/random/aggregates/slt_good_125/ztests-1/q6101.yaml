script: |
  super -f text -I q6101.spq | sort > q6101.out

inputs:
  - name: q6101.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT NULL = NULL AND CAST ( - col0 AS INTEGER ) * - col2 = ( NULL );
  - name: tab0

outputs:
  - name: q6101.out
    data: |
