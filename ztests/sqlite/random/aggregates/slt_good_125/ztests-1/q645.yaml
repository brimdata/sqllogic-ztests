script: |
  super -f text -I q645.spq | sort > q645.out

inputs:
  - name: q645.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( ( - col1 ) ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q645.out
    data: |
