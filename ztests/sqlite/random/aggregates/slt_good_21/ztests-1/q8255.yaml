script: |
  super -f text -I q8255.spq | sort > q8255.out

inputs:
  - name: q8255.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT + col1 NOT IN ( + col2 + col0 + - 1 * - col0 + - col2 );
  - name: tab1

outputs:
  - name: q8255.out
    data: |
