script: |
  super -f tsv -I q413.sql | sort > q413.out

vector: true

inputs:
  - name: q413.sql
    data: |
      SELECT COUNT ( * ) / + COUNT ( * ) + MIN ( DISTINCT - col2 ) FROM tab1;
  - name: tab1

outputs:
  - name: q413.out
    data: |
      -95
