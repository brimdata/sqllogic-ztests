script: |
  super -f text -I q172.spq | sort > q172.out

inputs:
  - name: q172.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT ( + 53 ) <= + CAST ( NULL AS INTEGER ) AND NOT + ( col2 ) IS NULL;
  - name: tab2

outputs:
  - name: q172.out
    data: |
