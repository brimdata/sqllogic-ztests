script: |
  super -f text -I q874.spq | sort > q874.out

inputs:
  - name: q874.spq
    data: |
      SELECT ALL - 50 + + 90 FROM tab1 (FORMAT parquet) WHERE ( NOT NULL <= NULL );
  - name: tab1

outputs:
  - name: q874.out
    data: |
