script: |
  super -f text -I q3545.spq | sort > q3545.out

inputs:
  - name: q3545.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL BETWEEN NULL AND ( + col1 * col1 );
  - name: tab1

outputs:
  - name: q3545.out
    data: |
