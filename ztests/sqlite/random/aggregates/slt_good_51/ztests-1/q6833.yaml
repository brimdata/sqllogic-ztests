script: |
  super -f text -I q6833.spq | sort > q6833.out

inputs:
  - name: q6833.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT col2 + col0 * - col1 NOT IN ( col1 );
  - name: tab2

outputs:
  - name: q6833.out
    data: |
