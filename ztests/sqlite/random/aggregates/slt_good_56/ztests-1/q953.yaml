script: |
  super -f text -I q953.spq | sort > q953.out

inputs:
  - name: q953.spq
    data: |
      SELECT ALL - col1 * - col1 * + - CAST ( col2 AS INTEGER ) FROM tab1 (FORMAT parquet) cor0 WHERE col0 * - col1 * + col0 IS NULL;
  - name: tab1

outputs:
  - name: q953.out
    data: |
