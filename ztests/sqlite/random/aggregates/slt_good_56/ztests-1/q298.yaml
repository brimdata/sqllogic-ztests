script: |
  super -f text -I q298.spq | sort > q298.out

inputs:
  - name: q298.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT - CAST ( + ( col2 ) AS INTEGER ) BETWEEN NULL AND NULL;
  - name: tab1

outputs:
  - name: q298.out
    data: |
