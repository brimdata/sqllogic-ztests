script: |
  super -f text -I q3415.spq | sort > q3415.out

inputs:
  - name: q3415.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE ( NOT + col1 * + col0 + col0 IS NOT NULL );
  - name: tab1

outputs:
  - name: q3415.out
    data: |
