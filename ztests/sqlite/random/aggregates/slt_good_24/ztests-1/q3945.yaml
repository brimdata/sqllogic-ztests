script: |
  super -f tsv -I q3945.sql | sort > q3945.out

vector: true

inputs:
  - name: q3945.sql
    data: |
      SELECT DISTINCT + MIN ( col2 ) FROM tab0 WHERE ( - col2 / col1 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q3945.out
    data: |
      10
