script: |
  super -f tsv -I q377.sql | sort > q377.out

vector: true

inputs:
  - name: q377.sql
    data: |
      SELECT + col0 + - col2 FROM tab1 WHERE NULL = NULL;
  - name: tab1

outputs:
  - name: q377.out
    data: |
