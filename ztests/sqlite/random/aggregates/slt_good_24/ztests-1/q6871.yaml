script: |
  super -f text -I q6871.spq | sort > q6871.out

inputs:
  - name: q6871.spq
    data: |
      SELECT COUNT ( * ) / + - 96 * SUM ( + CAST ( NULL AS REAL ) ) FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q6871.out
    data: |
      -
