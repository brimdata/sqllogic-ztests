script: |
  super -f text -I q9089.spq | sort > q9089.out

inputs:
  - name: q9089.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( + col0 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q9089.out
    data: |
