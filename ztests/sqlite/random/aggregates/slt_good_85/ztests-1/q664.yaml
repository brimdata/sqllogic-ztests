script: |
  super -f text -I q664.spq | sort > q664.out

inputs:
  - name: q664.spq
    data: |
      SELECT col1 + - 39 + + col2 * - 16 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q664.out
    data: |
      -1080
      -1561
      -978
