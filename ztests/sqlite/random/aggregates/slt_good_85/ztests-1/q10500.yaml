script: |
  super -f text -I q10500.spq | sort > q10500.out

inputs:
  - name: q10500.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 CROSS JOIN tab0 (FORMAT parquet) WHERE NOT - ( 96 ) IS NOT NULL;
  - name: tab0
  - name: tab1

outputs:
  - name: q10500.out
    data: |
