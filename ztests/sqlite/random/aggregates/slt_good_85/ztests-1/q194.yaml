script: |
  super -f text -I q194.spq | sort > q194.out

inputs:
  - name: q194.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT NULL IN ( col1 ) AND NOT NULL BETWEEN ( - ( + col2 ) ) AND NULL;
  - name: tab1

outputs:
  - name: q194.out
    data: |
