script: |
  super -f text -I q680.spq | sort > q680.out

inputs:
  - name: q680.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT ( + + col1 ) IS NOT NULL;
  - name: tab2

outputs:
  - name: q680.out
    data: |
