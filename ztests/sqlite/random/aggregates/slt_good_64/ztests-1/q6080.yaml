script: |
  super -f text -I q6080.spq | sort > q6080.out

inputs:
  - name: q6080.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT ( + col0 ) NOT BETWEEN ( NULL ) AND NULL;
  - name: tab2

outputs:
  - name: q6080.out
    data: |
