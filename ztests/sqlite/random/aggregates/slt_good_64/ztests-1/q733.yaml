script: |
  super -f text -I q733.sql | sort > q733.out

inputs:
  - name: q733.sql
    data: |
      SELECT ALL * FROM tab1 WHERE NOT col1 * + col2 IS NOT NULL;
  - name: tab1

outputs:
  - name: q733.out
    data: |
