script: |
  super -f text -I q802.spq | sort > q802.out

inputs:
  - name: q802.spq
    data: |
      SELECT + col0 FROM tab0 (FORMAT parquet) AS cor0 WHERE ( NULL ) BETWEEN NULL AND + col0 OR NULL IS NOT NULL;
  - name: tab0

outputs:
  - name: q802.out
    data: |
