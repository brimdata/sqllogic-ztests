script: |
  super -f text -I q3717.spq | sort > q3717.out

inputs:
  - name: q3717.spq
    data: |
      SELECT DISTINCT - col0 / + 99 FROM tab2 (FORMAT parquet) WHERE ( + col2 * - col0 ) BETWEEN NULL AND NULL;
  - name: tab2

outputs:
  - name: q3717.out
    data: |
