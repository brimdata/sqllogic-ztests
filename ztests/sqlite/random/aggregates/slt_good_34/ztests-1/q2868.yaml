script: |
  super -f text -I q2868.spq | sort > q2868.out

inputs:
  - name: q2868.spq
    data: |
      SELECT ALL - 64 FROM tab1 (FORMAT parquet) AS cor0 CROSS JOIN tab1 (FORMAT parquet) AS cor1;
  - name: tab1

outputs:
  - name: q2868.out
    data: |
      -64
      -64
      -64
      -64
      -64
      -64
      -64
      -64
      -64
