script: |
  super -f tsv -I q7431.sql | sort > q7431.out

vector: true

inputs:
  - name: q7431.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT + col1 <= + col0;
  - name: tab1

outputs:
  - name: q7431.out
    data: |
