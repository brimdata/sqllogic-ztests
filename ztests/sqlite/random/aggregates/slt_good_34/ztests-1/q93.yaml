script: |
  super -f text -I q93.spq | sort > q93.out

inputs:
  - name: q93.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT + + col1 * - col1 <> NULL;
  - name: tab1

outputs:
  - name: q93.out
    data: |
