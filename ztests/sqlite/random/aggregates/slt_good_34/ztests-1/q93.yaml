script: |
  super -f tsv -I q93.sql | sort > q93.out

vector: true

inputs:
  - name: q93.sql
    data: |
      SELECT ALL * FROM tab1 WHERE NOT + + col1 * - col1 <> NULL;
  - name: tab1

outputs:
  - name: q93.out
    data: |
