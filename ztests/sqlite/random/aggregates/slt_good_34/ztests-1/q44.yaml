script: |
  super -f text -I q44.spq | sort > q44.out

inputs:
  - name: q44.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( NOT ( ( NOT ( NULL ) < + + 77 ) ) );
  - name: tab1

outputs:
  - name: q44.out
    data: |
