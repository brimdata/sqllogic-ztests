script: |
  super -f text -I q998.spq | sort > q998.out

inputs:
  - name: q998.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NULL NOT BETWEEN ( - col1 * + ( + col2 / + col2 ) ) AND NULL;
  - name: tab1

outputs:
  - name: q998.out
    data: |
