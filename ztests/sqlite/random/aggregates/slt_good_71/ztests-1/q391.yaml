script: |
  super -f text -I q391.spq | sort > q391.out

inputs:
  - name: q391.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT 97 * - - 9 * + col2 IS NOT NULL;
  - name: tab0

outputs:
  - name: q391.out
    data: |
