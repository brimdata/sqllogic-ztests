script: |
  super -f text -I q2929.spq | sort > q2929.out

inputs:
  - name: q2929.spq
    data: |
      SELECT + col0 * - col1 + + col1 / col0 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q2929.out
    data: |
      -425
      -4277
      -714
