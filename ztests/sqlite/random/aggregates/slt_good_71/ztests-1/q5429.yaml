script: |
  super -f text -I q5429.spq | sort > q5429.out

inputs:
  - name: q5429.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT ( + 2 ) IS NOT NULL;
  - name: tab2

outputs:
  - name: q5429.out
    data: |
