script: |
  super -f text -I q287.spq | sort > q287.out

inputs:
  - name: q287.spq
    data: |
      SELECT DISTINCT ( + 31 ) / + - col1 FROM tab0 (FORMAT parquet);
  - name: tab0

outputs:
  - name: q287.out
    data: |
      -1
      -31
      0
