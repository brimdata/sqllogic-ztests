script: |
  super -f text -I q1994.spq | sort > q1994.out

inputs:
  - name: q1994.spq
    data: |
      SELECT COUNT ( * ) FROM tab2 (FORMAT parquet) AS cor0 INNER JOIN tab0 (FORMAT parquet) AS cor1 ON NOT NULL IS NOT NULL;
  - name: tab0
  - name: tab2

outputs:
  - name: q1994.out
    data: |
      9
