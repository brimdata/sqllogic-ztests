script: |
  super -f text -I q219.spq | sort > q219.out

inputs:
  - name: q219.spq
    data: |
      SELECT + col2 / col2 - ( + col1 ) FROM tab2 (FORMAT parquet);
  - name: tab2

outputs:
  - name: q219.out
    data: |
      -50
      -66
      -76
