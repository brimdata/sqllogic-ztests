script: |
  super -f tsv -I q172.sql | sort > q172.out

inputs:
  - name: q172.sql
    data: |
      SELECT + col0 FROM tab1 WHERE NOT NULL <> - col0;
  - name: tab1

outputs:
  - name: q172.out
    data: |
