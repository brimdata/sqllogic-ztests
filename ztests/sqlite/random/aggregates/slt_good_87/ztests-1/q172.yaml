script: |
  super -f text -I q172.spq | sort > q172.out

inputs:
  - name: q172.spq
    data: |
      SELECT + col0 FROM tab1 (FORMAT parquet) WHERE NOT NULL <> - col0;
  - name: tab1

outputs:
  - name: q172.out
    data: |
