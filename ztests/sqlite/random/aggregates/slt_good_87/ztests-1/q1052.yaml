script: |
  super -f text -I q1052.spq | sort > q1052.out

inputs:
  - name: q1052.spq
    data: |
      SELECT + col0 / + - col2 FROM tab1 (FORMAT parquet) AS cor0 WHERE ( + col1 IS NOT NULL );
  - name: tab1

outputs:
  - name: q1052.out
    data: |
      -1
      -1
      0
