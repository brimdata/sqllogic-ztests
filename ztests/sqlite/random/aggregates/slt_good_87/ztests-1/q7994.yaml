script: |
  super -f text -I q7994.spq | sort > q7994.out

inputs:
  - name: q7994.spq
    data: |
      SELECT DISTINCT + 64 AS col2 FROM tab0 (FORMAT parquet) WHERE ( ( NOT + col0 / col2 IS NOT NULL ) );
  - name: tab0

outputs:
  - name: q7994.out
    data: |
