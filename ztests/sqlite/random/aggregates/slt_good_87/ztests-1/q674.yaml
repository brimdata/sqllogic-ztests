script: |
  super -f text -I q674.spq | sort > q674.out

inputs:
  - name: q674.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT + col1 >= + col1 * - 47 * - - 48;
  - name: tab0

outputs:
  - name: q674.out
    data: |
