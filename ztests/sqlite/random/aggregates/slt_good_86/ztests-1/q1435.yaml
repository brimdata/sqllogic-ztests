script: |
  super -f text -I q1435.spq | sort > q1435.out

inputs:
  - name: q1435.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE + col2 - ( + col0 ) > NULL;
  - name: tab1

outputs:
  - name: q1435.out
    data: |
