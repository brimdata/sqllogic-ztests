script: |
  super -f text -I q2714.spq | sort > q2714.out

inputs:
  - name: q2714.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) cor0 WHERE NOT NULL IS NOT NULL AND NOT NULL BETWEEN col1 AND + + col0;
  - name: tab1

outputs:
  - name: q2714.out
    data: |
