script: |
  super -f text -I q6656.sql | sort > q6656.out

inputs:
  - name: q6656.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL <= + ( + - col2 ) * col1;
  - name: tab1

outputs:
  - name: q6656.out
    data: |
