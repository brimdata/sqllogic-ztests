script: |
  super -f tsv -I q6656.sql | sort > q6656.out

vector: true

inputs:
  - name: q6656.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL <= + ( + - col2 ) * col1;
  - name: tab1

outputs:
  - name: q6656.out
    data: |
