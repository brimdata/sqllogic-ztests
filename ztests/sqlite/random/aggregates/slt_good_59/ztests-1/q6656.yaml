script: |
  super -f text -I q6656.spq | sort > q6656.out

inputs:
  - name: q6656.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL <= + ( + - col2 ) * col1;
  - name: tab1

outputs:
  - name: q6656.out
    data: |
