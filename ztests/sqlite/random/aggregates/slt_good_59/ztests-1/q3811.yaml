script: |
  super -f text -I q3811.spq | sort > q3811.out

inputs:
  - name: q3811.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT NULL NOT IN ( - ( ( + + col0 ) ) * - col0 / col1 * - + 3, col1 * + 7 );
  - name: tab1

outputs:
  - name: q3811.out
    data: |
