script: |
  super -f text -I q2989.spq | sort > q2989.out

inputs:
  - name: q2989.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) AS cor0 WHERE + ( col1 ) + + ( - 60 ) > 31;
  - name: tab2

outputs:
  - name: q2989.out
    data: |
