script: |
  super -f text -I q565.sql | sort > q565.out

inputs:
  - name: q565.sql
    data: |
      SELECT + ( + col2 ) FROM tab1 WHERE NOT NULL >= + col0;
  - name: tab1

outputs:
  - name: q565.out
    data: |
