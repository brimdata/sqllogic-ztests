script: |
  super -f text -I q565.spq | sort > q565.out

inputs:
  - name: q565.spq
    data: |
      SELECT + ( + col2 ) FROM tab1 (FORMAT parquet) WHERE NOT NULL >= + col0;
  - name: tab1

outputs:
  - name: q565.out
    data: |
