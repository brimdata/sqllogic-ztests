script: |
  super -f text -I q455.spq | sort > q455.out

inputs:
  - name: q455.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT + col2 NOT IN ( - col0 * - - col2 - - CAST ( + 20 AS INTEGER ) );
  - name: tab2

outputs:
  - name: q455.out
    data: |
