script: |
  super -f text -I q2046.spq | sort > q2046.out

inputs:
  - name: q2046.spq
    data: |
      SELECT DISTINCT 20 FROM tab1 (FORMAT parquet) AS cor0 CROSS JOIN tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q2046.out
    data: |
      20
