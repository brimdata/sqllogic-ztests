script: |
  super -f text -I q10728.spq | sort > q10728.out

inputs:
  - name: q10728.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT ( + col1 ) + - col1 + + col1 IS NOT NULL;
  - name: tab1

outputs:
  - name: q10728.out
    data: |
