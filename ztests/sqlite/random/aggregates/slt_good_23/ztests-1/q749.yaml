script: |
  super -f text -I q749.spq | sort > q749.out

inputs:
  - name: q749.spq
    data: |
      SELECT 2 + ( COUNT ( * ) ) * COUNT ( * ) FROM tab0 (FORMAT parquet) AS cor0;
  - name: tab0

outputs:
  - name: q749.out
    data: |
      11
