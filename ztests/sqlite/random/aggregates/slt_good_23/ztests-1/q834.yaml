script: |
  super -f text -I q834.spq | sort > q834.out

inputs:
  - name: q834.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT - col1 * - + CAST ( NULL AS REAL ) * + CAST ( NULL AS REAL ) > + + col0;
  - name: tab0

outputs:
  - name: q834.out
    data: |
