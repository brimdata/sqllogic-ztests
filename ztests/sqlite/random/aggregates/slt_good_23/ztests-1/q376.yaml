script: |
  super -f text -I q376.spq | sort > q376.out

inputs:
  - name: q376.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT ( NOT col1 / 84 IS NULL );
  - name: tab1

outputs:
  - name: q376.out
    data: |
