script: |
  super -f tsv -I q509.sql | sort > q509.out

vector: true

inputs:
  - name: q509.sql
    data: |
      SELECT * FROM tab2 WHERE + ( + + col0 ) <= + ( + col0 ) / - col2;
  - name: tab2

outputs:
  - name: q509.out
    data: |
