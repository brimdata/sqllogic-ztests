script: |
  super -f text -I q509.spq | sort > q509.out

inputs:
  - name: q509.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE + ( + + col0 ) <= + ( + col0 ) / - col2;
  - name: tab2

outputs:
  - name: q509.out
    data: |
