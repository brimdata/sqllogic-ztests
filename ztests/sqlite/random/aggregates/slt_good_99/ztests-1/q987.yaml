script: |
  super -f tsv -I q987.sql | sort > q987.out

vector: true

inputs:
  - name: q987.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL BETWEEN ( + ( + 14 ) ) AND NULL;
  - name: tab1

outputs:
  - name: q987.out
    data: |
