script: |
  super -f tsv -I q735.sql | sort > q735.out

vector: true

inputs:
  - name: q735.sql
    data: |
      SELECT - ( + col0 ) FROM tab0 WHERE NOT NULL >= - 89 * + ( ( + col1 ) );
  - name: tab0

outputs:
  - name: q735.out
    data: |
