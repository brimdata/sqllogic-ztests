script: |
  super -f text -I q735.spq | sort > q735.out

inputs:
  - name: q735.spq
    data: |
      SELECT - ( + col0 ) FROM tab0 (FORMAT parquet) WHERE NOT NULL >= - 89 * + ( ( + col1 ) );
  - name: tab0

outputs:
  - name: q735.out
    data: |
