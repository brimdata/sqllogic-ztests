script: |
  super -f text -I q6934.spq | sort > q6934.out

inputs:
  - name: q6934.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT ( + col0 / - col1 ) NOT BETWEEN NULL AND NULL;
  - name: tab1

outputs:
  - name: q6934.out
    data: |
