script: |
  super -f text -I q360.spq | sort > q360.out

inputs:
  - name: q360.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + ( + col2 ) >= - - col0;
  - name: tab1

outputs:
  - name: q360.out
    data: |
      85	5	59
      91	47	68
