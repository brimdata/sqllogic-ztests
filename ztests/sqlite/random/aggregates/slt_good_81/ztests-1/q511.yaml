script: |
  super -f text -I q511.spq | sort > q511.out

inputs:
  - name: q511.spq
    data: |
      SELECT - col1 + - col0 FROM tab2 (FORMAT parquet) WHERE NOT + col0 NOT IN ( col0 * ( - 61 ) + - 40 );
  - name: tab2

outputs:
  - name: q511.out
    data: |
