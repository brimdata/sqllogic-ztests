script: |
  super -f text -I q471.spq | sort > q471.out

inputs:
  - name: q471.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT + 41 * - + col2 + - col2 IS NOT NULL;
  - name: tab0

outputs:
  - name: q471.out
    data: |
