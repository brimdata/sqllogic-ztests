script: |
  super -f text -I q142.spq | sort > q142.out

inputs:
  - name: q142.spq
    data: |
      SELECT DISTINCT - - 18 * - + SUM ( + col2 ) FROM tab1 (FORMAT parquet) WHERE - - col0 IS NOT NULL;
  - name: tab1

outputs:
  - name: q142.out
    data: |
      -4014
