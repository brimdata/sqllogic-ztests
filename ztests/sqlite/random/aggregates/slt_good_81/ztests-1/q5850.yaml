script: |
  super -f text -I q5850.spq | sort > q5850.out

inputs:
  - name: q5850.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT NULL IS NOT NULL AND NOT ( + - col0 - - - col2 * 57 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q5850.out
    data: |
