script: |
  super -f tsv -I q656.sql | sort > q656.out

vector: true

inputs:
  - name: q656.sql
    data: |
      SELECT ALL - - COUNT ( * ) FROM tab1 WHERE NOT + ( - + col0 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q656.out
    data: |
      0
