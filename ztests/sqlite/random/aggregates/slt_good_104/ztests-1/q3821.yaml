script: |
  super -f tsv -I q3821.sql | sort > q3821.out

vector: true

inputs:
  - name: q3821.sql
    data: |
      SELECT * FROM tab1 WHERE NOT ( NOT + col2 + + ( - col0 ) IN ( + - col1, + col1 ) );
  - name: tab1

outputs:
  - name: q3821.out
    data: |
