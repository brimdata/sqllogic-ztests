script: |
  super -f text -I q3821.spq | sort > q3821.out

inputs:
  - name: q3821.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( NOT + col2 + + ( - col0 ) IN ( + - col1, + col1 ) );
  - name: tab1

outputs:
  - name: q3821.out
    data: |
