script: |
  super -f text -I q50.spq | sort > q50.out

inputs:
  - name: q50.spq
    data: |
      SELECT DISTINCT 83 FROM tab1 (FORMAT parquet) WHERE NOT NULL <> col2 * - + col0 + - + col1 / + + col1;
  - name: tab1

outputs:
  - name: q50.out
    data: |
