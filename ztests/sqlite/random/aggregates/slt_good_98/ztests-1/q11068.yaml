script: |
  super -f text -I q11068.spq | sort > q11068.out

inputs:
  - name: q11068.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT col1 + col2 * - col1 IS NOT NULL;
  - name: tab1

outputs:
  - name: q11068.out
    data: |
