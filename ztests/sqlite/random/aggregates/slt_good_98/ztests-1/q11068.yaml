script: |
  super -f tsv -I q11068.sql | sort > q11068.out

vector: true

inputs:
  - name: q11068.sql
    data: |
      SELECT ALL * FROM tab1 WHERE NOT col1 + col2 * - col1 IS NOT NULL;
  - name: tab1

outputs:
  - name: q11068.out
    data: |
