script: |
  super -f text -I q834.spq | sort > q834.out

inputs:
  - name: q834.spq
    data: |
      SELECT 85 / + - col1 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q834.out
    data: |
      -1
      -17
      -6
