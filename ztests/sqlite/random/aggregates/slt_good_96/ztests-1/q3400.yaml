script: |
  super -f tsv -I q3400.sql | sort > q3400.out

vector: true

inputs:
  - name: q3400.sql
    data: |
      SELECT * FROM tab1 WHERE NOT col2 / + col0 NOT IN ( - col2 * ( + col2 ) );
  - name: tab1

outputs:
  - name: q3400.out
    data: |
