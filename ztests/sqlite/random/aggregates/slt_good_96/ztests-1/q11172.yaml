script: |
  super -f tsv -I q11172.sql | sort > q11172.out

vector: true

inputs:
  - name: q11172.sql
    data: |
      SELECT * FROM tab2 WHERE ( + col1 ) IS NULL;
  - name: tab2

outputs:
  - name: q11172.out
    data: |
