script: |
  super -f text -I q5614.spq | sort > q5614.out

inputs:
  - name: q5614.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT + ( col0 ) + - col0 IS NOT NULL;
  - name: tab0

outputs:
  - name: q5614.out
    data: |
