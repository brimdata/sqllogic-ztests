script: |
  super -f text -I q744.spq | sort > q744.out

inputs:
  - name: q744.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT - + col0 * - col1 BETWEEN col1 AND NULL;
  - name: tab1

outputs:
  - name: q744.out
    data: |
