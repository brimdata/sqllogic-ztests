script: |
  super -f tsv -I q997.sql | sort > q997.out

vector: true

inputs:
  - name: q997.sql
    data: |
      SELECT * FROM tab2 WHERE NOT ( ( NULL ) NOT IN ( - - col1, - col1, - - col2, + col2 ) );
  - name: tab2

outputs:
  - name: q997.out
    data: |
