script: |
  super -f text -I q1300.spq | sort > q1300.out

inputs:
  - name: q1300.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT col0 * + col0 IS NOT NULL;
  - name: tab2

outputs:
  - name: q1300.out
    data: |
