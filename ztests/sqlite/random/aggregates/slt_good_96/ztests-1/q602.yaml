script: |
  super -f text -I q602.spq | sort > q602.out

inputs:
  - name: q602.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) AS cor0 WHERE NULL IN ( - col2 / CAST ( NULL AS REAL ) + + col0 );
  - name: tab1

outputs:
  - name: q602.out
    data: |
