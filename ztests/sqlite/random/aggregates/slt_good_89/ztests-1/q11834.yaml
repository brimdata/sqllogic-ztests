script: |
  super -f text -I q11834.spq | sort > q11834.out

inputs:
  - name: q11834.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT col0 * - col1 IS NOT NULL AND - ( - + col0 ) IS NOT NULL;
  - name: tab0

outputs:
  - name: q11834.out
    data: |
