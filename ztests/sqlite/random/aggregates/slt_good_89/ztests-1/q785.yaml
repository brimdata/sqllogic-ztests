script: |
  super -f tsv -I q785.sql | sort > q785.out

vector: true

inputs:
  - name: q785.sql
    data: |
      SELECT * FROM tab2 WHERE NOT NULL BETWEEN + col2 * col1 * 18 * - 74 AND - 41;
  - name: tab2

outputs:
  - name: q785.out
    data: |
