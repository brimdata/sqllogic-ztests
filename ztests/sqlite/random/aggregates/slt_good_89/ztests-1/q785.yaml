script: |
  super -f text -I q785.spq | sort > q785.out

inputs:
  - name: q785.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT NULL BETWEEN + col2 * col1 * 18 * - 74 AND - 41;
  - name: tab2

outputs:
  - name: q785.out
    data: |
