script: |
  super -f tsv -I q9800.sql | sort > q9800.out

vector: true

inputs:
  - name: q9800.sql
    data: |
      SELECT * FROM tab1 WHERE NOT + col0 + - col1 IS NOT NULL;
  - name: tab1

outputs:
  - name: q9800.out
    data: |
