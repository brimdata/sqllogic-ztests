script: |
  super -f text -I q6107.spq | sort > q6107.out

inputs:
  - name: q6107.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT + col1 * ( + col2 ) + + + ( - 48 ) + + col1 IS NOT NULL;
  - name: tab2

outputs:
  - name: q6107.out
    data: |
