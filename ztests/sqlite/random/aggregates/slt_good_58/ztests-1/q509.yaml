script: |
  super -f text -I q509.spq | sort > q509.out

inputs:
  - name: q509.spq
    data: |
      SELECT + col1 * - col1 FROM tab0 (FORMAT parquet) WHERE NOT ( - 18 ) IS NULL;
  - name: tab0

outputs:
  - name: q509.out
    data: |
      -1
      -441
      -6561
