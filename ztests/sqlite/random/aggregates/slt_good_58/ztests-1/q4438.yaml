script: |
  super -f text -I q4438.spq | sort > q4438.out

inputs:
  - name: q4438.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT + col2 + col2 * col1 IS NOT NULL;
  - name: tab2

outputs:
  - name: q4438.out
    data: |
