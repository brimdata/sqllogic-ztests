script: |
  super -f text -I q301.spq | sort > q301.out

inputs:
  - name: q301.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) AS cor0 WHERE NOT col1 BETWEEN + - 80 AND - col1 * - 30 + col2 + col0;
  - name: tab2

outputs:
  - name: q301.out
    data: |
