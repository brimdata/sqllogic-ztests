script: |
  super -f tsv -I q3804.sql | sort > q3804.out

vector: true

inputs:
  - name: q3804.sql
    data: |
      SELECT DISTINCT * FROM tab2 WHERE col0 / col1 IS NULL;
  - name: tab2

outputs:
  - name: q3804.out
    data: |
