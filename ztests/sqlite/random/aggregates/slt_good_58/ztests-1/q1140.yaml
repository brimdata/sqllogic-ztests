script: |
  super -f text -I q1140.spq | sort > q1140.out

inputs:
  - name: q1140.spq
    data: |
      SELECT DISTINCT + COUNT ( * ) FROM tab0 (FORMAT parquet) WHERE col1 NOT IN ( + col2 - col1 );
  - name: tab0

outputs:
  - name: q1140.out
    data: |
      3
