script: |
  super -f text -I q908.spq | sort > q908.out

inputs:
  - name: q908.spq
    data: |
      SELECT DISTINCT 35, col1 AS col1 FROM tab1 (FORMAT parquet) WHERE NOT ( col2 * - col2 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q908.out
    data: |
