script: |
  super -f tsv -I q908.sql | sort > q908.out

vector: true

inputs:
  - name: q908.sql
    data: |
      SELECT DISTINCT 35, col1 AS col1 FROM tab1 WHERE NOT ( col2 * - col2 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q908.out
    data: |
