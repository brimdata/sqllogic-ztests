script: |
  super -f text -I q640.spq | sort > q640.out

inputs:
  - name: q640.spq
    data: |
      SELECT SUM ( - col1 ) FROM tab2 (FORMAT parquet);
  - name: tab2

outputs:
  - name: q640.out
    data: |
      -195
