script: |
  super -f text -I q844.spq | sort > q844.out

inputs:
  - name: q844.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL = + + CAST ( + - col2 AS INTEGER );
  - name: tab1

outputs:
  - name: q844.out
    data: |
