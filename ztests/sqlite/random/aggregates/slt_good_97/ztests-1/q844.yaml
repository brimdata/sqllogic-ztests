script: |
  super -f tsv -I q844.sql | sort > q844.out

inputs:
  - name: q844.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL = + + CAST ( + - col2 AS INTEGER );
  - name: tab1

outputs:
  - name: q844.out
    data: |
