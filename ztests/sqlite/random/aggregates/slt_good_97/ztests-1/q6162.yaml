script: |
  super -f text -I q6162.spq | sort > q6162.out

inputs:
  - name: q6162.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT ( + - col1 * - col0 ) >= + col1;
  - name: tab0

outputs:
  - name: q6162.out
    data: |
