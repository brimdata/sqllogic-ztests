script: |
  super -f text -I q739.spq | sort > q739.out

inputs:
  - name: q739.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE + 80 >= NULL;
  - name: tab2

outputs:
  - name: q739.out
    data: |
