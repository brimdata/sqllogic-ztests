script: |
  super -f text -I q2989.spq | sort > q2989.out

inputs:
  - name: q2989.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT NULL BETWEEN col2 AND - 72 + - col1 + + col2;
  - name: tab1

outputs:
  - name: q2989.out
    data: |
