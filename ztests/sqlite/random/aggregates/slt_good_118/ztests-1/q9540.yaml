script: |
  super -f text -I q9540.spq | sort > q9540.out

inputs:
  - name: q9540.spq
    data: |
      SELECT DISTINCT SUM ( DISTINCT + 80 ) FROM tab0 (FORMAT parquet);
  - name: tab0

outputs:
  - name: q9540.out
    data: |
      80
