script: |
  super -f text -I q886.spq | sort > q886.out

inputs:
  - name: q886.spq
    data: |
      SELECT DISTINCT + + col1 FROM tab0 (FORMAT parquet) WHERE NOT NULL < + col0;
  - name: tab0

outputs:
  - name: q886.out
    data: |
