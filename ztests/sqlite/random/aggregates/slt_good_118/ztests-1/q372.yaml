script: |
  super -f text -I q372.spq | sort > q372.out

inputs:
  - name: q372.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT NULL < + col2 / - + ( col1 ) + - col2 + - 52;
  - name: tab0

outputs:
  - name: q372.out
    data: |
