script: |
  super -f text -I q664.spq | sort > q664.out

inputs:
  - name: q664.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT + 65 IS NOT NULL;
  - name: tab2

outputs:
  - name: q664.out
    data: |
