script: |
  super -f tsv -I q653.sql | sort > q653.out

vector: true

inputs:
  - name: q653.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT + col0 BETWEEN + col1 AND NULL;
  - name: tab1

outputs:
  - name: q653.out
    data: |
