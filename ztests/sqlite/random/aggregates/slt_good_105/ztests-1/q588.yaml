script: |
  super -f tsv -I q588.sql | sort > q588.out

vector: true

inputs:
  - name: q588.sql
    data: |
      SELECT + 20 + SUM ( ALL - col2 ) FROM tab2 WHERE NOT col0 = ( NULL );
  - name: tab2

outputs:
  - name: q588.out
    data: |
      NULL
