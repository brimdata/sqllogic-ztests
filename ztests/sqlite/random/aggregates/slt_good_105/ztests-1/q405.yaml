script: |
  super -f text -I q405.sql | sort > q405.out

inputs:
  - name: q405.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT NULL <= + col2;
  - name: tab1

outputs:
  - name: q405.out
    data: |
