script: |
  super -f tsv -I q405.sql | sort > q405.out

vector: true

inputs:
  - name: q405.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT NULL <= + col2;
  - name: tab1

outputs:
  - name: q405.out
    data: |
