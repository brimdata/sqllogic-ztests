script: |
  super -f text -I q438.spq | sort > q438.out

inputs:
  - name: q438.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) cor0 WHERE col1 > ( NULL );
  - name: tab1

outputs:
  - name: q438.out
    data: |
