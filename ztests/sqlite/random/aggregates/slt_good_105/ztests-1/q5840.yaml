script: |
  super -f text -I q5840.spq | sort > q5840.out

inputs:
  - name: q5840.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT ( NOT NULL BETWEEN NULL AND col0 + + CAST ( NULL AS INTEGER ) );
  - name: tab1

outputs:
  - name: q5840.out
    data: |
