script: |
  super -f tsv -I q802.sql | sort > q802.out

vector: true

inputs:
  - name: q802.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE NOT NULL > + 15;
  - name: tab1

outputs:
  - name: q802.out
    data: |
