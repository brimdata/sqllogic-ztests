script: |
  super -f text -I q195.spq | sort > q195.out

inputs:
  - name: q195.spq
    data: |
      SELECT + col1 + + - col1 * - 1 * - col0, - col0 AS col0 FROM tab2 (FORMAT parquet) AS cor0 WHERE NOT NULL = col0;
  - name: tab2

outputs:
  - name: q195.out
    data: |
