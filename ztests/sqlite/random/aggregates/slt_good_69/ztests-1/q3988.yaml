script: |
  super -f text -I q3988.spq | sort > q3988.out

inputs:
  - name: q3988.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( col1, - 54 * col1 * - col1 );
  - name: tab1

outputs:
  - name: q3988.out
    data: |
