script: |
  super -f tsv -I q3988.sql | sort > q3988.out

vector: true

inputs:
  - name: q3988.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL NOT IN ( col1, - 54 * col1 * - col1 );
  - name: tab1

outputs:
  - name: q3988.out
    data: |
