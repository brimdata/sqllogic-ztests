script: |
  super -f text -I q6985.spq | sort > q6985.out

inputs:
  - name: q6985.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) cor0 WHERE NOT + col0 + col0 * - 16 < + col0 + + CAST ( NULL AS INTEGER );
  - name: tab1

outputs:
  - name: q6985.out
    data: |
