script: |
  super -f tsv -I q5874.sql | sort > q5874.out

vector: true

inputs:
  - name: q5874.sql
    data: |
      SELECT DISTINCT * FROM tab2 WHERE NOT ( - col0 * - ( + col2 ) ) < NULL;
  - name: tab2

outputs:
  - name: q5874.out
    data: |
