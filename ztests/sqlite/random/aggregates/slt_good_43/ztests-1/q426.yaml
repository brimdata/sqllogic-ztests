script: |
  super -f text -I q426.spq | sort > q426.out

inputs:
  - name: q426.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT ( + col1 * col1 ) <= NULL;
  - name: tab0

outputs:
  - name: q426.out
    data: |
