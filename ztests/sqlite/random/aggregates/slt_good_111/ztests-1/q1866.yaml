script: |
  super -f text -I q1866.spq | sort > q1866.out

inputs:
  - name: q1866.spq
    data: |
      SELECT ( + + col0 ) / - col1 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q1866.out
    data: |
      -1
      -17
      -3
