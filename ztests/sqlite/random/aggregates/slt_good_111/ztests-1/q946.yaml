script: |
  super -f text -I q946.spq | sort > q946.out

inputs:
  - name: q946.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE + col2 * - col1 NOT BETWEEN ( NULL ) AND ( NULL );
  - name: tab1

outputs:
  - name: q946.out
    data: |
