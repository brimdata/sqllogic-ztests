script: |
  super -f tsv -I q6058.sql | sort > q6058.out

vector: true

inputs:
  - name: q6058.sql
    data: |
      SELECT DISTINCT + 44 FROM tab1 WHERE NOT 17 + col1 NOT IN ( + ( ( + col1 ) ) );
  - name: tab1

outputs:
  - name: q6058.out
    data: |
