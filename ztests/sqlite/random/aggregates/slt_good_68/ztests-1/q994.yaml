script: |
  super -f tsv -I q994.sql | sort > q994.out

vector: true

inputs:
  - name: q994.sql
    data: |
      SELECT * FROM tab1 WHERE NOT + + col0 / col1 NOT IN ( + col1 );
  - name: tab1

outputs:
  - name: q994.out
    data: |
