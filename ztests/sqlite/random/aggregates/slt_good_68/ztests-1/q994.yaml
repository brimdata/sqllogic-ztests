script: |
  super -f text -I q994.spq | sort > q994.out

inputs:
  - name: q994.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + + col0 / col1 NOT IN ( + col1 );
  - name: tab1

outputs:
  - name: q994.out
    data: |
