script: |
  super -f text -I q5202.spq | sort > q5202.out

inputs:
  - name: q5202.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT - col1 - - - col1 + - - ( + 4 ) IS NOT NULL;
  - name: tab1

outputs:
  - name: q5202.out
    data: |
