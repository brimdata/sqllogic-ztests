script: |
  super -f tsv -I q4140.sql | sort > q4140.out

vector: true

inputs:
  - name: q4140.sql
    data: |
      SELECT * FROM tab1 WHERE ( NOT NULL BETWEEN col1 AND + - col2 );
  - name: tab1

outputs:
  - name: q4140.out
    data: |
