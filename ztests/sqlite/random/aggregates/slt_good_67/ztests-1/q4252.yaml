script: |
  super -f text -I q4252.spq | sort > q4252.out

inputs:
  - name: q4252.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) cor0 WHERE NOT NULL = col1 * - ( + CAST ( NULL AS INTEGER ) );
  - name: tab1

outputs:
  - name: q4252.out
    data: |
