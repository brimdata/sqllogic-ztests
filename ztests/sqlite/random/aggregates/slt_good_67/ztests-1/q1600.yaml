script: |
  super -f text -I q1600.spq | sort > q1600.out

inputs:
  - name: q1600.spq
    data: |
      SELECT DISTINCT + col0 FROM tab1 (FORMAT parquet) WHERE NOT NULL <= ( + col0 );
  - name: tab1

outputs:
  - name: q1600.out
    data: |
