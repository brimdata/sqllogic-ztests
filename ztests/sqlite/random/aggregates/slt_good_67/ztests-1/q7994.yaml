script: |
  super -f text -I q7994.spq | sort > q7994.out

inputs:
  - name: q7994.spq
    data: |
      SELECT DISTINCT + col0 * + 15 FROM tab2 (FORMAT parquet) WHERE NOT NULL >= - ( col1 );
  - name: tab2

outputs:
  - name: q7994.out
    data: |
