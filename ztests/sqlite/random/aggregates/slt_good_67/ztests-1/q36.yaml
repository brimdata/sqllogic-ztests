script: |
  super -f text -I q36.spq | sort > q36.out

inputs:
  - name: q36.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT ( + col2 * 60 ) = ( col2 * + CAST ( NULL AS INTEGER ) + col1 * - col1 );
  - name: tab1

outputs:
  - name: q36.out
    data: |
