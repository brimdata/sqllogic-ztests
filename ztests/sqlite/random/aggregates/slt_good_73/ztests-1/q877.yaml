script: |
  super -f text -I q877.spq | sort > q877.out

inputs:
  - name: q877.spq
    data: |
      SELECT + col2 * - col0 FROM tab2 (FORMAT parquet) WHERE NOT + col0 > NULL;
  - name: tab2

outputs:
  - name: q877.out
    data: |
