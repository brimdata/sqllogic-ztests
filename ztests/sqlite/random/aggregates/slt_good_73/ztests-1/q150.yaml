script: |
  super -f text -I q150.spq | sort > q150.out

inputs:
  - name: q150.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT + col1 * - col2 IS NOT NULL;
  - name: tab1

outputs:
  - name: q150.out
    data: |
