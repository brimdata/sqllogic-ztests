script: |
  super -f text -I q3900.spq | sort > q3900.out

inputs:
  - name: q3900.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT ( + col0 * - - col1 * - col0 - col1 ) <> col1;
  - name: tab0

outputs:
  - name: q3900.out
    data: |
