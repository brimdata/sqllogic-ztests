script: |
  super -f tsv -I q3900.sql | sort > q3900.out

vector: true

inputs:
  - name: q3900.sql
    data: |
      SELECT * FROM tab0 WHERE NOT ( + col0 * - - col1 * - col0 - col1 ) <> col1;
  - name: tab0

outputs:
  - name: q3900.out
    data: |
