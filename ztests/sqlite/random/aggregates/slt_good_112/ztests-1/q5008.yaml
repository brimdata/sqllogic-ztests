script: |
  super -f tsv -I q5008.sql | sort > q5008.out

vector: true

inputs:
  - name: q5008.sql
    data: |
      SELECT DISTINCT * FROM tab0 WHERE NOT NULL NOT IN ( + 1 * - ( - col0 * - ( - ( ( + col0 ) ) ) ) );
  - name: tab0

outputs:
  - name: q5008.out
    data: |
