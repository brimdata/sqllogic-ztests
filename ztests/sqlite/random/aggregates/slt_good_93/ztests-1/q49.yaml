script: |
  super -f text -I q49.spq | sort > q49.out

inputs:
  - name: q49.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT + col2 * - col2 < ( + + 16 * col1 );
  - name: tab0

outputs:
  - name: q49.out
    data: |
