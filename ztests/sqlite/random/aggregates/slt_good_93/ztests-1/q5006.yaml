script: |
  super -f text -I q5006.spq | sort > q5006.out

inputs:
  - name: q5006.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT + col2 NOT IN ( + col0 + ( col0 ) );
  - name: tab2

outputs:
  - name: q5006.out
    data: |
