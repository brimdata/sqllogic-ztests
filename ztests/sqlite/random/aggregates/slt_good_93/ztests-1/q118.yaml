script: |
  super -f text -I q118.spq | sort > q118.out

inputs:
  - name: q118.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT NULL BETWEEN - col0 / - - col0 AND - col0 / - CAST ( NULL AS INTEGER );
  - name: tab1

outputs:
  - name: q118.out
    data: |
