script: |
  super -f text -I q298.spq | sort > q298.out

inputs:
  - name: q298.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) AS cor0 CROSS JOIN tab1 (FORMAT parquet) AS cor1 WHERE NULL NOT BETWEEN NULL AND ( NULL );
  - name: tab1
  - name: tab2

outputs:
  - name: q298.out
    data: |
