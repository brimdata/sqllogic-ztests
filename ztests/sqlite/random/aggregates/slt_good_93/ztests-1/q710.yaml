script: |
  super -f text -I q710.spq | sort > q710.out

inputs:
  - name: q710.spq
    data: |
      SELECT DISTINCT - 15 FROM tab1 (FORMAT parquet) WHERE NOT col2 * + col0 + + col2 BETWEEN + col2 + col1 AND ( NULL );
  - name: tab1

outputs:
  - name: q710.out
    data: |
