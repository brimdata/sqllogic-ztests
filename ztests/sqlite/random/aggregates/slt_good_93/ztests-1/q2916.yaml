script: |
  super -f text -I q2916.spq | sort > q2916.out

inputs:
  - name: q2916.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE ( NOT + ( + col2 ) IS NOT NULL );
  - name: tab0

outputs:
  - name: q2916.out
    data: |
