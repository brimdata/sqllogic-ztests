script: |
  super -f text -I q733.spq | sort > q733.out

inputs:
  - name: q733.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT ( + 48 ) NOT IN ( + col2 );
  - name: tab2

outputs:
  - name: q733.out
    data: |
