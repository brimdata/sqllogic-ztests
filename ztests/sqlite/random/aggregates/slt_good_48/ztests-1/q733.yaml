script: |
  super -f tsv -I q733.sql | sort > q733.out

vector: true

inputs:
  - name: q733.sql
    data: |
      SELECT * FROM tab2 WHERE NOT ( + 48 ) NOT IN ( + col2 );
  - name: tab2

outputs:
  - name: q733.out
    data: |
