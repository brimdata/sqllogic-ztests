script: |
  super -f text -I q847.spq | sort > q847.out

inputs:
  - name: q847.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE + col2 * ( + - ( + - col2 ) ) + - - col2 IS NULL;
  - name: tab1

outputs:
  - name: q847.out
    data: |
