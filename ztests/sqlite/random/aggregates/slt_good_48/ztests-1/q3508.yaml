script: |
  super -f text -I q3508.spq | sort > q3508.out

inputs:
  - name: q3508.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT ( NOT + ( col2 ) NOT IN ( + col2 ) );
  - name: tab2

outputs:
  - name: q3508.out
    data: |
