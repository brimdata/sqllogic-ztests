script: |
  super -f tsv -I q3508.sql | sort > q3508.out

vector: true

inputs:
  - name: q3508.sql
    data: |
      SELECT * FROM tab2 WHERE NOT ( NOT + ( col2 ) NOT IN ( + col2 ) );
  - name: tab2

outputs:
  - name: q3508.out
    data: |
