script: |
  super -f text -I q118.spq | sort > q118.out

inputs:
  - name: q118.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT + ( + col1 ) * - - col0 BETWEEN + 31 AND NULL;
  - name: tab0

outputs:
  - name: q118.out
    data: |
