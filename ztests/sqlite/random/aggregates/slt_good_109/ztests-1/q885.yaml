script: |
  super -f text -I q885.spq | sort > q885.out

inputs:
  - name: q885.spq
    data: |
      SELECT 60 FROM tab2 (FORMAT parquet) WHERE NOT 2 + ( col1 ) * + col1 BETWEEN ( NULL ) AND - col1;
  - name: tab2

outputs:
  - name: q885.out
    data: |
      60
      60
      60
