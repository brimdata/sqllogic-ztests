script: |
  super -f text -I q908.spq | sort > q908.out

inputs:
  - name: q908.spq
    data: |
      SELECT ALL - col1 + col1 AS col0 FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( + col1 );
  - name: tab1

outputs:
  - name: q908.out
    data: |
