script: |
  super -f text -I q9023.spq | sort > q9023.out

inputs:
  - name: q9023.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT + + col1 * - col2 * 10 * col1 IS NOT NULL;
  - name: tab1

outputs:
  - name: q9023.out
    data: |
