script: |
  super -f text -I q656.sql | sort > q656.out

inputs:
  - name: q656.sql
    data: |
      SELECT DISTINCT MIN ( + col2 ) col2 FROM tab0;
  - name: tab0

outputs:
  - name: q656.out
    data: |
      10
