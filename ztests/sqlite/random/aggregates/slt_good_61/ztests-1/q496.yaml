script: |
  super -f tsv -I q496.sql | sort > q496.out

vector: true

inputs:
  - name: q496.sql
    data: |
      SELECT DISTINCT + MAX ( ALL + CAST ( NULL AS INTEGER ) ) / MAX ( + col2 ) FROM tab1;
  - name: tab1

outputs:
  - name: q496.out
    data: |
      NULL
