script: |
  super -f text -I q3651.spq | sort > q3651.out

inputs:
  - name: q3651.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 INNER JOIN tab0 (FORMAT parquet) cor1 ON NOT + + CAST ( + 52 AS INTEGER ) IS NOT NULL WHERE NULL IS NOT NULL;
  - name: tab0
  - name: tab1

outputs:
  - name: q3651.out
    data: |
