script: |
  super -f text -I q1856.spq | sort > q1856.out

inputs:
  - name: q1856.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT ( + CAST ( + col2 AS INTEGER ) * ( 1 ) ) IS NOT NULL;
  - name: tab2

outputs:
  - name: q1856.out
    data: |
