script: |
  super -f tsv -I q462.sql | sort > q462.out

vector: true

inputs:
  - name: q462.sql
    data: |
      SELECT * FROM tab1 WHERE NOT + col2 NOT IN ( + col1 );
  - name: tab1

outputs:
  - name: q462.out
    data: |
