script: |
  super -f text -I q191.spq | sort > q191.out

inputs:
  - name: q191.spq
    data: |
      SELECT DISTINCT + SUM ( + - col1 ) * + 20 AS col0 FROM tab2 (FORMAT parquet);
  - name: tab2

outputs:
  - name: q191.out
    data: |
      -3900
