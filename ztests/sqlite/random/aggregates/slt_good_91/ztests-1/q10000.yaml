script: |
  super -f text -I q10000.spq | sort > q10000.out

inputs:
  - name: q10000.spq
    data: |
      SELECT + col1 FROM tab1 (FORMAT parquet) WHERE NOT + col0 = col0;
  - name: tab1

outputs:
  - name: q10000.out
    data: |
