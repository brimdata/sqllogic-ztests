script: |
  super -f tsv -I q44.sql | sort > q44.out

vector: true

inputs:
  - name: q44.sql
    data: |
      SELECT * FROM tab1 WHERE + - col0 >= + col2 * col0;
  - name: tab1

outputs:
  - name: q44.out
    data: |
