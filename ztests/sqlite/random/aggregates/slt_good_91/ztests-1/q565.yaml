script: |
  super -f text -I q565.spq | sort > q565.out

inputs:
  - name: q565.spq
    data: |
      SELECT + col1 * ( + col2 ) + 32 * - 80 FROM tab2 (FORMAT parquet);
  - name: tab2

outputs:
  - name: q565.out
    data: |
      -1387
      1326
      520
