script: |
  super -f text -I q930.spq | sort > q930.out

inputs:
  - name: q930.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT col0 + - col1 + col1 IS NOT NULL;
  - name: tab1

outputs:
  - name: q930.out
    data: |
