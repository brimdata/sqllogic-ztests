script: |
  super -f text -I q3027.spq | sort > q3027.out

inputs:
  - name: q3027.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT + col1 IS NOT NULL AND NOT ( + - col0 ) IS NOT NULL;
  - name: tab2

outputs:
  - name: q3027.out
    data: |
