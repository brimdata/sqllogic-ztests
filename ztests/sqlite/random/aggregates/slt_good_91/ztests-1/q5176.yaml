script: |
  super -f tsv -I q5176.sql | sort > q5176.out

vector: true

inputs:
  - name: q5176.sql
    data: |
      SELECT * FROM tab1 WHERE NOT col0 * - 2 NOT IN ( - col1 / + ( col0 ) );
  - name: tab1

outputs:
  - name: q5176.out
    data: |
