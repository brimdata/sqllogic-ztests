script: |
  super -f text -I q93.spq | sort > q93.out

inputs:
  - name: q93.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) AS cor0 WHERE NOT - col1 NOT BETWEEN ( + col0 ) AND col2;
  - name: tab1

outputs:
  - name: q93.out
    data: |
