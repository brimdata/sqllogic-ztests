script: |
  super -f text -I q1767.spq | sort > q1767.out

inputs:
  - name: q1767.spq
    data: |
      SELECT DISTINCT 14 FROM tab2 (FORMAT parquet) WHERE NOT + col1 IS NOT NULL;
  - name: tab2

outputs:
  - name: q1767.out
    data: |
