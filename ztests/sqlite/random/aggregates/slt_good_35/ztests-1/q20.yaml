script: |
  super -f text -I q20.spq | sort > q20.out

inputs:
  - name: q20.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) AS cor0 WHERE ( + col2 ) <= + col0 * CAST ( NULL AS INTEGER );
  - name: tab1

outputs:
  - name: q20.out
    data: |
