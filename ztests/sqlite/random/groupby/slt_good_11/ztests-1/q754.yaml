script: |
  super -f tsv -I q754.sql | sort > q754.out

vector: true

inputs:
  - name: q754.sql
    data: |
      SELECT col0 FROM tab1 GROUP BY col0, col0 HAVING NOT NULL > NULL;
  - name: tab1

outputs:
  - name: q754.out
    data: |
