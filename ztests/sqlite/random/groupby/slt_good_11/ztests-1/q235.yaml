script: |
  super -f tsv -I q235.sql | sort > q235.out

vector: true

inputs:
  - name: q235.sql
    data: |
      SELECT - col0 / col0 FROM tab1 WHERE NOT + col0 IS NULL GROUP BY col0;
  - name: tab1

outputs:
  - name: q235.out
    data: |
      -1
      -1
      -1
