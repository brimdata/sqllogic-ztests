script: |
  super -f tsv -I q23.sql | sort > q23.out

vector: true

inputs:
  - name: q23.sql
    data: |
      SELECT + col2 * - col0 FROM tab1 WHERE NULL <= NULL GROUP BY col2, col0;
  - name: tab1

outputs:
  - name: q23.out
    data: |
