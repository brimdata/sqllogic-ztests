script: |
  super -f tsv -I q3874.sql | sort > q3874.out

vector: true

inputs:
  - name: q3874.sql
    data: |
      SELECT AVG ( + col1 ) FROM tab1 GROUP BY col1 HAVING NULL > NULL;
  - name: tab1

outputs:
  - name: q3874.out
    data: |
