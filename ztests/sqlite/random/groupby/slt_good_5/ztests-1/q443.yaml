script: |
  super -f tsv -I q443.sql | sort > q443.out

vector: true

inputs:
  - name: q443.sql
    data: |
      SELECT + + ( + 80 ) FROM tab0 GROUP BY tab0.col2;
  - name: tab0

outputs:
  - name: q443.out
    data: |
      80
      80
      80
