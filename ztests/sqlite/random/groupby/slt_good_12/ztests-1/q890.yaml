script: |
  super -f tsv -I q890.sql | sort > q890.out

vector: true

inputs:
  - name: q890.sql
    data: |
      SELECT DISTINCT 45 * - + col1 * - col1 AS col0 FROM tab0 GROUP BY col1;
  - name: tab0

outputs:
  - name: q890.out
    data: |
      0
      295245
