script: |
  super -f tsv -I q3804.sql | sort > q3804.out

vector: true

inputs:
  - name: q3804.sql
    data: |
      SELECT DISTINCT col0 FROM tab2 GROUP BY col0 HAVING NULL > NULL;
  - name: tab2

outputs:
  - name: q3804.out
    data: |
