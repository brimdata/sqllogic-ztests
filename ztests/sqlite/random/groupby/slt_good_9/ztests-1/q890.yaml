script: |
  super -f tsv -I q890.sql | sort > q890.out

vector: true

inputs:
  - name: q890.sql
    data: |
      SELECT DISTINCT - col0 / - col0 FROM tab1 GROUP BY col0;
  - name: tab1

outputs:
  - name: q890.out
    data: |
      1
