script: |
  super -f tsv -I q656.sql | sort > q656.out

vector: true

inputs:
  - name: q656.sql
    data: |
      SELECT 17 + - CAST ( NULL AS REAL );

outputs:
  - name: q656.out
    data: |
      NULL
