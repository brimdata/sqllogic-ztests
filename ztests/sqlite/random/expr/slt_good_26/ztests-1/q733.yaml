script: |
  super -f tsv -I q733.sql | sort > q733.out

vector: true

inputs:
  - name: q733.sql
    data: |
      SELECT + CASE COUNT ( * ) WHEN - 16 THEN NULL WHEN 80 + COUNT ( * ) THEN + 80 END;

outputs:
  - name: q733.out
    data: |
      NULL
