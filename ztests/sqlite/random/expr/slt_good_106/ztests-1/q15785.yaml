script: |
  super -f tsv -I q15785.sql | sort > q15785.out

vector: true

inputs:
  - name: q15785.sql
    data: |
      SELECT COUNT ( * ) * - ( 60 ) / + COUNT ( * );

outputs:
  - name: q15785.out
    data: |
      -60
