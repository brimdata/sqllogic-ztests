script: |
  super -f tsv -I q5172.sql | sort > q5172.out

vector: true

inputs:
  - name: q5172.sql
    data: |
      SELECT + CAST ( SUM ( + 85 ) AS INTEGER );

outputs:
  - name: q5172.out
    data: |
      85
