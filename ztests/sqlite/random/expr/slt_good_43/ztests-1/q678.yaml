script: |
  super -f tsv -I q678.sql | sort > q678.out

vector: true

inputs:
  - name: q678.sql
    data: |
      SELECT CAST ( + MAX ( DISTINCT 4 ) AS INTEGER );

outputs:
  - name: q678.out
    data: |
      4
