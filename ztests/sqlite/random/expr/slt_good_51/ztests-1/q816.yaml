script: |
  super -f tsv -I q816.sql | sort > q816.out

vector: true

inputs:
  - name: q816.sql
    data: |
      SELECT COUNT ( DISTINCT + CAST ( NULL AS INTEGER ) );

outputs:
  - name: q816.out
    data: |
      0
