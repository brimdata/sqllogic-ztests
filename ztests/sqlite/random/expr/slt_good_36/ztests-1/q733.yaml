script: |
  super -f tsv -I q733.sql | sort > q733.out

vector: true

inputs:
  - name: q733.sql
    data: |
      SELECT COALESCE ( 1, - COUNT ( * ) );

outputs:
  - name: q733.out
    data: |
      1
