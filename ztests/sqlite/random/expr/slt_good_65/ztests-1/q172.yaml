script: |
  super -f tsv -I q172.sql | sort > q172.out

vector: true

inputs:
  - name: q172.sql
    data: |
      SELECT DISTINCT + 14 + - - CAST ( NULL AS REAL );

outputs:
  - name: q172.out
    data: |
      NULL
