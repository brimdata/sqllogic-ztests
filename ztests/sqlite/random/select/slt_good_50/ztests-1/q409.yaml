script: |
  super -f text -I q409.spq | sort > q409.out

inputs:
  - name: q409.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE col0 * - col0 * + col2 IN ( col2 * col2 * + col0 );
  - name: tab1

outputs:
  - name: q409.out
    data: |
