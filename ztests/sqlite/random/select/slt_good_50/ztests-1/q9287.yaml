script: |
  super -f text -I q9287.spq | sort > q9287.out

inputs:
  - name: q9287.spq
    data: |
      SELECT DISTINCT col2 + col0 FROM tab0 (FORMAT parquet) WHERE NOT ( col0 + col2 * col0 ) <= ( + col0 + col1 );
  - name: tab0

outputs:
  - name: q9287.out
    data: |
      171
      57
