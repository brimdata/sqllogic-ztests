script: |
  super -f text -I q10420.spq | sort > q10420.out

inputs:
  - name: q10420.spq
    data: |
      SELECT DISTINCT + col0 + col0 / - col1 FROM tab1 (FORMAT parquet) WHERE col2 * col1 < ( NULL );
  - name: tab1

outputs:
  - name: q10420.out
    data: |
