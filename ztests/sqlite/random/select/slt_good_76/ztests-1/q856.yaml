script: |
  super -f text -I q856.spq | sort > q856.out

inputs:
  - name: q856.spq
    data: |
      SELECT + col2 * col0 / col1 + - col2 * col0 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q856.out
    data: |
      -156
      -3284
      -7090
