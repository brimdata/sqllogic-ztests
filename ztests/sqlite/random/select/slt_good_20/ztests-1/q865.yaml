script: |
  super -f text -I q865.spq | sort > q865.out

inputs:
  - name: q865.spq
    data: |
      SELECT DISTINCT col0 + col2 + col2 * col1 FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( col0 * + col0 );
  - name: tab1

outputs:
  - name: q865.out
    data: |
