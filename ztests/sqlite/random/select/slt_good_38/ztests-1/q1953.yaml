script: |
  super -f text -I q1953.spq | sort > q1953.out

inputs:
  - name: q1953.spq
    data: |
      SELECT + col1 + tab1.col0 * - col2 * col0 + col1 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q1953.out
    data: |
      -233452
      -434
      -614374
