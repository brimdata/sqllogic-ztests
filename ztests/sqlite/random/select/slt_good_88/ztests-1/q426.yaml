script: |
  super -f text -I q426.spq | sort > q426.out

inputs:
  - name: q426.spq
    data: |
      SELECT ALL + col0 FROM tab1 (FORMAT parquet) WHERE ( + col2 * + col2 + col2 ) NOT IN ( + col1 + tab1.col1 );
  - name: tab1

outputs:
  - name: q426.out
    data: |
      3
      64
      80
