script: |
  super -f text -I q1000.spq | sort > q1000.out

inputs:
  - name: q1000.spq
    data: |
      SELECT DISTINCT - col2 FROM tab1 (FORMAT parquet) WHERE col2 NOT IN ( tab1.col0 * col1 + + col1 + + tab1.col0 );
  - name: tab1

outputs:
  - name: q1000.out
    data: |
      -54
      -57
      -96
