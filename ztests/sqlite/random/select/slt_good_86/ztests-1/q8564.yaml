script: |
  super -f text -I q8564.spq | sort > q8564.out

inputs:
  - name: q8564.spq
    data: |
      SELECT DISTINCT + col2 * col1 + tab2.col1 FROM tab2 (FORMAT parquet) WHERE NOT ( NULL ) BETWEEN + col1 AND ( NULL );
  - name: tab2

outputs:
  - name: q8564.out
    data: |
