script: |
  super -f text -I q199.spq | sort > q199.out

inputs:
  - name: q199.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT ( col2 * col1 - + col0 / - col0 ) NOT IN ( - tab0.col1 );
  - name: tab0

outputs:
  - name: q199.out
    data: |
