script: |
  super -f text -I q6867.spq | sort > q6867.out

inputs:
  - name: q6867.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + col0 * - col0 <= NULL;
  - name: tab1

outputs:
  - name: q6867.out
    data: |
