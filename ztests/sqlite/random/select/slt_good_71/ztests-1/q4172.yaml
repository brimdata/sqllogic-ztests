script: |
  super -f text -I q4172.spq | sort > q4172.out

inputs:
  - name: q4172.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT col0 + - col0 <= col1 * + col1;
  - name: tab2

outputs:
  - name: q4172.out
    data: |
