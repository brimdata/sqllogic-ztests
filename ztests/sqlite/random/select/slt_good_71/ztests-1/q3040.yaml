script: |
  super -f text -I q3040.spq | sort > q3040.out

inputs:
  - name: q3040.spq
    data: |
      SELECT DISTINCT + ( 89 ) * - tab0.col2 + col1 * - col1 AS col1 FROM tab0 (FORMAT parquet);
  - name: tab0

outputs:
  - name: q3040.out
    data: |
      -10333
      -15579
      -9498
