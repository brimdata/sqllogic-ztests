script: |
  super -f text -I q765.spq | sort > q765.out

inputs:
  - name: q765.spq
    data: |
      SELECT - tab1.col2 / + col2 AS col0 FROM tab1 (FORMAT parquet) WHERE + col2 * - col2 NOT IN ( + col2 );
  - name: tab1

outputs:
  - name: q765.out
    data: |
      -1
      -1
      -1
