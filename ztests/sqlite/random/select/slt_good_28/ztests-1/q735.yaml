script: |
  super -f text -I q735.spq | sort > q735.out

inputs:
  - name: q735.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT - col1 * - col1 + - col0 BETWEEN NULL AND ( NULL );
  - name: tab0

outputs:
  - name: q735.out
    data: |
