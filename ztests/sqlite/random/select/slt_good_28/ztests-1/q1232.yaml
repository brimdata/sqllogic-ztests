script: |
  super -f text -I q1232.spq | sort > q1232.out

inputs:
  - name: q1232.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet), tab0 (FORMAT parquet) AS cor0 CROSS JOIN tab0 (FORMAT parquet) WHERE NOT ( NULL ) < NULL;
  - name: tab0
  - name: tab1

outputs:
  - name: q1232.out
    data: |
