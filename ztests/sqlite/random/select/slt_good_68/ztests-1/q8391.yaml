script: |
  super -f text -I q8391.spq | sort > q8391.out

inputs:
  - name: q8391.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet), tab0 (FORMAT parquet) cor0 CROSS JOIN tab2 (FORMAT parquet) WHERE NOT NULL BETWEEN NULL AND ( + tab2.col2 );
  - name: tab0
  - name: tab2

outputs:
  - name: q8391.out
    data: |
