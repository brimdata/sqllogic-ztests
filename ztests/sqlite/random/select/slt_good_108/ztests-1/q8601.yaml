script: |
  super -f text -I q8601.spq | sort > q8601.out

inputs:
  - name: q8601.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( col0 ) = NULL AND NOT NULL NOT IN ( - col1 / + col2 );
  - name: tab1

outputs:
  - name: q8601.out
    data: |
