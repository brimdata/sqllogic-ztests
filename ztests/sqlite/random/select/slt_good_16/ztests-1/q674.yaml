script: |
  super -f text -I q674.spq | sort > q674.out

inputs:
  - name: q674.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT col1 * col1 >= NULL;
  - name: tab1

outputs:
  - name: q674.out
    data: |
