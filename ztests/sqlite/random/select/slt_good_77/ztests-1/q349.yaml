script: |
  super -f text -I q349.spq | sort > q349.out

inputs:
  - name: q349.spq
    data: |
      SELECT DISTINCT + col0 - ( + col0 ) FROM tab2 (FORMAT parquet);
  - name: tab2

outputs:
  - name: q349.out
    data: |
      0
