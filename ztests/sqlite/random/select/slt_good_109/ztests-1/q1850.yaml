script: |
  super -f text -I q1850.spq | sort > q1850.out

inputs:
  - name: q1850.spq
    data: |
      SELECT ALL * FROM tab0 (FORMAT parquet), tab0 (FORMAT parquet) AS cor0, tab1 (FORMAT parquet) AS cor1, tab1 (FORMAT parquet), tab2 (FORMAT parquet) AS cor2 WHERE NOT NULL <= ( NULL );
  - name: tab0
  - name: tab1
  - name: tab2

outputs:
  - name: q1850.out
    data: |
