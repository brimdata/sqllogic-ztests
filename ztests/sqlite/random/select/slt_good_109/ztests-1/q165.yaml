script: |
  super -f tsv -I q165.sql | sort > q165.out

vector: true

inputs:
  - name: q165.sql
    data: |
      SELECT DISTINCT + col0 * - col0 FROM tab1 WHERE NOT + col2 NOT IN ( + col0 );
  - name: tab1

outputs:
  - name: q165.out
    data: |
