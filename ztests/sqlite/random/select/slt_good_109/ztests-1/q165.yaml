script: |
  super -f text -I q165.spq | sort > q165.out

inputs:
  - name: q165.spq
    data: |
      SELECT DISTINCT + col0 * - col0 FROM tab1 (FORMAT parquet) WHERE NOT + col2 NOT IN ( + col0 );
  - name: tab1

outputs:
  - name: q165.out
    data: |
