script: |
  super -f text -I q118.spq | sort > q118.out

inputs:
  - name: q118.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL < col0 / col2;
  - name: tab1

outputs:
  - name: q118.out
    data: |
