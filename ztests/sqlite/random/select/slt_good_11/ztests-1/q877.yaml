script: |
  super -f text -I q877.spq | sort > q877.out

inputs:
  - name: q877.spq
    data: |
      SELECT col0 + col2 * - col0 FROM tab0 (FORMAT parquet) WHERE NOT col2 >= ( NULL );
  - name: tab0

outputs:
  - name: q877.out
    data: |
