script: |
  super -f tsv -I q874.sql | sort > q874.out

vector: true

inputs:
  - name: q874.sql
    data: |
      SELECT ALL * FROM tab1 WHERE NOT NULL BETWEEN col1 AND - col2;
  - name: tab1

outputs:
  - name: q874.out
    data: |
