script: |
  super -f text -I q5150.spq | sort > q5150.out

inputs:
  - name: q5150.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( + col1 * tab1.col0 - + col1 * col1 );
  - name: tab1

outputs:
  - name: q5150.out
    data: |
