script: |
  super -f text -I q6352.spq | sort > q6352.out

inputs:
  - name: q6352.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet), tab0 (FORMAT parquet) AS cor0 INNER JOIN tab0 (FORMAT parquet) ON ( NULL ) >= ( NULL );
  - name: tab0
  - name: tab1

outputs:
  - name: q6352.out
    data: |
