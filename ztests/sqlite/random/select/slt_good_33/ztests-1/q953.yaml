script: |
  super -f text -I q953.spq | sort > q953.out

inputs:
  - name: q953.spq
    data: |
      SELECT - col1 / col0 + - col2 AS col2 FROM tab2 (FORMAT parquet) WHERE NOT ( NULL ) = ( NULL );
  - name: tab2

outputs:
  - name: q953.out
    data: |
