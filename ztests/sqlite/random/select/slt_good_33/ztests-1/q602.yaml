script: |
  super -f text -I q602.spq | sort > q602.out

inputs:
  - name: q602.spq
    data: |
      SELECT col2 + 11 * - col1 + - col2 FROM tab0 (FORMAT parquet);
  - name: tab0

outputs:
  - name: q602.out
    data: |
      -1001
      -1067
      -946
