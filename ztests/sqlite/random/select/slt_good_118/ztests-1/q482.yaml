script: |
  super -f text -I q482.spq | sort > q482.out

inputs:
  - name: q482.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT ( col2 + + col2 + - col1 ) > ( NULL );
  - name: tab1

outputs:
  - name: q482.out
    data: |
