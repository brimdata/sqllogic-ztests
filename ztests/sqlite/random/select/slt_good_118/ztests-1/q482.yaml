script: |
  super -f tsv -I q482.sql | sort > q482.out

vector: true

inputs:
  - name: q482.sql
    data: |
      SELECT * FROM tab1 WHERE NOT ( col2 + + col2 + - col1 ) > ( NULL );
  - name: tab1

outputs:
  - name: q482.out
    data: |
