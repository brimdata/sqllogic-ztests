script: |
  super -f text -I q771.spq | sort > q771.out

inputs:
  - name: q771.spq
    data: |
      SELECT DISTINCT - col1 FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( col1 + - col0 * tab1.col0 * + tab1.col0 );
  - name: tab1

outputs:
  - name: q771.out
    data: |
