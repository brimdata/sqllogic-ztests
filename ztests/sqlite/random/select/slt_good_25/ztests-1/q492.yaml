script: |
  super -f text -I q492.spq | sort > q492.out

inputs:
  - name: q492.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT col0 + col0 * - col2 * - col2 >= col1 * - col2 * col2;
  - name: tab2

outputs:
  - name: q492.out
    data: |
