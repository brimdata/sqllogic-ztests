script: |
  super -f text -I q640.spq | sort > q640.out

inputs:
  - name: q640.spq
    data: |
      SELECT col1 * - col2 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q640.out
    data: |
      -1248
      -1404
      -570
