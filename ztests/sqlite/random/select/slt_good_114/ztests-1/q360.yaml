script: |
  super -f text -I q360.spq | sort > q360.out

inputs:
  - name: q360.spq
    data: |
      SELECT DISTINCT + col0 * - col2 * - col0 AS col1 FROM tab2 (FORMAT parquet) WHERE NOT NULL NOT IN ( col2 + col1 * + col1 );
  - name: tab2

outputs:
  - name: q360.out
    data: |
