script: |
  super -f text -I q341.spq | sort > q341.out

inputs:
  - name: q341.spq
    data: |
      SELECT ( 36 ) + - cor1.col1 / ( + 72 ) FROM tab1 (FORMAT parquet) AS cor0 CROSS JOIN tab1 (FORMAT parquet) cor1;
  - name: tab1

outputs:
  - name: q341.out
    data: |
      36
      36
      36
      36
      36
      36
      36
      36
      36
