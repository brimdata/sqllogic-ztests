script: |
  super -f text -I q1929.spq | sort > q1929.out

inputs:
  - name: q1929.spq
    data: |
      SELECT + col1 * - tab1.col1 * col1 FROM tab1 (FORMAT parquet) WHERE NOT ( NULL ) <= col0;
  - name: tab1

outputs:
  - name: q1929.out
    data: |
