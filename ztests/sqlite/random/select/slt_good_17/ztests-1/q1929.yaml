script: |
  super -f tsv -I q1929.sql | sort > q1929.out

vector: true

inputs:
  - name: q1929.sql
    data: |
      SELECT + col1 * - tab1.col1 * col1 FROM tab1 WHERE NOT ( NULL ) <= col0;
  - name: tab1

outputs:
  - name: q1929.out
    data: |
