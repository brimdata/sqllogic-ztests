script: |
  super -f text -I q9536.spq | sort > q9536.out

inputs:
  - name: q9536.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT + col2 * - col1 < NULL;
  - name: tab1

outputs:
  - name: q9536.out
    data: |
