script: |
  super -f tsv -I q2016.sql | sort > q2016.out

vector: true

inputs:
  - name: q2016.sql
    data: |
      SELECT * FROM tab1 WHERE NOT NULL NOT IN ( - col1 * - col2 + col1 - + col1 );
  - name: tab1

outputs:
  - name: q2016.out
    data: |
