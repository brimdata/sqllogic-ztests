script: |
  super -f text -I q2016.spq | sort > q2016.out

inputs:
  - name: q2016.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( - col1 * - col2 + col1 - + col1 );
  - name: tab1

outputs:
  - name: q2016.out
    data: |
