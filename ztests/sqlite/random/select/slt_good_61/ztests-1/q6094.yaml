script: |
  super -f text -I q6094.spq | sort > q6094.out

inputs:
  - name: q6094.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE NOT + col2 * - col1 NOT IN ( + col1 );
  - name: tab0

outputs:
  - name: q6094.out
    data: |
