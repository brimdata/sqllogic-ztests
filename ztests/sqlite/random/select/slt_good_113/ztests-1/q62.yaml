script: |
  super -f text -I q62.spq | sort > q62.out

inputs:
  - name: q62.spq
    data: |
      SELECT DISTINCT ( + 50 ) FROM tab2 (FORMAT parquet);
  - name: tab2

outputs:
  - name: q62.out
    data: |
      50
