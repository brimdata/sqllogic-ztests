script: |
  super -f text -I q8719.spq | sort > q8719.out

inputs:
  - name: q8719.spq
    data: |
      SELECT DISTINCT - col1 * col1 + + col2 FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( + col0 );
  - name: tab1

outputs:
  - name: q8719.out
    data: |
