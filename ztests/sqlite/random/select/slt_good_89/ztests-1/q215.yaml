script: |
  super -f tsv -I q215.sql | sort > q215.out

vector: true

inputs:
  - name: q215.sql
    data: |
      SELECT * FROM tab1 WHERE NOT ( + col0 ) BETWEEN NULL AND ( NULL );
  - name: tab1

outputs:
  - name: q215.out
    data: |
