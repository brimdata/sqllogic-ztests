script: |
  super -f tsv -I q172.sql | sort > q172.out

vector: true

inputs:
  - name: q172.sql
    data: |
      SELECT - tab1.col2 / + col2 FROM tab1;
  - name: tab1

outputs:
  - name: q172.out
    data: |
      -1
      -1
      -1
