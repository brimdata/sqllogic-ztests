script: |
  super -f text -I q321.spq | sort > q321.out

inputs:
  - name: q321.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT + col2 + col1 NOT IN ( - tab2.col2 * - col1 );
  - name: tab2

outputs:
  - name: q321.out
    data: |
