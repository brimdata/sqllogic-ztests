script: |
  super -f tsv -I q321.sql | sort > q321.out

vector: true

inputs:
  - name: q321.sql
    data: |
      SELECT * FROM tab2 WHERE NOT + col2 + col1 NOT IN ( - tab2.col2 * - col1 );
  - name: tab2

outputs:
  - name: q321.out
    data: |
