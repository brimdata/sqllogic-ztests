script: |
  super -f text -I q546.spq | sort > q546.out

inputs:
  - name: q546.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT + col2 NOT IN ( - col2 + col2 * + col0 );
  - name: tab1

outputs:
  - name: q546.out
    data: |
