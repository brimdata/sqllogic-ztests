script: |
  super -f text -I q4507.spq | sort > q4507.out

inputs:
  - name: q4507.spq
    data: |
      SELECT DISTINCT tab0.col1 AS col2 FROM tab0 (FORMAT parquet) WHERE NOT NULL NOT IN ( + col1 * - col1 + col1 );
  - name: tab0

outputs:
  - name: q4507.out
    data: |
