script: |
  super -f text -I q953.spq | sort > q953.out

inputs:
  - name: q953.spq
    data: |
      SELECT DISTINCT ( + col1 ) + - col0 FROM tab0 (FORMAT parquet);
  - name: tab0

outputs:
  - name: q953.out
    data: |
      2
      62
