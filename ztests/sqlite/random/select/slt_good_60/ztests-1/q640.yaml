script: |
  super -f text -I q640.spq | sort > q640.out

inputs:
  - name: q640.spq
    data: |
      SELECT + col0 + + col1 * - 24 FROM tab2 (FORMAT parquet);
  - name: tab2

outputs:
  - name: q640.out
    data: |
      -1338
      -329
      -737
