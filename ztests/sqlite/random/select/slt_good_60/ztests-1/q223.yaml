script: |
  super -f text -I q223.spq | sort > q223.out

inputs:
  - name: q223.spq
    data: |
      SELECT DISTINCT + col1 + col2 * col0 / - col1 FROM tab1 (FORMAT parquet) WHERE NOT ( NULL ) <> ( NULL );
  - name: tab1

outputs:
  - name: q223.out
    data: |
