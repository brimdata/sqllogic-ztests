script: |
  super -f text -I q804.spq | sort > q804.out

inputs:
  - name: q804.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NULL <= + col2 / + col2;
  - name: tab1

outputs:
  - name: q804.out
    data: |
