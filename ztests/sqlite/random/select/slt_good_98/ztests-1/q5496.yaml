script: |
  super -f text -I q5496.spq | sort > q5496.out

inputs:
  - name: q5496.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet), tab1 (FORMAT parquet) AS cor0 CROSS JOIN tab2 (FORMAT parquet), tab0 (FORMAT parquet) AS cor1 WHERE NULL = NULL;
  - name: tab0
  - name: tab1
  - name: tab2

outputs:
  - name: q5496.out
    data: |
