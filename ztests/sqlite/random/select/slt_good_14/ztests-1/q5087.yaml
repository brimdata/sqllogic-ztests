script: |
  super -f text -I q5087.spq | sort > q5087.out

inputs:
  - name: q5087.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT ( - col0 * + col0 * col1 + col1 + col1 ) NOT IN ( + col2 );
  - name: tab1

outputs:
  - name: q5087.out
    data: |
