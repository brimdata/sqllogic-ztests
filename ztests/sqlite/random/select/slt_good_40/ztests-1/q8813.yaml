script: |
  super -f text -I q8813.spq | sort > q8813.out

inputs:
  - name: q8813.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet), tab0 (FORMAT parquet) AS cor0 JOIN tab1 (FORMAT parquet) ON NULL = NULL;
  - name: tab0
  - name: tab1

outputs:
  - name: q8813.out
    data: |
