script: |
  super -f text -I q405.spq | sort > q405.out

inputs:
  - name: q405.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE ( NOT NULL NOT IN ( col0 * - col1 / - col1 ) ) AND NULL NOT IN ( col0 );
  - name: tab1

outputs:
  - name: q405.out
    data: |
