script: |
  super -f text -I q602.spq | sort > q602.out

inputs:
  - name: q602.spq
    data: |
      SELECT DISTINCT col0 + col1 FROM tab1 (FORMAT parquet) WHERE col1 * col0 + col2 + col0 * col1 <= ( NULL );
  - name: tab1

outputs:
  - name: q602.out
    data: |
