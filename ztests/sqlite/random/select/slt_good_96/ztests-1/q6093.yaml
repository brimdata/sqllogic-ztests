script: |
  super -f text -I q6093.spq | sort > q6093.out

inputs:
  - name: q6093.spq
    data: |
      SELECT + col0 AS col0 FROM tab1 (FORMAT parquet) cor0 WHERE NOT col0 * - col1 * - col1 NOT IN ( + col1 * col1 );
  - name: tab1

outputs:
  - name: q6093.out
    data: |
