script: |
  super -f text -I q93.spq | sort > q93.out

inputs:
  - name: q93.spq
    data: |
      SELECT ALL col2 + - col1 AS col0 FROM tab2 (FORMAT parquet) WHERE NOT ( + col2 + col2 / + col0 ) = ( NULL );
  - name: tab2

outputs:
  - name: q93.out
    data: |
