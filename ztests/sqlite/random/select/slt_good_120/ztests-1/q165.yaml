script: |
  super -f text -I q165.spq | sort > q165.out

inputs:
  - name: q165.spq
    data: |
      SELECT + col2 * ( + col0 + - col0 ) FROM tab2 (FORMAT parquet);
  - name: tab2

outputs:
  - name: q165.out
    data: |
      0
      0
      0
