script: |
  super -f text -I q575.spq | sort > q575.out

inputs:
  - name: q575.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT col2 * - col2 NOT IN ( col1 / + col1 + col0 * + col1 );
  - name: tab1

outputs:
  - name: q575.out
    data: |
