script: |
  super -f tsv -I q575.sql | sort > q575.out

vector: true

inputs:
  - name: q575.sql
    data: |
      SELECT * FROM tab1 WHERE NOT col2 * - col2 NOT IN ( col1 / + col1 + col0 * + col1 );
  - name: tab1

outputs:
  - name: q575.out
    data: |
