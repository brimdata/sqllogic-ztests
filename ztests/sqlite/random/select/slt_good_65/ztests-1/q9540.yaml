script: |
  super -f text -I q9540.spq | sort > q9540.out

inputs:
  - name: q9540.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT NULL > col2 * col2 - col1 * col2;
  - name: tab1

outputs:
  - name: q9540.out
    data: |
