script: |
  super -f text -I q370.spq | sort > q370.out

inputs:
  - name: q370.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT ( col2 + - col0 + - col2 ) >= col2 + col0 * - col0;
  - name: tab2

outputs:
  - name: q370.out
    data: |
