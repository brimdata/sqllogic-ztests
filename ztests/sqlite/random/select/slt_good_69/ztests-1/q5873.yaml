script: |
  super -f text -I q5873.spq | sort > q5873.out

inputs:
  - name: q5873.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet), tab0 (FORMAT parquet) cor0 CROSS JOIN tab0 (FORMAT parquet) WHERE NOT NULL BETWEEN NULL AND NULL;
  - name: tab0
  - name: tab1

outputs:
  - name: q5873.out
    data: |
