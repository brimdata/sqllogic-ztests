script: |
  super -f text -I q602.spq | sort > q602.out

inputs:
  - name: q602.spq
    data: |
      SELECT DISTINCT - tab2.col2 / - col1 FROM tab2 (FORMAT parquet);
  - name: tab2

outputs:
  - name: q602.out
    data: |
      0
      2
