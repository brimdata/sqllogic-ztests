script: |
  super -f text -I q5322.spq | sort > q5322.out

inputs:
  - name: q5322.spq
    data: |
      SELECT + col0 * col2 AS col1 FROM tab1 (FORMAT parquet) WHERE NOT + col0 + - col0 - - col2 NOT IN ( tab1.col2 + + col1 );
  - name: tab1

outputs:
  - name: q5322.out
    data: |
