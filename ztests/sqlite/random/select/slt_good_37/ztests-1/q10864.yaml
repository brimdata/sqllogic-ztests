script: |
  super -f text -I q10864.spq | sort > q10864.out

inputs:
  - name: q10864.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT NULL <= col1 + - tab1.col1 * + col2 + col1;
  - name: tab1

outputs:
  - name: q10864.out
    data: |
