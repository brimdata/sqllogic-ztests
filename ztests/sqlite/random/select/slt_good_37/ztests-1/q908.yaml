script: |
  super -f text -I q908.spq | sort > q908.out

inputs:
  - name: q908.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT col0 / + col2 = NULL;
  - name: tab1

outputs:
  - name: q908.out
    data: |
