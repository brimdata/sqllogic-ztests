script: |
  super -f tsv -I q908.sql | sort > q908.out

vector: true

inputs:
  - name: q908.sql
    data: |
      SELECT ALL * FROM tab1 WHERE NOT col0 / + col2 = NULL;
  - name: tab1

outputs:
  - name: q908.out
    data: |
