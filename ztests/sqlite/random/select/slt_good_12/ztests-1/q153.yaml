script: |
  super -f text -I q153.spq | sort > q153.out

inputs:
  - name: q153.spq
    data: |
      SELECT DISTINCT * FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( + col0 + col2 * - col0 / col2 );
  - name: tab1

outputs:
  - name: q153.out
    data: |
