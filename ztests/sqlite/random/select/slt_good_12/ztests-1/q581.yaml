script: |
  super -f text -I q581.spq | sort > q581.out

inputs:
  - name: q581.spq
    data: |
      SELECT ALL - col2 * tab1.col2 + + col0 * ( - tab1.col2 ) + col0 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q581.out
    data: |
      -16816
      -3075
      -6833
