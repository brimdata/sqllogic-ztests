script: |
  super -f text -I q409.spq | sort > q409.out

inputs:
  - name: q409.spq
    data: |
      SELECT + col0 + + col1 + col1 FROM tab1 (FORMAT parquet) WHERE - col1 * - col0 <> NULL;
  - name: tab1

outputs:
  - name: q409.out
    data: |
