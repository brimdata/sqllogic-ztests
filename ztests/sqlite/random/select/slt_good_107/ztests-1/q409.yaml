script: |
  super -f tsv -I q409.sql | sort > q409.out

vector: true

inputs:
  - name: q409.sql
    data: |
      SELECT + col0 + + col1 + col1 FROM tab1 WHERE - col1 * - col0 <> NULL;
  - name: tab1

outputs:
  - name: q409.out
    data: |
