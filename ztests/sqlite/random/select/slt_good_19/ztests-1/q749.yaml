script: |
  super -f tsv -I q749.sql | sort > q749.out

vector: true

inputs:
  - name: q749.sql
    data: |
      SELECT * FROM tab0 WHERE NOT col1 + + col1 * - col1 NOT IN ( col2 + tab0.col1 + col0 * + col1 );
  - name: tab0

outputs:
  - name: q749.out
    data: |
