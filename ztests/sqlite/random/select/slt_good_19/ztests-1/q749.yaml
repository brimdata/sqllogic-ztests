script: |
  super -f text -I q749.spq | sort > q749.out

inputs:
  - name: q749.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT col1 + + col1 * - col1 NOT IN ( col2 + tab0.col1 + col0 * + col1 );
  - name: tab0

outputs:
  - name: q749.out
    data: |
