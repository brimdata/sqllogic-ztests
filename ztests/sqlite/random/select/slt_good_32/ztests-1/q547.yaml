script: |
  super -f text -I q547.spq | sort > q547.out

inputs:
  - name: q547.spq
    data: |
      SELECT DISTINCT + tab2.col1 * col1 + col1 + col1 * - col0 / tab2.col2 FROM tab2 (FORMAT parquet) WHERE NOT NULL = tab2.col0 * col0 - col2;
  - name: tab2

outputs:
  - name: q547.out
    data: |
