script: |
  super -f text -I q3202.spq | sort > q3202.out

inputs:
  - name: q3202.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT col2 * - col1 + - col1 NOT BETWEEN NULL AND NULL;
  - name: tab1

outputs:
  - name: q3202.out
    data: |
