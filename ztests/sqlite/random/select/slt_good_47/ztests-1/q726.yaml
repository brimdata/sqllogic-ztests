script: |
  super -f text -I q726.spq | sort > q726.out

inputs:
  - name: q726.spq
    data: |
      SELECT DISTINCT col1 + col1 FROM tab0 (FORMAT parquet) WHERE NULL IN ( col2 * - col1 );
  - name: tab0

outputs:
  - name: q726.out
    data: |
