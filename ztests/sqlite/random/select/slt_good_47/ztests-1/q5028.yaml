script: |
  super -f tsv -I q5028.sql | sort > q5028.out

vector: true

inputs:
  - name: q5028.sql
    data: |
      SELECT DISTINCT col0 / - col0 + col1 FROM tab0 WHERE NOT NULL >= ( NULL );
  - name: tab0

outputs:
  - name: q5028.out
    data: |
