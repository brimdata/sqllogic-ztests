script: |
  super -f text -I q4996.spq | sort > q4996.out

inputs:
  - name: q4996.spq
    data: |
      SELECT + col2 + col1 * - col1 FROM tab1 (FORMAT parquet) WHERE NOT ( col0 * col1 ) > NULL;
  - name: tab1

outputs:
  - name: q4996.out
    data: |
