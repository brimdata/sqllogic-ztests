script: |
  super -f text -I q20.spq | sort > q20.out

inputs:
  - name: q20.spq
    data: |
      SELECT col2 / - col0 + - col2 FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( + col0 );
  - name: tab1

outputs:
  - name: q20.out
    data: |
