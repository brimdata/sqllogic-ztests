script: |
  super -f text -I q3543.spq | sort > q3543.out

inputs:
  - name: q3543.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE + col0 * col1 + - col2 * col1 IN ( + col0 * col1 + + col2 + col2 * col0 );
  - name: tab0

outputs:
  - name: q3543.out
    data: |
