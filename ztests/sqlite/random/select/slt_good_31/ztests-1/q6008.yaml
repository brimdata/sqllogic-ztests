script: |
  super -f text -I q6008.spq | sort > q6008.out

inputs:
  - name: q6008.spq
    data: |
      SELECT + ( + col0 ) + + ( - col0 ) * col1 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q6008.out
    data: |
      -576
      -75
      -960
