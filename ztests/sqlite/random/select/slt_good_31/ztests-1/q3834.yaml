script: |
  super -f text -I q3834.spq | sort > q3834.out

inputs:
  - name: q3834.spq
    data: |
      SELECT + tab1.col1 FROM tab1 (FORMAT parquet) WHERE ( + col2 ) IN ( - col2 / - col1 + tab1.col1 );
  - name: tab1

outputs:
  - name: q3834.out
    data: |
