script: |
  super -f tsv -I q3834.sql | sort > q3834.out

vector: true

inputs:
  - name: q3834.sql
    data: |
      SELECT + tab1.col1 FROM tab1 WHERE ( + col2 ) IN ( - col2 / - col1 + tab1.col1 );
  - name: tab1

outputs:
  - name: q3834.out
    data: |
