script: |
  super -f tsv -I q377.sql | sort > q377.out

vector: true

inputs:
  - name: q377.sql
    data: |
      SELECT * FROM tab2 WHERE NOT NULL BETWEEN + col2 * col1 AND NULL;
  - name: tab2

outputs:
  - name: q377.out
    data: |
