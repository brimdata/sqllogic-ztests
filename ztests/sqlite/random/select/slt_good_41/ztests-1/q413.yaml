script: |
  super -f tsv -I q413.sql | sort > q413.out

vector: true

inputs:
  - name: q413.sql
    data: |
      SELECT DISTINCT * FROM tab0 WHERE NOT col1 * - col2 NOT IN ( + col0 );
  - name: tab0

outputs:
  - name: q413.out
    data: |
