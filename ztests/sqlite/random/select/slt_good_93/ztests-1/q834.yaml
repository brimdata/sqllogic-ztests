script: |
  super -f tsv -I q834.sql | sort > q834.out

vector: true

inputs:
  - name: q834.sql
    data: |
      SELECT * FROM tab2 WHERE NOT ( NULL ) NOT IN ( + col1 + - col0 * col0 * + col1 + col1 );
  - name: tab2

outputs:
  - name: q834.out
    data: |
