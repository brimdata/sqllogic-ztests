script: |
  super -f text -I q834.spq | sort > q834.out

inputs:
  - name: q834.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT ( NULL ) NOT IN ( + col1 + - col0 * col0 * + col1 + col1 );
  - name: tab2

outputs:
  - name: q834.out
    data: |
