script: |
  super -f text -I q915.sql | sort > q915.out

inputs:
  - name: q915.sql
    data: |
      SELECT * FROM tab1 WHERE NOT col1 NOT IN ( + col0 );
  - name: tab1

outputs:
  - name: q915.out
    data: |
