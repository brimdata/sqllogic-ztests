script: |
  super -f text -I q982.spq | sort > q982.out

inputs:
  - name: q982.spq
    data: |
      SELECT ALL * FROM tab1 (FORMAT parquet) WHERE NOT col1 + + col0 * col1 NOT IN ( col1 );
  - name: tab1

outputs:
  - name: q982.out
    data: |
