script: |
  super -f text -I q6058.sql | sort > q6058.out

inputs:
  - name: q6058.sql
    data: |
      SELECT ALL * FROM tab1 WHERE NOT NULL NOT IN ( + col2 + col0 / + col0 );
  - name: tab1

outputs:
  - name: q6058.out
    data: |
