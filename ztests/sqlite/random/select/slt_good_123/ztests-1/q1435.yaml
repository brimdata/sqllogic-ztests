script: |
  super -f text -I q1435.spq | sort > q1435.out

inputs:
  - name: q1435.spq
    data: |
      SELECT + col1 / ( ( col0 ) * - 20 ) - 80 FROM tab1 (FORMAT parquet) AS cor0;
  - name: tab1

outputs:
  - name: q1435.out
    data: |
      -80
      -80
      -80
