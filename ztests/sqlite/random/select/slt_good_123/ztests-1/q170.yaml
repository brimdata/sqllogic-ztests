script: |
  super -f text -I q170.spq | sort > q170.out

inputs:
  - name: q170.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT NULL NOT IN ( + tab2.col2 - + tab2.col2 * - col2 );
  - name: tab2

outputs:
  - name: q170.out
    data: |
