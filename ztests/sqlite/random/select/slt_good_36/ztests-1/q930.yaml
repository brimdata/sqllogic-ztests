script: |
  super -f text -I q930.spq | sort > q930.out

inputs:
  - name: q930.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT col0 NOT IN ( + col2 + col0 + + col1 );
  - name: tab2

outputs:
  - name: q930.out
    data: |
