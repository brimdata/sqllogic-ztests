script: |
  super -f tsv -I q997.sql | sort > q997.out

vector: true

inputs:
  - name: q997.sql
    data: |
      SELECT * FROM tab1 WHERE NOT + col1 + - col2 + + col1 NOT IN ( col1 * col1 + + col2 );
  - name: tab1

outputs:
  - name: q997.out
    data: |
