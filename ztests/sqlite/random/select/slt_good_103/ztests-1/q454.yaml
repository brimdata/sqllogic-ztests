script: |
  super -f text -I q454.spq | sort > q454.out

inputs:
  - name: q454.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) WHERE col1 * col1 + + col1 BETWEEN + col2 * - col1 AND ( NULL );
  - name: tab0

outputs:
  - name: q454.out
    data: |
