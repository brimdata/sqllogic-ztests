script: |
  super -f text -I q409.spq | sort > q409.out

inputs:
  - name: q409.spq
    data: |
      SELECT DISTINCT + 17 + ( + col2 * - 10 ) FROM tab2 (FORMAT parquet);
  - name: tab2

outputs:
  - name: q409.out
    data: |
      -243
      -253
      -363
