script: |
  super -f text -I q1740.spq | sort > q1740.out

inputs:
  - name: q1740.spq
    data: |
      SELECT ALL ( col1 ) * col1 / ( + col0 * - col0 ) FROM tab0 (FORMAT parquet);
  - name: tab0

outputs:
  - name: q1740.out
    data: |
      -1
      -12
      -7
