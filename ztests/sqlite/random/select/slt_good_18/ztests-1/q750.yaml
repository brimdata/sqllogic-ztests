script: |
  super -f text -I q750.spq | sort > q750.out

inputs:
  - name: q750.spq
    data: |
      SELECT DISTINCT * FROM tab0 (FORMAT parquet) AS cor0 WHERE col0 + + col1 + + col1 * - col1 < ( NULL );
  - name: tab0

outputs:
  - name: q750.out
    data: |
