script: |
  super -f text -I q814.spq | sort > q814.out

inputs:
  - name: q814.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) WHERE NOT NULL NOT BETWEEN + col2 * col1 + col1 AND ( NULL );
  - name: tab2

outputs:
  - name: q814.out
    data: |
