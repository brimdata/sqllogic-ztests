script: |
  super -f text -I q241.spq | sort > q241.out

inputs:
  - name: q241.spq
    data: |
      SELECT - col2 / ( col1 * col1 ) + col0 FROM tab1 (FORMAT parquet);
  - name: tab1

outputs:
  - name: q241.out
    data: |
      3
      64
      80
