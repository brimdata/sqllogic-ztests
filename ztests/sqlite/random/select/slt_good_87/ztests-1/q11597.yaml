script: |
  super -f text -I q11597.spq | sort > q11597.out

inputs:
  - name: q11597.spq
    data: |
      SELECT DISTINCT ( - cor0.col0 ) AS col1 FROM tab1 (FORMAT parquet) AS cor0 CROSS JOIN tab0 (FORMAT parquet), tab0 (FORMAT parquet) AS cor1, tab0 (FORMAT parquet) AS cor2, tab1 (FORMAT parquet) AS cor3;
  - name: tab0
  - name: tab1

outputs:
  - name: q11597.out
    data: |
      -3
      -64
      -80
