script: |
  super -f text -I q4412.spq | sort > q4412.out

inputs:
  - name: q4412.spq
    data: |
      SELECT DISTINCT ( - cor0.col1 ) * + tab2.col1 FROM tab1 (FORMAT parquet), tab2 (FORMAT parquet) AS cor0 CROSS JOIN tab2 (FORMAT parquet);
  - name: tab1
  - name: tab2

outputs:
  - name: q4412.out
    data: |
      -1003
      -1829
      -289
      -3481
      -527
      -961
