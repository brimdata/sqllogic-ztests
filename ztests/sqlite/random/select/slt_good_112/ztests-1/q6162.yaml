script: |
  super -f text -I q6162.spq | sort > q6162.out

inputs:
  - name: q6162.spq
    data: |
      SELECT DISTINCT * FROM tab2 (FORMAT parquet) WHERE NOT ( + col2 ) BETWEEN col0 * - col1 AND col2;
  - name: tab2

outputs:
  - name: q6162.out
    data: |
