script: |
  super -f tsv -I q816.sql | sort > q816.out

vector: true

inputs:
  - name: q816.sql
    data: |
      SELECT * FROM tab0 WHERE NOT NULL NOT IN ( + col2 + + col1 );
  - name: tab0

outputs:
  - name: q816.out
    data: |
