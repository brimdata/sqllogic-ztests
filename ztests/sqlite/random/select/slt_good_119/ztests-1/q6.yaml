script: |
  super -f tsv -I q6.sql | sort > q6.out

vector: true

inputs:
  - name: q6.sql
    data: |
      SELECT ALL col0 FROM tab0 WHERE NOT NULL IN ( + col1 ) AND NOT NULL NOT BETWEEN ( + col1 + col2 * - col1 ) AND ( NULL );
  - name: tab0

outputs:
  - name: q6.out
    data: |
