script: |
  super -f text -I q6.spq | sort > q6.out

inputs:
  - name: q6.spq
    data: |
      SELECT ALL col0 FROM tab0 (FORMAT parquet) WHERE NOT NULL IN ( + col1 ) AND NOT NULL NOT BETWEEN ( + col1 + col2 * - col1 ) AND ( NULL );
  - name: tab0

outputs:
  - name: q6.out
    data: |
