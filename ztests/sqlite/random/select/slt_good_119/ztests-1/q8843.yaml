script: |
  super -f text -I q8843.spq | sort > q8843.out

inputs:
  - name: q8843.spq
    data: |
      SELECT DISTINCT 69 FROM tab1 (FORMAT parquet), tab0 (FORMAT parquet) cor0, tab0 (FORMAT parquet) AS cor1;
  - name: tab0
  - name: tab1

outputs:
  - name: q8843.out
    data: |
      69
