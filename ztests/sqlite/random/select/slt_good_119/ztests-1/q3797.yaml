script: |
  super -f text -I q3797.spq | sort > q3797.out

inputs:
  - name: q3797.spq
    data: |
      SELECT * FROM tab2 (FORMAT parquet) AS cor0 WHERE NOT ( NOT NULL BETWEEN NULL AND ( NULL ) ) AND NULL NOT IN ( col0 + + col2 / col0 ) OR NOT NULL > ( NULL );
  - name: tab2

outputs:
  - name: q3797.out
    data: |
