script: |
  super -f text -I q400.spq | sort > q400.out

inputs:
  - name: q400.spq
    data: |
      SELECT * FROM tab1 (FORMAT parquet) WHERE NOT NULL NOT IN ( + col1 + col0 * col1 * col1 );
  - name: tab1

outputs:
  - name: q400.out
    data: |
