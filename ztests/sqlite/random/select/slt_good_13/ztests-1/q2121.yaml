script: |
  super -f text -I q2121.spq | sort > q2121.out

inputs:
  - name: q2121.spq
    data: |
      SELECT * FROM tab0 (FORMAT parquet) WHERE NOT NULL NOT IN ( + col1 * - col0 * col2 + col1 / col0 + + tab0.col1 * col2 );
  - name: tab0

outputs:
  - name: q2121.out
    data: |
