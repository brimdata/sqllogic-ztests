script: |
  super -f text -I q313.spq | sort > q313.out

inputs:
  - name: q313.spq
    data: |
      SELECT DISTINCT + col2 AS col1 FROM tab0 (FORMAT parquet) WHERE NOT col0 NOT IN ( tab0.col1 * col1 + col0 * - col1 );
  - name: tab0

outputs:
  - name: q313.out
    data: |
