script: |
  super -f tsv -I q588.sql | sort > q588.out

vector: true

inputs:
  - name: q588.sql
    data: |
      SELECT DISTINCT * FROM tab1 WHERE ( + col0 ) > NULL;
  - name: tab1

outputs:
  - name: q588.out
    data: |
