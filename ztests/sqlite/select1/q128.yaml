script: |
  super -f text -I q128.spq > q128.out

inputs:
  - name: q128.spq
    data: |
      SELECT b-c,
      a+b*2+c*3+d*4+e*5
      FROM t1 (FORMAT parquet)
      WHERE b>c
      AND a>b
      AND (e>a AND e<b)
      ORDER BY 2,1;
  - name: t1

outputs:
  - name: q128.out
    data: |
