script: |
  super -f text -I q584.spq > q584.out

inputs:
  - name: q584.spq
    data: |
      SELECT c,
      a+b*2+c*3+d*4
      FROM t1 (FORMAT parquet)
      WHERE b>c
      AND (a>b-2 AND a<b+2)
      AND (e>a AND e<b)
      ORDER BY 2,1;
  - name: t1

outputs:
  - name: q584.out
    data: |
