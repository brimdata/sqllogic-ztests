script: |
  super -f tsv -I q806.sql > q806.out

vector: true

inputs:
  - name: q806.sql
    data: |
      SELECT d,
      a+b*2+c*3+d*4+e*5,
      a+b*2,
      abs(a),
      d-e
      FROM t1
      WHERE (c<=d-2 OR c>=d+2)
      AND (a>b-2 AND a<b+2)
      ORDER BY 2,5,4,3,1;
  - name: t1

outputs:
  - name: q806.out
    data: |
      169	2501	502	168	4
