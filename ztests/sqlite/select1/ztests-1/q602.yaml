script: |
  super -f tsv -I q602.sql > q602.out

vector: true

inputs:
  - name: q602.sql
    data: |
      SELECT abs(b-c),
      b-c
      FROM t1
      WHERE e+d BETWEEN a+b-10 AND c+130
      AND (a>b-2 AND a<b+2)
      ORDER BY 1,2;
  - name: t1

outputs:
  - name: q602.out
    data: |
      1	-1
