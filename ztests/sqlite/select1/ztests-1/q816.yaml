script: |
  super -f tsv -I q816.sql > q816.out

vector: true

inputs:
  - name: q816.sql
    data: |
      SELECT c-d
      FROM t1
      WHERE d>e
      AND e+d BETWEEN a+b-10 AND c+130
      AND a>b
      ORDER BY 1;
  - name: t1

outputs:
  - name: q816.out
    data: |
