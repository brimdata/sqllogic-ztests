script: |
  super -f tsv -I q320.sql > q320.out

vector: true

inputs:
  - name: q320.sql
    data: |
      SELECT abs(a)
      FROM t1
      WHERE c BETWEEN b-2 AND d+2
      AND d>e
      AND (e>a AND e<b)
      ORDER BY 1;
  - name: t1

outputs:
  - name: q320.out
    data: |
      220
      245
