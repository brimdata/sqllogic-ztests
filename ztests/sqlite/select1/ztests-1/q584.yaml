script: |
  super -f tsv -I q584.sql > q584.out

vector: true

inputs:
  - name: q584.sql
    data: |
      SELECT c,
      a+b*2+c*3+d*4
      FROM t1
      WHERE b>c
      AND (a>b-2 AND a<b+2)
      AND (e>a AND e<b)
      ORDER BY 2,1;
  - name: t1

outputs:
  - name: q584.out
    data: |
