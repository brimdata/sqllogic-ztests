script: |
  super -f tsv -I q450.sql > q450.out

vector: true

inputs:
  - name: q450.sql
    data: |
      SELECT d-e,
      c-d,
      a
      FROM t1
      WHERE b>c
      AND (e>a AND e<b)
      AND c>d
      ORDER BY 1,2,3;
  - name: t1

outputs:
  - name: q450.out
    data: |
      -2	3	191
