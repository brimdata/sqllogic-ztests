script: |
  super -f tsv -I q66.sql > q66.out

vector: true

inputs:
  - name: q66.sql
    data: |
      SELECT e
      FROM t1
      WHERE (c<=d-2 OR c>=d+2)
      AND (a>b-2 AND a<b+2)
      ORDER BY 1;
  - name: t1

outputs:
  - name: q66.out
    data: |
      165
