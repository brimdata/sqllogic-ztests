script: |
  super -f tsv -I q605.sql > q605.out

vector: true

inputs:
  - name: q605.sql
    data: |
      SELECT abs(a),
      d
      FROM t1
      WHERE (e>a AND e<b)
      AND a>b
      AND c>d
      ORDER BY 2,1;
  - name: t1

outputs:
  - name: q605.out
    data: |
