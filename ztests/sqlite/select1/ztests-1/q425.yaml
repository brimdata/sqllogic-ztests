script: |
  super -f tsv -I q425.sql > q425.out

vector: true

inputs:
  - name: q425.sql
    data: |
      SELECT (a+b+c+d+e)/5,
      e,
      b
      FROM t1
      WHERE d>e
      AND (a>b-2 AND a<b+2)
      AND d NOT BETWEEN 110 AND 150
      ORDER BY 1,3,2;
  - name: t1

outputs:
  - name: q425.out
    data: |
      167	165	167
      182	180	181
