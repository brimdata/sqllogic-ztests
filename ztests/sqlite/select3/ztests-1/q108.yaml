script: |
  super -f tsv -I q108.sql | sort > q108.out

vector: true

inputs:
  - name: q108.sql
    data: |
      SELECT b-c,
      b,
      a+b*2+c*3+d*4
      FROM t1
      WHERE (a>b-2 AND a<b+2)
      AND a>b
      AND (c<=d-2 OR c>=d+2);
  - name: t1

outputs:
  - name: q108.out
    data: |
