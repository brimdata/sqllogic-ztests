script: |
  super -f tsv -I q112.sql | sort > q112.out

vector: true

inputs:
  - name: q112.sql
    data: |
      SELECT b-c,
      b,
      a+b*2+c*3+d*4
      FROM t1
      WHERE (c<=d-2 OR c>=d+2)
      AND (a>b-2 AND a<b+2)
      AND a>b;
  - name: t1

outputs:
  - name: q112.out
    data: |
