script: |
  super -f tsv -I q97.sql | sort > q97.out

vector: true

inputs:
  - name: q97.sql
    data: |
      SELECT a+b*2+c*3,
      (a+b+c+d+e)/5
      FROM t1
      WHERE b>c
      AND a>b
      AND (e>a AND e<b)
      ORDER BY 1,2;
  - name: t1

outputs:
  - name: q97.out
    data: |
